# 前端产品真实工作流验收记录

## 环境

- 前端：`http://127.0.0.1:5190`，真实 Vite Console
- 后端：`http://127.0.0.1:8000`，真实 FastAPI
- 数据库：本地 PostgreSQL `assetgraph_v1_acceptance_20260726`，端口 `55432`
- 浏览器：Chrome headless，`1440x900`
- 运行命令：`npm run accept:product-workflows`
- 结构化证据：`docs/evidence/frontend-product-workflow-acceptance-2026-07-29.json`

## 结果

共执行 18 个业务工作流：17 个通过，0 个失败，1 个因外部能力未启用而阻塞。

通过范围：业务概览、时间范围下钻、任务/通知抽屉、素材搜索和视图切换、素材详情和约束保存、素材组/素材包/素材缺口、素材导入和批量选材、模板筛选和详情、录屏视图、知识事实和来源、内容项目四步创建、项目简报/剧本/直播间/成片/交付/动态下钻、直播间场景和图层保存、成片编辑副本和时间轴保存、运营数据导入和四维分析、效果学习规律库、交付检查和撤回入口。

阻塞范围：真实麦兔草稿写入。当前验收环境没有启用麦兔写入能力，界面正确显示人工操作清单，未伪造自动完成。

## 本轮修复

- 素材约束保存使用后端认可的置顶/置底规则；素材包和素材缺口提交使用有效业务枚举。
- 内容项目向导修复“下一步”按钮在同一点击事件中变成提交按钮导致跳过素材选择的问题。
- 成片编辑副本增加持久化编辑锁，首次保存时间轴后才进入渲染队列，避免 worker 抢占造成保存 422。
- MinIO 不可用时使用本地文件系统降级存储，素材仍可导入、预览并参与批量选材；MinIO 可用时仍优先使用 MinIO。
- 录屏详情响应只返回业务字段，过滤数据库子表外键，避免上传成功后详情页返回 500。

## 其他验证

- 前端单测：78 passed
- 前端类型检查、产品文案门禁、生产构建：通过
- 后端测试：771 passed, 138 skipped
- `git diff --check`：通过
- 运行时最终页面内部码扫描：0；浏览器错误：0；非预期请求失败：0
- 页面切换时媒体预览/下载出现的 `ERR_ABORTED` 已单独记录为预期中止，不计为工作流失败。
