# AssetGraph 直播内容生产与运营闭环落地 Checklist

> 状态：执行中（Phase 0）
> 日期：2026-07-23
> 权威设计：[AssetGraph 直播内容生产与运营闭环设计](./2026-07-22-live-content-production-operations-closed-loop-design.md)
> 最终目标：[AssetGraph 最终目标](../final-goal.md)
> 适用范围：从当前仓库基线演进到权威设计第 30 节定义的完整系统

## 0. 使用规则

### 0.1 勾选规则

- [x] `CHK-0001` 指定 checklist owner，负责维护任务状态、证据链接、阻断项和设计偏差。
- [ ] `CHK-0002` 为每个 Phase 指定 product、engineering、data、security owner；涉及 UI 的 Phase 同时指定 design owner。
- [x] `CHK-0003` 约定严格按 `Phase 0 -> Phase 1 -> Phase 2 -> Phase 3 -> Phase 4 -> Phase 5 -> Phase 6 -> Phase 7 -> Phase 8` 推进；上一个 Phase 的退出门禁未全绿时，不开始下一个 Phase 的生产启用工作。
- [x] `CHK-0004` 允许为降低风险提前做 spike，但 spike 产物不得作为后续 Phase 已完成的依据，不得绕过当前 Phase 的门禁。
- [x] `CHK-0005` 任务只有在代码、追加迁移、API/schema、权限与审计、观测、文档、测试、回滚或对账方案均适用且完成后才能勾选。
- [x] `CHK-0006` 现有功能必须先通过本清单要求的契约和测试；“代码已经存在”本身不能作为勾选依据。
- [x] `CHK-0007` 每个勾选项在执行记录中填写 `owner / PR或commit / migration / test evidence / artifact或截图 / approved_by / completed_at`。
- [x] `CHK-0008` 父项只有在全部子项完成后勾选；不得以勾选父项代替子项证据。
- [x] `CHK-0009` 不适用项不能直接删除或当作完成；必须写 ADR 说明为何不适用、替代能力、影响和批准人，再将设计与本清单同步修改。
- [x] `CHK-0010` 任何实现偏离权威设计时先更新 ADR 和设计文档，再改 checklist；禁止只改任务措辞来掩盖契约变化。
- [x] `CHK-0011` 每个 Phase 开始前建立 2-6 周可验收里程碑、依赖图、风险清单和容量/成本预算，不预先填写没有依据的日历工期。
- [ ] `CHK-0012` 每个 Phase 结束时归档验收包，至少包含需求追踪矩阵、测试报告、数据迁移报告、安全审查、SLO/成本结果、回滚演练和已知限制。

### 0.2 单项完成定义

每个功能项在勾选前逐项确认；某项不适用时也要在执行记录中说明原因。

- [ ] `DOD-001` 领域行为、状态机、不变量和错误码已实现，失败行为为 fail closed。
- [ ] `DOD-002` 数据库变更使用新 migration；升级、回滚或前向修复、历史数据解释和大表影响已验证。
- [ ] `DOD-003` 写接口支持 expected revision 或 idempotency key；重复请求、并发修改和超时后的结果已测试。
- [ ] `DOD-004` API schema 有版本、示例、兼容窗口、consumer contract test 和弃用策略。
- [ ] `DOD-005` 权限、PolicyDecision、审计、敏感字段脱敏和保留策略已接入。
- [ ] `DOD-006` 领域 lineage、ArtifactRef、RunManifest、trace context、结构化日志和指标已接入。
- [ ] `DOD-007` 前端具备 loading、empty、error、warning、stale、partial、无权限和并发冲突状态，且支持实体 revision 深链。
- [ ] `DOD-008` 单元、属性、契约、集成和端到端测试按风险补齐，关键反例和故障路径已覆盖。
- [ ] `DOD-009` 运维 runbook、告警、kill switch、重试/对账、备份恢复和降级行为已文档化。
- [ ] `DOD-010` 产品、数据、安全和实际运营用户完成验收，验收证据可从任务记录反查。

### 0.3 执行记录模板

每完成一个 `CHK-*`，在项目管理系统或本文件末尾的执行日志按以下字段登记：

```text
check_id:
owner:
status: todo | in_progress | blocked | done
depends_on:
pull_request_or_commit:
migration:
test_command_and_result:
evidence_artifacts:
design_or_adr_refs:
known_limits:
approved_by:
completed_at:
```

---

## Phase 0. 不变量、演进边界与统一控制面

> 目标：先建立后续所有业务功能共享的稳定对象、版本、证据、安全和执行语义。
> 依赖：仅依赖当前仓库基线。

### 0.1 基线盘点与架构决策

- [x] `CHK-0101` 生成当前能力清单，逐项标记 `reuse / extend / migrate / retire`，覆盖 FastAPI、PostgreSQL、`/maitu/`、`/live-research/`、三个 worker、视频链路、Qwen3、MinIO、Milvus 和 Neo4j。
- [x] `CHK-0102` 生成当前数据对象与新领域对象映射，明确每个对象的 source of truth、兼容投影、owner 和下线条件。
- [x] `CHK-0103` 盘点旧 StoryBrief、VideoProductionJob、LiveRoomConfiguration、WorkbenchRun、Scene、layout hypothesis、JD 指标和历史交付数据的可迁移质量。
- [x] `CHK-0104` 为无法证明来源的历史字段定义 `unknown / legacy_import / legacy_delivery_unknown / descriptive_only` 语义，禁止反向猜测。
- [x] `CHK-0105` 建立 ADR 模板和索引，至少覆盖聚合边界、不可变 revision、时间语义、release/delivery/exposure、授权能力和证据等级。
- [x] `CHK-0106` 固定统一领域词汇：ProgramSegment、Shot、MaituSceneBlueprint、TimelineSegment、Artifact、Run、Release、Delivery、Exposure 不得互相代用。
- [x] `CHK-0107` 固定逻辑 ID、revision、schema version、事件 ID、幂等键、内容指纹和审计时间字段规范。
- [x] `CHK-0108` 固定编辑帧时间、媒体 PTS、会话毫秒时间和 event/processing time 的边界及转换规则。
- [x] `CHK-0109` 建立状态机变更规则和合法转移测试框架；非法跳转返回稳定错误码并记录审计。
- [ ] `CHK-0110` 记录当前容量基线：素材数、录屏小时/日、事件峰值、直播并发、渲染并发、artifact 增长、workflow 数量和 Browser-use 并发。
- [x] `CHK-0111` 建立外部 API/平台依赖登记，覆盖麦兔 API、麦兔 Web UI、DeepSeek、OpenAI、抖音采集端点和运营数据来源。
- [x] `CHK-0112` 为每个外部依赖登记用途、发送数据、凭据、配额、费用、超时、限流、数据区域、失败行为、fallback、owner 和退出方案。
- [x] `CHK-0113` 把 DeepSeek/OpenAI/ASR/OCR/视觉能力收敛到 provider adapter；领域 API 和持久化契约不得暴露供应商专有模型名。
- [x] `CHK-0114` 明确麦兔 authoritative API 的硬依赖和 fail-closed 行为；测试环境使用契约稳定的 fake/sandbox，不用宽松 mock 掩盖失败。

### 0.2 核心聚合、修订和 stale 传播

- [x] `CHK-0120` 设计并追加 ContentProject、ContentProjectRevision 表、约束、索引和 repository。
- [x] `CHK-0121` 设计并追加 StoryBrief、StoryBriefRevision 表，落实 `draft -> confirmed -> superseded` 和 confirmed 不可变。
- [x] `CHK-0122` 扩展 Script/ScriptBlock 为版本化内容对象，保留模型、提示、策略、事实引用和内容指纹。
- [x] `CHK-0123` 设计并追加 ContentProgramRevision、ProgramSegment 及 ScriptBlock 采用区间。
- [x] `CHK-0124` 设计并追加 ShotListRevision、Shot、ShotProjectionLink，确保 Shot 不保存可变路径或麦兔序号。
- [x] `CHK-0125` 设计并追加 ProductionVariant、ProductionVariantRevision，支持 `live_room / rendered_video` 且分支状态互不污染。
- [x] `CHK-0126` 将 LiveRoomConfiguration 映射为 live_room 分支配置，将 VideoProductionJob 映射为 rendered_video 执行聚合。
- [x] `CHK-0127` 实现每层 `derived_from`、source revision、producer role/strategy 和独立 fingerprint。
- [x] `CHK-0128` 实现上游修订变化后的 stale 传播图，覆盖 variant、BuildPlan、timeline、render、release candidate 和 attribution mapping。
- [x] `CHK-0129` 实现 immutable revision repository guard，阻止 confirmed/published/released 数据原地更新。
- [x] `CHK-0130` 为 revision 创建、重复确认、并发编辑、supersede 和 stale 传播补齐属性测试与事务测试。

### 0.3 Artifact、运行清单与可重建投影

- [x] `CHK-0140` 设计并追加 ArtifactRef，保存 media type、schema、URI、checksum、size、producer、输入引用、敏感级别和保留策略。
- [x] `CHK-0141` 实现内容寻址 artifact 写入、重复检测、校验、读取授权和对象存储版本保护。
- [x] `CHK-0142` 定义 RunManifest schema，固定输入修订、文件、inventory、素材/约束/权利、模板、事实、内容、模型、提示、代码、工具、随机种子和环境。
- [x] `CHK-0143` 实现规范化 manifest 和 input/output fingerprint 计算，增加跨运行一致性和差异解释工具。
- [x] `CHK-0144` 建立 transactional outbox 表、写事务、投影消费者 checkpoint、幂等消费和死信处理。
- [x] `CHK-0145` 设计 GraphProjectionVersion 和通用 projection watermark，但 Phase 0 不把 Neo4j 设为事实源。
- [x] `CHK-0146` 提供 OpenLineage 兼容投影，验证领域 lineage 在 trace 丢失时仍完整。
- [x] `CHK-0147` 在 API、队列、worker、模型和外部适配器间传播 OpenTelemetry trace/span context。
- [x] `CHK-0148` 对关键 manifest、授权、release 和操作结果实现服务端签名或 append-only hash chain。
- [x] `CHK-0149` 为 artifact 损坏、outbox 重放、重复消费、投影落后和签名失败建立测试与告警。

### 0.4 WorkflowRun、StepRun 与 HumanTask

- [x] `CHK-0160` 设计并追加 WorkflowRun、StepRun、HumanTask、ArtifactRef 关联和状态历史表。
- [x] `CHK-0161` 实现 parent/child、依赖、优先级、预算、排队原因和进度聚合。
- [x] `CHK-0162` 实现 PostgreSQL 队列 lease、heartbeat、claim token、lease version、超时回收和公平领取。
- [x] `CHK-0163` 为每个 Step 固定幂等键、副作用等级、最大尝试、退避、timeout、可取消点和补偿/对账策略。
- [x] `CHK-0164` 实现纯计算安全重试和副作用 `prepare -> authorize -> commit -> read-back/reconcile` 协议。
- [x] `CHK-0165` 实现 workflow 取消、等待人工、恢复和终态聚合，确保等待人工不占 Worker。
- [x] `CHK-0166` 实现 HumanTask claim、SLA、owner、revision、决定、结构化理由、过期和升级。
- [x] `CHK-0167` 将现有 WorkbenchRun、视频任务、直播研究任务和 retry queue 投影为兼容 WorkflowRun/StepRun。
- [x] `CHK-0168` 提供 `GET /api/workflow-runs/{run_code}`、取消接口和 HumanTask 查询/处理接口。
- [x] `CHK-0169` 测试 Worker 崩溃、重复领取、租约过期、取消竞态、超时、人工等待和未知副作用不会产生重复写入。
- [x] `CHK-0170` 用实测复杂度评估是否需要 Temporal；未达到门槛时记录“不引入”ADR，达到时保证领域 run code 和 lineage 不变。

### 0.5 策略决策、权限与执行授权

- [x] `CHK-0180` 建立最小权限能力矩阵，分离生产编辑、事实/模板发布、效果批准、草稿写入、上传、release 交付、图谱重建和开播。
- [x] `CHK-0181` 设计并追加 PolicyDecision、ExecutionAuthorization、ProtectedResourceRegistry。
- [x] `CHK-0182` 实现独立策略决策点，输入主体、目标、动作、plan/release hash、现场指纹、策略版本和批准链。
- [x] `CHK-0183` 实现短期、目标绑定、capability 绑定、nonce、过期、撤销和 single-use 授权。
- [x] `CHK-0184` 实现 Worker 提交时重新授权；preflight、workflow 状态或人工点击均不能替代 commit-time check。
- [x] `CHK-0185` 确保 `write_draft / upload_asset / deliver_release / go_live` 授权互不兼容。
- [x] `CHK-0186` 将参考房间、生产禁写对象、账号和外部资源纳入 ProtectedResourceRegistry，并在 preflight、PDP、Worker 三处独立检查。
- [x] `CHK-0187` 建立 feature flag 与 capability kill switch；当前 `go_live` 全环境默认关闭且无法签发。
- [x] `CHK-0188` 对越权、换目标、换 plan hash、过期、重放、授权服务不可用和注册表变化执行 fail-closed 测试。

### 0.6 Release、Delivery 与 Exposure 基础契约

- [x] `CHK-0200` 设计并追加 Release、ReleaseManifest、ReleaseApproval、DeliveryAttempt 和状态历史。
- [x] `CHK-0201` 固定 ReleaseManifest 通用外壳及 live_room/rendered_video carrier facet，禁止引用 `latest`。
- [x] `CHK-0202` 实现 candidate、validating、approval、delivery、failure、reconcile 和 revoke 合法转移。
- [x] `CHK-0203` 实现 release fingerprint、artifact checksum、批准记录、权利快照和 lineage 完整性门禁。
- [x] `CHK-0204` 实现 DeliveryAttempt 的目标、adapter、幂等键、授权、外部身份、请求/响应摘要、回读和证据契约。
- [x] `CHK-0205` 设计并追加 ContentExposureEvent append-only 基础表，禁止用“计划内容”代替实际曝光。
- [x] `CHK-0206` 用契约测试证明 workflow succeeded、release approved、delivery succeeded、release exposed 是四种独立事实。

### 0.7 指标、数据合同与证据等级基础契约

- [x] `CHK-0220` 设计并追加 MetricDefinition、MetricDefinitionRevision、DataContract 和 owner/version/status 字段。
- [x] `CHK-0221` 固定事件 envelope、event time/processing time、upsert/delete、幂等和 tombstone 语义。
- [x] `CHK-0222` 设计 AttributionRun/Result、PerformanceProfile、AssociationalEstimate、CausalEstimate 的边界。
- [x] `CHK-0223` 固定 `descriptive / associational / quasi_experimental / randomized` 证据等级，禁止人工直接抬级。
- [x] `CHK-0224` 设计 FeatureSnapshot、DecisionLog、LearningPolicy、EffectEligibilityPolicy 及 point-in-time 不变量。
- [x] `CHK-0225` 对同名不同口径指标、迟到事件、删除事件、未知 schema 和不合格数据批次建立 fail-closed 契约测试。

### 0.8 数据治理与保留底座

- [x] `CHK-0240` 给字段和 artifact 接入 `public / internal / confidential / restricted_personal / credential` 分类。
- [x] `CHK-0241` 建立角色加用途访问控制，覆盖导出、解密、批量查询、模型调用、图谱和删除。
- [x] `CHK-0242` 建立日志、截图、提示和模型响应的字段级脱敏规则；cookie/token/原始个人 payload 不可进入这些产物。
- [x] `CHK-0243` 固化原始录屏、个人事件、标准事件、汇总、关键审计和中间 artifact 的版本化保留策略。
- [x] `CHK-0244` 设计 DeletionRun、tombstone、legal hold、下游处理回执和 `partial_failed` 重试语义。
- [x] `CHK-0245` 实现凭据管理、轮换、最小发送字段和第三方 processor/区域/保留政策登记。

### 0.9 兼容迁移与 Console 壳

- [ ] `CHK-0260` 所有数据库变更使用追加 migration，并在真实数据副本验证耗时、锁和前向修复。
- [x] `CHK-0261` 保留旧素材几何和 duplicate_group 原语义，不自动提升为约束或用户分组。
- [x] `CHK-0262` 建立 legacy ContentProject、live_room variant、WorkflowRun 和 delivery unknown 兼容投影。
- [x] `CHK-0263` 保证旧 `layout-hypothesis.v1` 只读兼容，不原地转换成 content-strategy.v2。
- [x] `CHK-0264` 建立统一 Console 应用壳、登录态、一级导航、全局搜索入口、任务/通知中心和错误边界。
- [x] `CHK-0265` 实现稳定实体深链、revision 时间线、结构化 diff、来源与“被哪些运行/release 使用”。
- [x] `CHK-0266` 实现 draft 自动保存状态、expected revision 冲突提示和显式 confirm/publish/approve/authorize 命令。
- [x] `CHK-0267` 制定 `/maitu/`、`/live-research/` 迁移到稳定路由的功能等价、深链重定向和下线检查表。
- [x] `CHK-0268` 建立全局稳定错误码规范，错误展示影响、证据和下一步；warning/stale/insufficient_data 不冒充成功或失败。

### 0.10 Phase 0 退出门禁

- [x] `CHK-0290` 不可变 revision、幂等、并发修改、stale 传播和合法状态机契约测试全绿。
- [x] `CHK-0291` 授权换目标、过期、重放、跨 capability 和服务不可用均失败关闭。
- [x] `CHK-0292` Workflow 崩溃恢复、取消、人工等待和 reconcile 测试无重复副作用。
- [x] `CHK-0293` RunManifest、lineage、trace、outbox、签名和 artifact 校验可生成并可审计。
- [x] `CHK-0294` 旧主链通过兼容投影继续可读、可运行，迁移报告未伪造缺失来源。
- [x] `CHK-0295` 完成数据库 PITR 和对象 artifact 恢复基线演练，记录实际 RPO/RTO。
- [ ] `CHK-0296` Phase 0 产品、工程、数据、安全验收签字并归档验收包。

---

## Phase 1. 内容到麦兔草稿的第一条纵向闭环

> 目标：从目标与事实开始，经过 StoryBrief、剧本、节目段、Shot、麦兔 Blueprint、BuildPlan 和授权执行，得到证据完整的麦兔草稿 release。
> 依赖：Phase 0 全部退出门禁完成。

### 1.1 ContentProject 与目标/事实

- [ ] `CHK-1101` 实现 `POST/GET /api/content-projects`、详情和 expected-revision PATCH。
- [ ] `CHK-1102` 实现 project title、generation goal、目标时长、平台、受众、人设、语气、商品顺序、必含/禁用、互动/促单/视听要求。
- [ ] `CHK-1103` 支持选择多份固定版本的已批准 FactCard，并在确认时重验批准状态、有效期和适用范围。
- [ ] `CHK-1104` 保证 ContentProject 不要求 liveRoomId、麦兔标题、画布、帧率或 RenderProfile。
- [ ] `CHK-1105` 建立 ContentProject 工作区“目标与事实”视图，展示事实字段来源、有效期、冲突和未解决问题。

### 1.2 DesignBrief 解析与确认

- [ ] `CHK-1120` 实现受限 `parse-brief` provider 调用，原始文本按 untrusted user data 传入，不可覆盖系统规则。
- [ ] `CHK-1121` 输出 objective、theme/story、audience、priorities、persona、tone、duration、must include/avoid、staging、interaction、conversion、visual、audio 和 open questions。
- [ ] `CHK-1122` 对解析结果做严格 schema 校验、长度/枚举限制和提示注入防护。
- [ ] `CHK-1123` 最多提出 1-3 个真正影响结果的问题并提供推荐答案，允许跳过非阻断问题。
- [ ] `CHK-1124` 保存原文、模型原始响应 artifact、解析值、用户修改值、provider/model/prompt revision 和 diff。
- [ ] `CHK-1125` 实现 DesignBrief 可编辑界面、自动保存、问题回答和显式确认预览。

### 1.3 StoryBrief、剧本、节目结构与 Shot

- [ ] `CHK-1140` 实现确定性 `DesignBrief -> StoryBriefRevision` 转换，固定事实、模板贡献占位和输入 fingerprint。
- [ ] `CHK-1141` 实现 `POST /content-projects/{code}/confirm`，重复请求幂等，确认后不可原地修改。
- [ ] `CHK-1142` 实现生成上下文编译器，严格分离 system baseline、批准事实、用户目标和外部参考。
- [ ] `CHK-1143` 实现 Script/ScriptBlock 生成，保存模块类型、台词、时长、商品、CTA/互动、事实引用和来源。
- [ ] `CHK-1144` 实现 FactCitation 逐事实句扫描；价格、促销、库存、赠品和功效无批准事实时阻断。
- [ ] `CHK-1145` 允许模型生成非事实连接语和表达，不再要求逐句复制事实卡原句。
- [ ] `CHK-1146` 实现 ContentProgramRevision 和有序 ProgramSegment，记录语义目标、进入/退出条件和 ScriptBlock 区间。
- [ ] `CHK-1147` 实现 `generate-shot-list`，生成稳定 Shot、构图意图、素材角色需求、音频动作、连续性和验收标准。
- [ ] `CHK-1148` 校验必讲块映射完整、来源镜头存在、无循环映射，并确保 Shot 与 `MaituSceneBlueprint`、`TimelineSegment` 概念分离。
- [ ] `CHK-1149` 实现剧本、ProgramSegment 和 ShotList 修订时间线、来源下钻和人工新修订。

### 1.4 live_room ProductionVariant 与输入确认

- [ ] `CHK-1160` 实现 ContentProject 下创建/查询 ProductionVariant。
- [ ] `CHK-1161` live_room 分支要求目标空白草稿 liveRoomId、期望标题、现场保护策略和 `auto_write_draft / plan_only`。
- [ ] `CHK-1162` 实现 authoritative API 的房间存在性、草稿态、未开播、空白状态和目标身份 attestation。
- [ ] `CHK-1163` 先复用当前 inventory 建立确定性执行候选集，固定 InventorySnapshot 和素材绑定修订。
- [ ] `CHK-1164` 实现 ProductionVariant confirm，固定 StoryBrief/Script/ShotList、分支目标、素材输入和 fingerprint。
- [ ] `CHK-1165` liveRoomId、标题、现场或 inventory 缺失时拒绝确认；ContentProject 内容确认不受影响。
- [ ] `CHK-1166` 实现 live_room 配置页连续流程和“有效输入”预览。

### 1.5 MaituSceneBlueprint、LayerBlueprint 与 BuildPlan

- [ ] `CHK-1180` 从固定 ShotList 生成有序 MaituSceneBlueprint，记录 ProgramSegment/Shot、切换策略和预计活跃区间。
- [ ] `CHK-1181` 生成稳定 LayerBlueprint，包含 role、asset/binding revision、归一化几何、z-order、视音频属性和来源。
- [ ] `CHK-1182` 创建 ShotProjectionLink，支持一对多和多对一并保存关系类型和适用区间。
- [ ] `CHK-1183` 实现确定性 BuildPlan 编译器，固定 content/variant/revision/inventory/material/policy/site fingerprint。
- [ ] `CHK-1184` 仅允许 rename、默认空场景、创建场景、插入白名单素材、写脚本、受控属性、保存和只读验证。
- [ ] `CHK-1185` 拒绝未知 operation、任意 selector/URL、白名单外素材、非目标房间、清空房间和开播动作。
- [ ] `CHK-1186` 每个 operation 生成前置条件、后置条件、幂等键、capability、来源 Shot/ScriptBlock 和证据要求。
- [ ] `CHK-1187` 实现时长估算和约 50% 偏差 warning；单一时长偏差不得阻断。
- [ ] `CHK-1188` 生成结构化素材需求、冲突和分支质量报告。

### 1.6 门禁、preflight、授权与草稿执行

- [ ] `CHK-1200` 实现统一七段门禁执行器：身份版本、授权事实、输入边界、结构引用、执行约束、分支质量、证据完整性。
- [ ] `CHK-1201` 每项门禁输出 `pass/warning/blocked/not_applicable`、稳定规则码、规则版本、证据和修复建议。
- [ ] `CHK-1202` 静态门禁通过后自动执行只读 preflight，验证登录、URL、房间 ID、未开播、空白、现场 fingerprint 和素材可用性。
- [ ] `CHK-1203` `plan_only=true` 永不申请执行授权；`auto_write_draft=true` 仅在无 blocked 时请求短期 write_draft 授权。
- [ ] `CHK-1204` 授权绑定 BuildPlan hash、liveRoomId、现场 fingerprint 和单次 capability。
- [ ] `CHK-1205` 扩展 Browser-use Worker 支持安全 rename、创建场景、插入素材、写脚本、受控属性和保存。
- [ ] `CHK-1206` 每个写操作前检查前置条件和授权，操作后回读现场身份/属性并保存截图、DOM 摘要或必要网络证据。
- [ ] `CHK-1207` 对超时或不确定响应进入 reconcile_required，禁止直接重放写操作。
- [ ] `CHK-1208` finalize 逐场景/逐图层比较 Blueprint，输出偏差、证据完整率和现场 fingerprint。
- [ ] `CHK-1209` 缺层、错素材、错房间、未保存、现场变化或开播态必须停止；软视觉偏差只可 warning。
- [ ] `CHK-1210` 完成后保持 `ready_for_go_live=false`，Worker、API 和 UI 均不存在隐式开播路径。

### 1.7 Release、回读与修改流程

- [ ] `CHK-1220` 生成 live_room_draft release candidate，manifest 固定完整内容链、素材、inventory、BuildPlan、QC、授权和证据。
- [ ] `CHK-1221` 实现 release 校验、批准、草稿 DeliveryAttempt、authoritative readback 和 100% 证据完整率门禁。
- [ ] `CHK-1222` 成功写入不等于曝光；不创建虚假的 ContentExposureEvent。
- [ ] `CHK-1223` 实现 `clone` 到新空白房间，复制业务输入但清除旧目标 fingerprint、授权和执行状态。
- [ ] `CHK-1224` 禁止第一版对非空旧房间做增量重建、清空或差异回滚。
- [ ] `CHK-1225` 实现从 operation -> `LayerBlueprint/MaituSceneBlueprint` -> Shot -> ProgramSegment -> ScriptBlock -> StoryBrief -> facts/templates 的全链路下钻。
- [ ] `CHK-1226` 在 live-room UI 分开展示 workflow、quality、release、delivery 和现场 readback 状态。

### 1.8 Phase 1 退出门禁

- [ ] `CHK-1290` 端到端用例从 ContentProject 输入生成一个受保护规则外的空白麦兔草稿，并完成不可变 release。
- [ ] `CHK-1291` 每个事实句、Shot、`MaituSceneBlueprint`、`LayerBlueprint` 和 operation 均通过 `ShotProjectionLink` 显式回溯；旧 Scene/历史场景投影不能代替该验收，release 证据完整率 100%。
- [ ] `CHK-1292` 非空、已开播、错房间、现场变化、白名单外素材和未知操作均被拒绝。
- [ ] `CHK-1293` 注入网络超时和 Worker 崩溃后能够 reconcile，且无重复场景、素材、脚本或保存动作。
- [ ] `CHK-1294` ContentProject 无房间仍可确认；live_room 分支缺 title/ID/现场不能确认。
- [ ] `CHK-1295` 同一输入重放可解释结构结果和外部差异，RunManifest、指纹和现场证据齐全。
- [ ] `CHK-1296` Phase 1 产品、工程、数据、安全和运营验收签字并归档验收包。

---

## Phase 2. 素材约束、选材缺口与外部内容模板

> 目标：补齐要素资产库和外部录屏到内容策略模板，并接入 Phase 1 主链。
> 依赖：Phase 1 全部退出门禁完成。

### 2.1 素材身份、版本、Rendition 与同步生命周期

- [ ] `CHK-2101` 将 Asset 明确为逻辑作品，追加 AssetVersion、AssetFile/Rendition 和 derived_from 关系。
- [ ] `CHK-2102` 为 Asset/AssetVersion 落库三个相互独立的分类维度：单值 `media_kind`、多值 `material_role` 和单值 `execution_capability`；数据库约束、schema 和 revision fingerprint 均包含这三个维度。
- [ ] `CHK-2103` 实现 `media_kind = image/video/audio/digital_human/text/template_preview/document`，禁止以扩展名或麦兔页签作为持久化业务语义。
- [ ] `CHK-2104` 实现多值 `material_role = background/product_display/digital_human/brand_title/promotion_text/decoration_foreground/supporting_video/voice/background_music/sound_effect`，素材包、素材需求和关系约束只引用该维度。
- [ ] `CHK-2105` 实现 `execution_capability = maitu_bound/local_only/reference_only/unavailable`，状态只能由可验证绑定与生命周期证据确定，不得由 `media_kind`、`material_role`、`asset_type` 或 `maitu_category` 推断。
- [ ] `CHK-2106` 迁移现有 `asset_type/maitu_category` 为来源/兼容字段；为无法确定的三维分类创建人工校正任务，禁止通过不可靠映射自动赋予 `maitu_bound` 或可执行业务角色。
- [ ] `CHK-2107` 扩展素材创建、详情、列表、批量更新和筛选 API，分别读写/查询三个维度，并对未知枚举、单值/多值混用和不合法组合返回稳定错误码。
- [ ] `CHK-2108` 在素材库和详情 UI 中分别展示媒体类型、业务角色复选和执行能力；提供迁移待确认筛选、来源证据和批量校正，不把三者合并为一个“类型”控件。
- [ ] `CHK-2109` 选材、素材包解析、ResolvedMaterialSnapshot 和 BuildPlan 门禁只消费已固定的三维分类；`template_preview` 默认 `reference_only`，`local_only/reference_only/unavailable` 均不得冒充可写入麦兔素材。
- [ ] `CHK-2110` 保存内容 checksum、MIME、尺寸/时长、存储相对路径、技术元数据和生成运行。
- [ ] `CHK-2111` 版本化麦兔 material_id、页签、最近确认时间、binding revision 和 inventory evidence。
- [ ] `CHK-2112` 实现 `发现 -> 对账 -> 软失效 -> 人工确认/再次发现`，一次同步缺失不得直接 unavailable。
- [ ] `CHK-2113` 实现连续快照或明确现场证据确认失效，历史配置继续引用原绑定和证据。
- [ ] `CHK-2114` 保存分析模型、提示、参数、输出 checksum、质量和人工覆盖；重分析不覆盖旧结果。
- [ ] `CHK-2115` 用 checksum 和 perceptual hash 标记重复候选，合并时保留别名、来源和历史引用。
- [ ] `CHK-2116` 实现 assets versions/renditions API 和同步任务 UI，展示差异、质量、失败重试和软失效。

### 2.2 RightsGrant 与发布影响

- [ ] `CHK-2120` 实现 RightsGrant 主体、来源、用途、渠道、地域、有效期、修改/衍生、AI、署名、肖像/声音、凭证和撤销字段。
- [ ] `CHK-2121` 实现 `draft -> active -> expired/revoked/superseded` 和 `deny/revoked > narrower > broader` 合并。
- [ ] `CHK-2122` 未知权利素材允许受控分析但不得进入可交付 release。
- [ ] `CHK-2123` 在选材、release candidate、delivery 和后续 schedule 分别重验权利。
- [ ] `CHK-2124` 实现 grant 创建、revoke、impact API 和受影响 variant/release/decision HumanTask。
- [ ] `CHK-2125` 撤销/到期立即排除未来候选，阻止新交付但不删除历史证据。
- [ ] `CHK-2126` Rendition 默认继承并收紧源权利，不得扩大许可。

### 2.3 约束 Profile、命名区域与规则构建器

- [ ] `CHK-2140` 实现 AssetConstraintProfile/Revision、稳定 constraint_key、scope、hardness、parameters 和 fingerprint。
- [ ] `CHK-2141` 实现命名区域、矩形/多边形、锚点和归一化 `[0,1]` 坐标；保留原像素为证据。
- [ ] `CHK-2142` 实现 `allowed_region / forbidden_region / provide_named_region / require_named_region`。
- [ ] `CHK-2143` 实现 `preserve_aspect_ratio / size_range / scale_range / crop_policy / rotation_policy`。
- [ ] `CHK-2144` 实现 `pin_layer_top / pin_layer_bottom / above_role / below_role / avoid_overlap`。
- [ ] `CHK-2145` 实现 `align_anchor / distance_range / loop_policy / mute_policy / volume_range`。
- [ ] `CHK-2146` 实现背景 table_surface/surface_line 与商品 bottom_center 的桌面摆放组合规则。
- [ ] `CHK-2147` 实现多个硬约束取交集、soft 分层目标、系统规则不可覆盖和显式 constraint_key override。
- [ ] `CHK-2148` 实现房间私有覆盖 diff、原因、操作者和“提升为全局新修订”的二次确认。
- [ ] `CHK-2149` 提供约束 profile/revisions API 和表单式规则构建器、区域预览、规则模板及冲突展示。

### 2.4 求解器契约与诊断

- [ ] `CHK-2160` 定义 solver-independent ConstraintProblem、变量、硬/软约束、目标、预算、seed 和 warm-start schema。
- [ ] `CHK-2161` 建立代表性问题集，覆盖角色次数、排他、z-order、几何、桌面区域、音频和无解冲突。
- [ ] `CHK-2162` 通过 ADR 选择 OR-Tools CP-SAT/MIP 或维持等价 adapter；业务服务不得手写回溯求解器。
- [ ] `CHK-2163` 实现 `optimal/feasible/infeasible/unknown/invalid_model`，unknown 不得当作 infeasible 或自动可执行。
- [ ] `CHK-2164` feasible 保存 gap；infeasible 返回近似最小冲突集、来源实体和可执行修复。
- [ ] `CHK-2165` 固定 solver/version/variables/weights/seed/time/memory 并保存求解证据 artifact。
- [ ] `CHK-2166` 对确定性、超时、无解解释、整数精度、软目标不抵消硬约束做回归和属性测试。

### 2.5 素材分组与素材模板包

- [ ] `CHK-2180` 实现 AssetGroup 和多对多成员；允许跨组重复、不允许嵌套、不承载约束。
- [ ] `CHK-2181` 实现分组创建/查询/修改/删除、成员批量替换接口和并发保护。
- [ ] `CHK-2182` 实现 MaterialPack、Revision、Entry、Exclusivity 和 `draft/published/superseded/archived`。
- [ ] `CHK-2183` 分类包只属于一个 role 且不可递归；总体包可引用分类包但不可引用总体包。
- [ ] `CHK-2184` 实现 required/optional/alternative、min/max occurrences、scope、pack constraints 和 alternative_set_key。
- [ ] `CHK-2185` 实现角色域 exclusive_roles，两个不兼容排他包必须报告冲突。
- [ ] `CHK-2186` 实现每个 role 的沿用/追加/替换；替换显式移除总体包该域内容与排他。
- [ ] `CHK-2187` 合并同资产来源、`required > alternative > optional`、硬规则和来源列表。
- [ ] `CHK-2188` 发布修订不可变，编辑跟随 latest published，确认时展开为明确素材编码而不是动态分组引用。
- [ ] `CHK-2189` 实现 material pack create/revision/publish/resolve API，返回白名单、来源、规则、冲突和 fingerprint。
- [ ] `CHK-2190` 实现分组和素材包工作区，支持批量成员、版本 diff、发布、排他和解析预览。

### 2.6 分支选材、解释排序与 AssetGap

- [ ] `CHK-2200` 分支 UI 分开选择总体包、role 模式/分类包、分组和零散素材。
- [ ] `CHK-2201` 每个素材支持 optional 或至少使用一次，分别支持 live_room 约束覆盖和 video 时间轴/画幅覆盖。
- [ ] `CHK-2202` 确认时固定包 revision、展开分组、去重、合并约束/权利、创建 ResolvedMaterialSnapshot。
- [ ] `CHK-2203` 先做白名单、执行能力、权利、必用/排他和硬约束确定性过滤，再执行排序。
- [ ] `CHK-2204` 排序综合 role、语义、画幅/时长、质量、soft、重复惩罚和合格效果证据。
- [ ] `CHK-2205` 返回分项分数、版本、排除码、来源和选择理由；新素材无效果样本时效果分中性。
- [ ] `CHK-2206` 定义 AssetGap 的 open/candidate_found/resolved/waived/obsolete 状态和来源链。
- [ ] `CHK-2207` candidate_found 前完成分支绑定、RightsGrant、授权和约束预检；resolved 固定资产/version/rendition/binding/处理人。
- [ ] `CHK-2208` waived 只对当前分支 revision 生效；新素材匹配不得静默修改已确认输入。
- [ ] `CHK-2209` 实现 gap 查询、修改、resolve API 和缺口修复 UI。
- [ ] `CHK-2210` 实现素材详情“效果与关系”，低样本结果标记不可自动推荐。

### 2.7 外部直播采集与统一媒体时间线

- [ ] `CHK-2240` 实现 WatchTarget 的平台 adapter 边界，首期抖音；记录采集授权、条款版本、用途、owner、访问和保留。
- [ ] `CHK-2241` 无合法依据、来源撤销或 kill switch 开启时不得开始/继续采集。
- [ ] `CHK-2242` 固定 StreamCap/douyinLive 版本，保存 TS 分片、原始事件、checksum、工具版本和采集配置。
- [ ] `CHK-2243` 为 CaptureSession 建立 media-timeline.v1 和统一 `[start_ms,end_ms)` 半开区间。
- [ ] `CHK-2244` 将分片、音频、ASR token/segment、OCR、关键帧、视觉区间和互动摘要映射到统一时钟。
- [ ] `CHK-2245` 检测掉帧、断流、漂移和缺失通道，保存校准方式、置信度和质量 flags。
- [ ] `CHK-2246` 分离原始、标准和发布三层存储与访问，不按数组下标拼接多模态结果。
- [ ] `CHK-2247` 实现 retention worker、删除/隔离、断点恢复、心跳和 sidecar 故障处理。
- [ ] `CHK-2248` 实现来源直播间和录屏场次 UI，展示状态、媒体、质量、互动摘要、保留期限和证据。

### 2.8 清洗与 content-strategy.v2

- [ ] `CHK-2260` 执行 ASR、OCR、关键帧、视觉和互动摘要，保存 provider/model/prompt/schema/checksum 和 AnalysisRun。
- [ ] `CHK-2261` 实现转写校正、噪声清理、场景边界、内容模块和置信度编辑。
- [ ] `CHK-2262` 识别并隔离来源商品事实、价格、促销、平台噪声和一次性活动信息，保留来源证据但不进入发布投影。
- [ ] `CHK-2263` 一个模板草稿和发布 projection 必须且只能绑定一个来源直播间，所选 CaptureSession 必须全部属于该直播间；发现跨房间场次时以稳定规则码阻断确认/发布，并要求拆成不同模板。跨直播间组合只能在 ContentProject 的主/次模板选择阶段发生。
- [ ] `CHK-2264` 实现 content-strategy.v2 的 target_category、program_outline、duration/module/product rotation/interaction/conversion/host/material policies。
- [ ] `CHK-2265` 实现 reviewed_examples 槽位化、layout_reference 和完整 provenance。
- [ ] `CHK-2266` 分别持久化 `content_readiness`、`layout_fidelity`、`buildability`，API/DB/UI 使用定义内状态。
- [ ] `CHK-2267` 实现媒体完整性、时间线、ASR/OCR、事实去除、模块证据、人工审核、相似性和 schema 发布门禁。
- [ ] `CHK-2268` 实现连续文本重合、品牌/主播特征模仿、版权画面、个人与敏感信息检查和人工审阅。
- [ ] `CHK-2269` 允许 `content_readiness=ready`、`layout_fidelity=none|approximate`、`buildability=reference_only` 发布；近似布局缺失不阻断内容模板。
- [ ] `CHK-2270` 保持 layout-hypothesis.v1 只读；新发布接口按 contract version 产生不可变 projection。
- [ ] `CHK-2271` 实现播放器、ASR/OCR、模块和来源证据联动的数据清洗 UI。
- [ ] `CHK-2272` 实现模板草稿、问题、整体审核、发布、不可变版本和生产使用记录 UI。

### 2.9 主次模板与生产主链接入

- [ ] `CHK-2300` 在 ContentProject 支持最多一个主模板、任意数量次要模板和无主模板状态。
- [ ] `CHK-2301` 按品类/目标推荐主模板和次要模块贡献，展示兼容、重复和冲突。
- [ ] `CHK-2302` 允许用户接受或修改模块贡献，并固化 TemplateContributionDecision。
- [ ] `CHK-2303` 实现主模板控制总体阶段，次要模板仅补缺或经显式模块决策替换。
- [ ] `CHK-2304` 上下文编译器按品类过滤、模块检索、骨架去重，只发送结构摘要和采用例句。
- [ ] `CHK-2305` 生成结果列出采用、未采用和冲突来源；模板例句永远不能进入事实白名单。
- [ ] `CHK-2306` 把模板 material_cues 转成 Shot/分支素材需求，不把 reference_only layout 当可执行 Blueprint。
- [ ] `CHK-2307` 将素材 snapshot、constraint solver、rights 和 gap 门禁接入 Phase 1 variant 生成与 release。

### 2.10 Phase 2 退出门禁

- [ ] `CHK-2389` 验证 `media_kind`、多值 `material_role`、`execution_capability` 可独立保存/筛选/修订：同一素材可拥有多个业务角色，`template_preview/reference_only` 和 `local_only` 不能进入麦兔写入，旧 `asset_type/maitu_category` 不能隐式赋予角色或执行能力。
- [ ] `CHK-2390` 验证同一素材可加入两组且独立移除，分组变化不影响历史 snapshot。
- [ ] `CHK-2391` 验证总体/分类包沿用、追加、替换、必用、备选和排他全部行为。
- [ ] `CHK-2392` 验证桌面区域/锚点/层级约束可解，无命名区域或冲突时有可执行诊断。
- [ ] `CHK-2393` 验证未绑定必用素材阻断、普通未绑定候选排除、一次同步缺失不误删。
- [ ] `CHK-2394` 验证完整选材解释和 AssetGap 从 open 到 resolved 的不可变证据。
- [ ] `CHK-2395` 从同一抖音来源直播间的多场录屏发布一个去事实化内容模板，全部模块可反查媒体区间；混入另一个直播间的 CaptureSession 时确认和发布均被拒绝并提示拆分模板。
- [ ] `CHK-2396` reference_only 模板可以贡献内容但无法通过可执行布局身份门禁。
- [ ] `CHK-2397` 权利撤销影响分析完整，未来选材/交付失败关闭且历史证据保留。
- [ ] `CHK-2398` Phase 2 产品、工程、数据、安全和运营验收签字并归档验收包。

---

## Phase 3. 同内容成片、剪辑时间轴与交付

> 目标：同一内容修订独立产出麦兔草稿和竖屏成片，建立确定性时间轴、渲染质量与视频 release。
> 依赖：Phase 2 全部退出门禁完成。

### 3.1 rendered_video 分支与 ProductionTimeline

- [ ] `CHK-3101` 支持从已确认 ContentProject 或 live_room variant 的固定内容修订创建 rendered_video variant。
- [ ] `CHK-3102` 固定画布、帧率、目标时长策略、RenderProfile、音频/字幕策略和交付渠道。
- [ ] `CHK-3103` 确保 video 和 live_room 分支状态、运行、素材覆盖、release 和失败互不污染。
- [ ] `CHK-3104` 实现 ProductionTimeline 权威对象并采用/严格映射 OTIO Timeline/Stack/Track/Clip/Gap/Transition。
- [ ] `CHK-3105` 使用 RationalTime/TimeRange 表达帧边界、source range、变速和 transition，不以毫秒替代编辑时间。
- [ ] `CHK-3106` 实现 TimelineSegment 的 track、timeline/source range、playback、来源内容、asset checksum、transform/crop、转场、音量和 artifact refs。
- [ ] `CHK-3107` 支持画面、overlay、数字人口播/配音、BGM、音效和字幕轨。
- [ ] `CHK-3108` 校验重叠、负时长、timebase、source available range 和连续性。
- [ ] `CHK-3109` 创建 ShotProjectionLink 和 TimeMapping，将编辑时间映射为媒体 PTS 与会话毫秒半开区间。
- [ ] `CHK-3110` 实现 timeline GET/PUT、expected revision、结构化调整和 stale 传播。

### 3.2 阶段化生产与内容寻址 artifacts

- [ ] `CHK-3120` 将 brief、script、shot planning、asset selection、voice、subtitle、render、QC 统一接入 WorkflowRun/StepRun。
- [ ] `CHK-3121` 每个阶段只读固定输入 artifact，输出内容寻址 artifact、manifest 和 lineage。
- [ ] `CHK-3122` 支持从最近成功阶段重试，并保证变更输入后创建新 run/revision。
- [ ] `CHK-3123` 复用并版本化 Kokoro TTS provider，固定声音授权、模型、参数、音频 checksum 和响度目标。
- [ ] `CHK-3124` 生成版本化 ASS 字幕，保存 ScriptBlock/word timing、字体、样式、安全区和 checksum。
- [ ] `CHK-3125` 生成 poster、asset plan、render log、quality report 和最终 video artifacts。
- [ ] `CHK-3126` 记录每步 CPU/GPU、耗时、token、存储和估算/实际成本。

### 3.3 RenderManifest、FFmpeg 与质量门禁

- [ ] `CHK-3140` 版本化 RenderProfile，默认 1080x1920 H.264/AAC 但不在业务逻辑硬编码参数。
- [ ] `CHK-3141` 用结构化 FFmpeg 命令构建器生成滤镜图；禁止拼接未校验的 shell 输入。
- [ ] `CHK-3142` RenderManifest 固定 timeline、输入 checksum、命令、滤镜、工具版本、seed 和资源使用。
- [ ] `CHK-3143` QC 覆盖 ffprobe、编解码、分辨率、帧率、时长和音视频同步。
- [ ] `CHK-3144` QC 覆盖黑帧、冻结帧、静音、削波/响度、结尾截断。
- [ ] `CHK-3145` QC 覆盖字幕安全区/可读时长、必讲 ScriptBlock、素材许可和文件 checksum。
- [ ] `CHK-3146` blocked QC 保留诊断产物但禁止发布；人工 waiver 绑定产物 revision、理由、批准者和有效范围。
- [ ] `CHK-3147` 对同一 RenderManifest 重试生成结构差异报告，解释外部工具造成的输出差异。

### 3.4 视频 release、provenance 与 Delivery

- [ ] `CHK-3160` QC 通过只生成 rendered_video release candidate，不自动视为批准或已交付。
- [ ] `CHK-3161` 批准后创建 ReleaseManifest，固定内容、timeline、素材/权利、render/QC、artifact 和策略版本。
- [ ] `CHK-3162` 生成 IPTC sidecar，包含权利、技术元数据、AI 生成/编辑声明和平台扩展。
- [ ] `CHK-3163` 评估并实现 C2PA Content Credentials，记录 ingredient；不能实现时通过 ADR 明确兼容边界和退出条件。
- [ ] `CHK-3164` 实现资产库登记和目标渠道 DeliveryAttempt，每次提交重验权利和授权。
- [ ] `CHK-3165` 麦兔上传作为独立 upload_asset capability，回读目标 material identity 和 checksum，不等同于本地渲染完成。
- [ ] `CHK-3166` 实现 revoke 对未来交付、排播和效果决策的影响传播。

### 3.5 成片生产 UI

- [ ] `CHK-3180` 建立 `/production/videos` 列表、筛选、运行进度、成本、异常和深链。
- [ ] `CHK-3181` 实现结构化 segment 顺序、起止、转场、音轨和字幕调整，不建设通用 NLE。
- [ ] `CHK-3182` 展示 Shot/ScriptBlock/素材/音频/字幕来源、timeline revision diff 和 stale 状态。
- [ ] `CHK-3183` 分开展示 production completed、quality failed、release candidate、approved、delivered 和 exposed。
- [ ] `CHK-3184` 支持预览、poster、QC 规则下钻、waiver 和产物下载权限。

### 3.6 Phase 3 退出门禁

- [ ] `CHK-3290` 同一 StoryBrief/Script/ShotList 独立产出麦兔草稿和竖屏成片，任一失败不改变另一分支。
- [ ] `CHK-3291` 每个 TimelineSegment 可回溯 Shot、ScriptBlock、AssetFile、voice 和 subtitle artifact。
- [ ] `CHK-3292` 帧率、TimeMapping、素材权利、RenderManifest 和 release checksum 可重放验证。
- [ ] `CHK-3293` 黑帧、静音、字幕安全区、必讲缺失和权利失败均阻止视频 release。
- [ ] `CHK-3294` 本地完成、release 批准、渠道交付、麦兔上传和曝光状态无混淆。
- [ ] `CHK-3295` Phase 3 产品、工程、数据、安全和运营验收签字并归档验收包。

---

## Phase 4. 真实发布、曝光与人工归因闭环

> 目标：将准确 release、实际曝光和版本化指标对齐，产出可下钻的描述性与关联性结果。
> 依赖：Phase 3 全部退出门禁完成。

### 4.1 发布与运营工作区

- [ ] `CHK-4101` 建立 `/production/releases` 列表、manifest、批准、delivery、revoke、影响和证据详情。
- [ ] `CHK-4102` 建立 `/operations/live-sessions`，创建 LiveSession 并绑定精确 ReleaseManifest。
- [ ] `CHK-4103` 录入平台、外部会话、时区、开始/结束、账号、目标资源和 source evidence。
- [ ] `CHK-4104` 在 UI 明确区分 planned、delivered、exposed，未有 served log 时不得默认已曝光。
- [ ] `CHK-4105` 建立异常、人工任务、时间对齐、缺失通道和数据新鲜度视图。

### 4.2 ContentExposureEvent 与内容时间线

- [ ] `CHK-4120` 实现 exposure 写入 API，支持平台 served log、人工录入和录屏识别三种 source type。
- [ ] `CHK-4121` 保存 release/variant/revision、事件起止、ProgramSegment、Shot、载体投影、素材、商品、CTA、策略和置信度。
- [ ] `CHK-4122` 切场、断流、重复、人工替换和 release 切换使用追加/修正事件，不覆盖历史。
- [ ] `CHK-4123` 保证 LiveSession 同一时刻最多一个有效 release exposure，冲突进入 reconcile。
- [ ] `CHK-4124` 实现 ContentTimelineSpan，从实际 exposure 映射内容对象和素材。
- [ ] `CHK-4125` 实现 TimeMapping 的 offset、drift、证据、coverage 和版本；量化无法识别区间。
- [ ] `CHK-4126` 时间对齐不达阈值时降级到 session 级或 insufficient_data。
- [ ] `CHK-4127` 提供 content timeline GET/PUT 和结构化人工校正，校正创建新 revision。

### 4.3 事件摄取、Metric Catalog 与 DataContract

- [ ] `CHK-4140` 实现 LiveSession ingest API，按 `(source_system, source_event_id)` 幂等并保存 ingest batch/checksum。
- [ ] `CHK-4141` 支持 upsert/delete、退款、撤销、tombstone、event/processing time 和 source timezone。
- [ ] `CHK-4142` 首批标准指标覆盖曝光、停留/留存、互动、点击、加购、成交、退款和内容质量。
- [ ] `CHK-4143` 每个 MetricDefinitionRevision 固定 owner、含义、grain、单位/币种、分子/分母、时间、维度、去重、窗口和质量 SLO。
- [ ] `CHK-4144` 每个来源 DataContract 固定 schema、主键、更新/删除、event time、迟到、枚举、敏感级别、量级和 owner。
- [ ] `CHK-4145` 建立原始、标准、汇总、指标封存点，任何 AttributionRun 指明读取的 snapshot。
- [ ] `CHK-4146` 实现完整性、唯一性、引用、时效、分布和 source-to-standard 对账；坏批次隔离而非补零。
- [ ] `CHK-4147` 实现指标目录和 DataContract 治理 UI，支持 revision、diff、owner、质量和 consumer 影响。

### 4.4 描述性与关联性归因

- [ ] `CHK-4160` 实现 AttributionRun 创建、校验、计算、review、publish、insufficient_data 和 failed 状态。
- [ ] `CHK-4161` 固定 input snapshots、release、exposure revision、metric revision、窗口、基线、规则、对齐、方法和代码版本。
- [ ] `CHK-4162` 产出 session/span/ProgramSegment/Shot/ScriptBlock/template module/asset/product/CTA 粒度结果。
- [ ] `CHK-4163` 保存 numerator、denominator、sample、sessions、statistic/effect、interval、quality、assumptions、limits 和 lineage。
- [ ] `CHK-4164` 时间窗和切换前后分析仅标记 descriptive/associational，UI 禁用因果措辞。
- [ ] `CHK-4165` 指标、曝光、对齐、异常、样本和方法门禁通过后才可 published。
- [ ] `CHK-4166` 迟到、backfill、删除、退款和人工校正创建新 result revision 并 supersede，不覆盖旧结果。
- [ ] `CHK-4167` 提供归因列表、比较、下钻和证据资格 UI，展示样本、区间、方法、质量和封存点。

### 4.5 Phase 4 退出门禁

- [ ] `CHK-4290` 一场 LiveSession 绑定精确 release，并通过人工/录屏辅助建立实际 ContentExposureEvent。
- [ ] `CHK-4291` 一个 published 结果可下钻到曝光、指标桶、内容区间、素材/模板、封存点、方法和代码。
- [ ] `CHK-4292` 重复事件不重复计数；退款、删除、迟到和 backfill 产生可解释修订。
- [ ] `CHK-4293` 样本/对齐不足返回 insufficient_data；相关性在 API 和 UI 均不显示为因果。
- [ ] `CHK-4294` 历史无曝光数据只保留计划/推断来源并降低证据等级，不伪造 served log。
- [ ] `CHK-4295` Phase 4 产品、工程、数据、安全和运营验收签字并归档验收包。

---

## Phase 5. 自动采集、可信估计与效果驱动再生产

> 目标：实现自动数据回流、点时正确的决策日志、合格关联/准实验和受控再生产。
> 依赖：Phase 4 全部退出门禁完成。

### 5.1 自动来源适配与数据质量

- [ ] `CHK-5101` 为抖音互动、麦兔/JD 运营指标及后续来源实现版本化 adapter 和 DataContract。
- [ ] `CHK-5102` 明确每个来源使用官方 API、授权导出或受控浏览器采集；记录凭据、条款、限流、成本和退出方案。
- [ ] `CHK-5103` 实现 watermark、allowed lateness、退款/撤销窗口、backfill 和 snapshot sealing。
- [ ] `CHK-5104` 实现 exposure reconciliation，比较计划、delivery readback、served log 和录屏识别。
- [ ] `CHK-5105` 实现 DataQualityIncident、隔离、owner、修复、重放和 source-to-standard 对账。
- [ ] `CHK-5106` 建立 ingest freshness、complete、duplicate、schema drift、distribution drift 和 reconciliation SLO/告警。
- [ ] `CHK-5107` 验证数据积压 backpressure：已接收批次不丢失，分析/归因延迟在 UI 可见。

### 5.2 FeatureSnapshot、DecisionLog 与点时正确性

- [ ] `CHK-5120` 在选材、模板、策略和生成建议时写不可变 DecisionLog。
- [ ] `CHK-5121` 保存候选全集/排除摘要、FeatureSnapshot、策略/模型、权重、探索、propensity 和最终选择。
- [ ] `CHK-5122` 后续关联实际 release 和 exposure，不以计划选择冒充曝光。
- [ ] `CHK-5123` 实现 feature 定义版本、event-time 截止和 point-in-time join。
- [ ] `CHK-5124` 构建离线 replay，证明任何训练/评估样本不能读取决策后的未来数据。
- [ ] `CHK-5125` 对缺 propensity 或缺实际 exposure 的历史数据强制 descriptive-only。
- [ ] `CHK-5126` 评估在线 Feature Store 门槛；未满足时使用关系库 snapshot，满足时通过 ADR 且不改变领域契约。

### 5.3 EffectEligibilityPolicy 与效果对象

- [ ] `CHK-5140` 实现 PerformanceProfile、AssociationalEstimate、CausalEstimate 的查询、revision 和状态机。
- [ ] `CHK-5141` 每个对象固定实体 revision、上下文、metric、窗口、snapshot、tier、方法、interval、drift 和来源结果。
- [ ] `CHK-5142` 实现 EffectEligibilityPolicy：指标一致、曝光完整、时间对齐、最小场次/样本、稳定性、新鲜度和异常。
- [ ] `CHK-5143` 分用途设置证据门槛：展示 descriptive、人工建议 associational、自动排序 approved associational、策略流量变化 quasi/randomized。
- [ ] `CHK-5144` 实现 candidate/eligible/approved/rejected/review_required/revoked/superseded 和过期。
- [ ] `CHK-5145` 实现 estimate approve/revoke API，批准不能修改 evidence tier，撤销传播到依赖 DecisionLog/推荐。
- [ ] `CHK-5146` 建立 `/learning/effects` UI，按对象、上下文、样本、区间、漂移和决策使用记录展示。

### 5.4 准实验与敏感性分析

- [ ] `CHK-5160` 为准实验预先记录处理机制、目标估计量、纳入规则、窗口、协变量、识别假设和分析计划。
- [ ] `CHK-5161` 实现可比性诊断、平行趋势或方法对应假设、负向对照和敏感性分析。
- [ ] `CHK-5162` 保存方法/代码、数据封存、排除漏斗、区间和限制；方法不充分时降级为 associational。
- [ ] `CHK-5163` 评审通过的准实验才能生成 CausalEstimate，未通过不能使用因果表述或策略扩量。
- [ ] `CHK-5164` 对退款延迟、跨会话干扰、策略污染和选择偏差执行模拟测试。

### 5.5 受控排序与再生产

- [ ] `CHK-5180` 推荐器始终先执行权利、事实、白名单和硬约束过滤，再使用软效果权重。
- [ ] `CHK-5181` 只读取当前 LearningPolicy 允许、approved、未过期且上下文匹配的估计。
- [ ] `CHK-5182` 保留新对象和不确定策略的预算化探索，探索不得绕过安全或硬约束。
- [ ] `CHK-5183` 实现 point-in-time 离线回放、覆盖率、校准、分群稳定性和策略评估。
- [ ] `CHK-5184` 监控 selection/position/survivorship bias、自我强化、流量漂移和训练/服务偏差。
- [ ] `CHK-5185` 再生产创建新的 ContentProjectRevision 或 ProductionVariantRevision，固定来源 estimate、DecisionLog、旧 revision 和变更假设。
- [ ] `CHK-5186` 新方案有独立 release/exposure/evaluation，不修改全局约束、不自动发布模板、不自动开播。
- [ ] `CHK-5187` 指标变化、权利撤销、数据删除、漂移或在线失效使结果 review_required/revoked 并重新评估依赖决策。
- [ ] `CHK-5188` 建立效果驱动再生产 UI，展示保留/替换、预期影响、证据门槛、探索配额和回滚。

### 5.6 Phase 5 退出门禁

- [ ] `CHK-5290` 自动来源可在 watermark/backfill/退款/删除下稳定生成版本化标准数据和质量证据。
- [ ] `CHK-5291` 任一自动排序决策可还原候选、排除、点时特征、propensity、release、exposure 和结果。
- [ ] `CHK-5292` 离线 replay 证明无未来泄漏；缺 propensity/exposure 的数据不能进入因果训练。
- [ ] `CHK-5293` 只有满足 LearningPolicy 的批准结果影响软排序，硬约束和事实永远优先。
- [ ] `CHK-5294` 准实验结果具备假设、诊断和敏感性证据；不足结果正确降级。
- [ ] `CHK-5295` 漂移、撤销、数据删除和 kill switch 能停止依赖策略并触发影响任务。
- [ ] `CHK-5296` Phase 5 产品、工程、数据、安全和运营验收签字并归档验收包。

---

## Phase 6. 随机实验、RoleStrategy 与 RoleEvaluation

> 目标：用可信随机实验评价编剧、导演、影像、剪辑、场控和数据角色策略，并受控调整流量。
> 依赖：Phase 5 全部退出门禁完成。

### 6.1 Role 与 RoleStrategyRevision

- [ ] `CHK-6101` 定义 Role 的职责、输入/输出 schema、允许工具、权限、质量门禁和 owner。
- [ ] `CHK-6102` 实现 RoleStrategyRevision 的 prompt/规则/模型/代码、参数、成本、支持范围、状态和 fingerprint。
- [ ] `CHK-6103` 每个生产 artifact 强制记录实际 role/strategy revision；人工编辑使用 human_override 和操作者。
- [ ] `CHK-6104` 实现显式默认、人工指定或 ExperimentAssignment 三种选择路径并写 DecisionLog。
- [ ] `CHK-6105` 策略版本变化不改写旧 artifact、release 或评价。

### 6.2 Experiment 设计、分配与曝光对账

- [ ] `CHK-6120` 实现 Experiment/Revision 的 draft/design_review/ready/running/stopped/analyzing/concluded/rejected/aborted。
- [ ] `CHK-6121` 预注册 hypothesis、randomization unit、treatment/control、primary/guardrail metric、MDE、power/sample、window、exposure、exclusion、namespace、stopping 和 analysis。
- [ ] `CHK-6122` 在曝光前用稳定哈希或实验服务产生 Assignment，保存 propensity 和 assignment revision。
- [ ] `CHK-6123` 实际 treatment 以 ContentExposureEvent 为准，分配与曝光不一致进入质量门禁。
- [ ] `CHK-6124` 实现并发实验 namespace、互斥/正交规则、跨设备/跨会话污染和 interference 记录。
- [ ] `CHK-6125` 实现 A/A、SRM、guardrail、重复度量、退款延迟、多重比较和完整排除漏斗。
- [ ] `CHK-6126` 紧急护栏停止为 aborted 并保留原因，不包装为成功；禁止事后改主指标或样本范围。
- [ ] `CHK-6127` 建立 `/learning/experiments` 设计、审批、分配、对账、监控和分析 UI。

### 6.3 RoleEvaluation 与分阶段上线

- [ ] `CHK-6140` RoleEvaluation 绑定明确策略 exposure、metric revision、effect estimate 和 evidence tier。
- [ ] `CHK-6141` 分别报告质量、时延、成本、安全和业务效果，不把总 GMV 全归单一角色。
- [ ] `CHK-6142` 只有预注册随机实验或批准准实验可以自动扩大/收缩流量；描述结果只提出假设。
- [ ] `CHK-6143` 新策略按 offline -> shadow -> 人工建议 -> 小流量 -> 扩量 -> 常态推进。
- [ ] `CHK-6144` 每一级定义护栏、成本/探索预算、最小证据、回滚条件和 kill switch。
- [ ] `CHK-6145` 监控策略漂移、自我强化和多目标权衡，失败自动降级到最后批准 revision。

### 6.4 Phase 6 退出门禁

- [ ] `CHK-6290` 至少完成一次预注册实验的策略分配 -> 实际曝光 -> 因果估计 -> 受控策略变更。
- [ ] `CHK-6291` A/A、SRM、护栏、assignment/exposure mismatch 和紧急停止反例全部通过。
- [ ] `CHK-6292` 实验可从 revision、Assignment、DecisionLog、Exposure、Metric、Analysis 完整重放。
- [ ] `CHK-6293` RoleEvaluation 不产生错误的单角色总 GMV 归因，流量调整满足证据门槛。
- [ ] `CHK-6294` Phase 6 产品、工程、数据、安全和运营验收签字并归档验收包。

---

## Phase 7. 知识图谱、排播授权、治理与生产化运行

> 目标：补齐可重建知识/关系投影、排播模型、高风险能力评审及完整生产治理。
> 依赖：Phase 6 全部退出门禁完成。

### 7.1 知识库与关系事实源

- [ ] `CHK-7101` 建立 `/knowledge/facts`，管理 FactCard/FactClaim、字段来源、有效期、批准、冲突和使用记录。
- [ ] `CHK-7102` 建立内容知识、合规规则、术语、表达禁区和审核模块，不与目标商品事实混用。
- [ ] `CHK-7103` 建立 SourceEvidence 文档/网页/人工来源、抽取运行、checksum、引用区间和访问控制。
- [ ] `CHK-7104` 搜索命中必须回事实源校验批准、有效期、权利和适用范围，命中本身不是授权。
- [ ] `CHK-7105` 实现 knowledge search 和 fact lineage API，关系浏览可下钻剧本、Shot、载体、release、session 和 effect。

### 7.2 图谱与混合检索投影

- [ ] `CHK-7120` 先记录至少三个稳定业务查询、关系库基线耗时、本体稳定性、容量和完整重建结果。
- [ ] `CHK-7121` 未达到采用门槛时继续使用关系表/outbox；达到后通过 ADR 决定 Neo4j 是否成为正式投影依赖。
- [ ] `CHK-7122` 实现规划本体节点：素材/权利、事实/证据、模板、内容链、分支、载体、计划、release/exposure、metric/effect、strategy/experiment。
- [ ] `CHK-7123` 实现 DERIVED_FROM、CITES、USES_ASSET、PROJECTED_AS、EXECUTED_AS、RELEASED_AS、EXPOSED_DURING、MEASURED_BY、ESTIMATED_EFFECT_ON 等关系。
- [ ] `CHK-7124` 每条边保存来源主键/revision、有效时间、assertion kind、confidence 和 evidence；推断/相关性不显示为事实。
- [ ] `CHK-7125` 实现 GraphProjectionVersion、ontology/embedding version、watermark、full rebuild、增量对账和陈旧状态。
- [ ] `CHK-7126` 实现 lexical + vector + graph rerank，分开显示事实、历史结果、建议和推断。
- [ ] `CHK-7127` 召回后回事实源重验授权、状态、权利和硬约束；派生层禁止反写事实。
- [ ] `CHK-7128` 实现 graph entity API、projection rebuild API 和治理任务。
- [ ] `CHK-7129` 删除 Neo4j/Milvus 后从封存点重建相同版本，checksum、节点/边数和业务查询对账通过。

### 7.3 BroadcastSchedule

- [ ] `CHK-7140` 实现 BroadcastSchedule/Revision 和 draft/validated/approval_required/approved/active/rejected/canceled/completed。
- [ ] `CHK-7141` 固定目标账号/房间、ReleaseManifest、平台、时区、窗口、促销/库存依赖、冲突策略、owner 和停止条件。
- [ ] `CHK-7142` 验证 release 未撤销、权利渠道/地域/期限、目标空闲、凭据能力、库存/优惠、平台规则和并发冲突。
- [ ] `CHK-7143` 建立 `/operations/schedules` 列表、日历/时间窗、冲突、审批和授权状态 UI。
- [ ] `CHK-7144` capability 关闭时允许设计/验证计划，但不显示可误触开播命令，授权请求稳定返回策略拒绝。

### 7.4 GoLiveAuthorization 与高风险能力评审

- [ ] `CHK-7160` 实现 GoLiveAuthorization requested/approved/consumed/rejected/expired/revoked 的独立 capability。
- [ ] `CHK-7161` 授权绑定主体、目标、release hash、现场 fingerprint、时间窗、策略版本、nonce 和 single use。
- [ ] `CHK-7162` 强制生产者/批准者分离，高风险账号启用双人复核和最小权限凭据。
- [ ] `CHK-7163` 实现提交前 release/权利/库存/优惠/现场/时间窗/授权/保护注册表的重新检查。
- [ ] `CHK-7164` 实现紧急停止、授权撤销、账号隔离、现场 readback、未知结果 reconcile 和审计通知。
- [ ] `CHK-7165` 在隔离账号/房间完成错误 release、错时间窗、现场变化、授权服务中断、网络超时和紧急停止演练。
- [ ] `CHK-7166` 完成独立安全评审、平台条款审阅、运营 runbook 和应急值守批准。
- [ ] `CHK-7167` 只有 `CHK-7160` 至 `CHK-7166` 全部完成后，才允许通过受审 ADR 打开生产 go_live feature flag。
- [ ] `CHK-7168` 若业务决定继续关闭生产开播，记录 capability 关闭决策；契约、sandbox、拒绝路径和演练仍必须完成，不能删项。

### 7.5 治理与任务中心

- [ ] `CHK-7180` 建立 `/governance/runs`，按 run/step/task/owner/status/cost/queue 查看和深链。
- [ ] `CHK-7181` 建立发布审批、策略/实验、指标目录、数据质量、权利到期、保留删除和 projection rebuild 待办。
- [ ] `CHK-7182` 提供长任务排队原因、当前 step、重试、timeout、等待人工、取消和 reconcile 结果。
- [ ] `CHK-7183` 高风险批量操作显示影响数量、阻断项和 sample diff，并进入 WorkflowRun。
- [ ] `CHK-7184` 所有 approve/reject/waive 显示固定 revision 和影响，要求结构化理由且防重复副作用。
- [ ] `CHK-7185` 通知深链到实体 revision/证据；warning、stale、insufficient_data、reconcile_required 保持独立视觉语义。

### 7.6 隐私、删除、权利与供应链治理

- [ ] `CHK-7200` 实现 DeletionRun requested/validating/legal_hold/approved/executing/verifying/completed/partial_failed。
- [ ] `CHK-7201` 删除传播到对象存储、事件层、特征、向量、图谱和可删除派生；每个处理器回写结果或保留依据。
- [ ] `CHK-7202` legal hold 具备 owner、范围、证据、到期和解除，不作为永久保留默认值。
- [ ] `CHK-7203` 权利到期/撤销传播到 Rendition、模板、release、schedule 和后续决策，阻止新交付。
- [ ] `CHK-7204` 建立访问、导出、解密、批量查询、模型发送和删除审计报表。
- [ ] `CHK-7205` 生成 SLSA 风格构建 provenance，固定镜像/依赖/工具来源和构建环境。
- [ ] `CHK-7206` 为证据类定义 owner、访问、保留、legal hold 和销毁证明；UI 分开显示完整性验证和业务批准。

### 7.7 容量、SLO、成本与灾备

- [ ] `CHK-7220` 更新当前值、12 个月预测、峰值系数和单元成本容量模型。
- [ ] `CHK-7221` 在目标峰值 2 倍压测 API、队列、数据库、对象存储、采集、渲染和外部 adapter。
- [ ] `CHK-7222` 验证录屏、事件、Browser-use 和渲染 backpressure 及账号/房间串行限制。
- [ ] `CHK-7223` 落实元数据 API 99.9% 月可用性和 p95 500ms 初始目标；搜索/重查询单列 SLO。
- [ ] `CHK-7224` 落实任务不丢、排队原因、release/授权证据完整率 100%、摄取新鲜度、归因时延和 projection lag SLI。
- [ ] `CHK-7225` 为删除、权利撤销和紧急停用设完成时限、升级和 error budget。
- [ ] `CHK-7226` 每个 WorkflowRun 记录模型、ASR/OCR、渲染、存储/出口、图和 Browser-use 成本。
- [ ] `CHK-7227` 实现项目/团队/adapter 预算、并发配额、cost approval 和 kill switch。
- [ ] `CHK-7228` 实现只降级非必要分析、预览、图谱和离线学习，永不跳过事实/权利/授权/质量/release 门禁。
- [ ] `CHK-7229` 分离关键生产和派生分析队列，避免批处理挤占草稿写入、撤销和删除。
- [ ] `CHK-7230` 落实 PostgreSQL PITR，验证 RPO <= 5 分钟、RTO <= 4 小时或由 owner 批准的新目标。
- [ ] `CHK-7231` 对象存储版本/复制、凭据/签名密钥备份轮换、投影重建和关键 release 导出均通过演练。
- [ ] `CHK-7232` 每季度恢复演练、每半年第三方故障/错误 release/大规模删除演练进入治理日历。

### 7.8 路由迁移与完整产品体验

- [ ] `CHK-7240` 完成 `/assets/library`、分组、素材包、同步、效果关系和知识跳转。
- [ ] `CHK-7241` 完成 `/research/live-sources` 的来源、场次、清洗、草稿和已发布模板。
- [ ] `CHK-7242` 完成 `/content/projects` 的目标事实、模板、StoryBrief/剧本、节目/Shot、分支和 lineage。
- [ ] `CHK-7243` 完成 `/production/live-rooms`、`/production/videos` 和 `/production/releases`。
- [ ] `CHK-7244` 完成 `/operations/live-sessions`、attribution、schedules 和 `/learning/effects/experiments`。
- [ ] `CHK-7245` 完成 governance runs/metrics/tasks 和知识库一级工作区。
- [ ] `CHK-7246` 首页进入“我的任务/异常”；全局搜索、保存筛选、批量选择、通知和实体深链可跨域工作。
- [ ] `CHK-7247` 完成桌面与移动视口的文字溢出、遮挡、键盘、焦点、对比度和无障碍检查。
- [ ] `CHK-7248` 新路由功能等价和深链审计通过后重定向旧入口；旧运行保留只读，未达条件的兼容入口不删除。

### 7.9 Phase 7 退出门禁

- [ ] `CHK-7290` 知识搜索、lineage、影响分析和缺口候选查询均回事实源校验，推断不冒充事实。
- [ ] `CHK-7291` 图/向量投影可从封存点完整重建；投影落后不覆盖事实源且 UI 显示水位。
- [ ] `CHK-7292` 排播验证覆盖 release、权利、资源、库存/优惠、平台规则和冲突。
- [ ] `CHK-7293` go_live 在完成独立安全评审前全路径失败关闭；启用后也只能消费短期目标绑定授权。
- [ ] `CHK-7294` 原始个人数据删除、legal hold、权利撤销和 release revoke 的跨存储传播均有完整证据。
- [ ] `CHK-7295` 2 倍峰值压测、SLO、cost quota、kill switch、PITR、对象恢复和投影重建全部达到批准目标。
- [ ] `CHK-7296` 全部稳定路由与跨工作区旅程通过运营验收，旧入口迁移无断链。
- [ ] `CHK-7297` Phase 7 产品、工程、数据、安全、设计和运营验收签字并归档验收包。

---

## Phase 8. 全系统测试、反思与最终验收

> 目标：在逐 Phase 测试之外，额外从完整业务闭环、反例、故障、可复现性和设计假设角度验证系统，并把发现反馈到代码和规划。
> 依赖：Phase 7 全部退出门禁完成；本 Phase 不是补写测试的替代品。

### 8.1 需求追踪与测试资产审计

- [ ] `CHK-8101` 建立“设计章节 -> checklist ID -> 代码模块 -> migration -> API -> UI -> 测试 -> 证据”双向追踪矩阵。
- [ ] `CHK-8102` 逐条映射权威设计第 19 节全部验收场景，不允许用一个 happy-path E2E 代替多个不变量。
- [ ] `CHK-8103` 逐条映射最终目标五个核心子系统、六个角色和短/中/长期成功标准。
- [ ] `CHK-8104` 扫描未被测试覆盖的路由、状态转移、规则码、worker operation、queue handler 和 migration。
- [ ] `CHK-8105` 删除或说明跳过、xfail、flaky quarantine 和无断言测试；每个保留项有 owner、期限和风险。
- [ ] `CHK-8106` 固定测试数据工厂、时钟、随机 seed、provider fake、媒体 fixture 和敏感数据禁入规则。

### 8.2 静态、单元、属性与契约测试

- [ ] `CHK-8120` 后端、worker 完成格式、lint、类型、依赖和 secret scan；前端完成 typecheck、lint 和 build。
- [ ] `CHK-8121` 所有领域状态机完成合法/非法转移单元和属性测试。
- [ ] `CHK-8122` revision 不可变、fingerprint、stale 传播、idempotency 和 expected revision 完成并发属性测试。
- [ ] `CHK-8123` 约束合并、pack 解析、排他、solver status 和选材优先级完成组合/属性测试。
- [ ] `CHK-8124` 事实引用、模板去事实、权利范围、EffectEligibility 和证据等级完成反例测试。
- [ ] `CHK-8125` 时间线 RationalTime、TimeMapping、event-time/window/refund/backfill 完成边界和时区属性测试。
- [ ] `CHK-8126` OpenAPI、队列消息、artifact schema、provider adapter、DataContract 和 OpenLineage consumer contract 全绿。
- [ ] `CHK-8127` 对未知必需字段、旧可选字段、schema 升降级和弃用窗口执行兼容测试。

### 8.3 数据库迁移与数据质量测试

- [ ] `CHK-8140` 从空库顺序应用全部 migrations，schema 与测试期望一致。
- [ ] `CHK-8141` 从生产数据脱敏副本升级，验证耗时、锁、索引、磁盘、失败恢复和前向修复。
- [ ] `CHK-8142` 验证 legacy 投影不伪造 StoryBrief、Shot、ReleaseManifest 或 Exposure。
- [ ] `CHK-8143` 验证 outbox 事务原子性、重复消费、乱序、死信、重放和 full rebuild。
- [ ] `CHK-8144` 验证删除 tombstone、权利撤销、release revoke、stale 和 supersede 不留下可误用孤儿引用。
- [ ] `CHK-8145` 执行主外键、唯一性、revision 连续性、checksum、时间区间和状态不变量巡检。

### 8.4 服务与 Worker 集成测试

- [ ] `CHK-8160` 用真实 PostgreSQL/MinIO 和本地 provider fake 运行 API -> queue -> worker -> artifact -> readback 集成测试。
- [ ] `CHK-8161` Browser-use 在隔离麦兔草稿完成 preflight、write、checkpoint、readback、reconcile、finalize。
- [ ] `CHK-8162` 注入登录失效、错房间、非空、已开播、现场变化、素材失效、网络超时和 authoritative API 不可用。
- [ ] `CHK-8163` 直播研究测试掉帧、断流、时钟漂移、sidecar 崩溃、重复事件、retention 和 resume。
- [ ] `CHK-8164` 视频 worker 测试 TTS、字幕、FFmpeg、QC、阶段重试、坏媒体、无声、黑帧和 checksum mismatch。
- [ ] `CHK-8165` 数据链测试 ingest、watermark、late/backfill、退款、删除、封存、attribution 和 estimate revoke。
- [ ] `CHK-8166` 外部 provider 测试超时、429、5xx、坏 schema、部分响应、数据泄漏防护、成本限制和 fallback。
- [ ] `CHK-8167` 验证所有未知外部副作用先 reconcile，任何 retry 都不会复制现场对象或交付。

### 8.5 前端与完整业务旅程 E2E

- [ ] `CHK-8180` E2E 覆盖素材同步 -> 权利/约束 -> 分组/素材包 -> resolve -> gap。
- [ ] `CHK-8181` E2E 覆盖抖音来源 -> CaptureSession -> 多模态时间线 -> 清洗 -> 模板审核发布。
- [ ] `CHK-8182` E2E 覆盖目标/事实/模板 -> StoryBrief -> Script -> ProgramSegment -> ShotList。
- [ ] `CHK-8183` E2E 覆盖 live_room variant -> snapshot -> Blueprint -> BuildPlan -> authorization -> draft -> release/readback。
- [ ] `CHK-8184` E2E 覆盖 rendered_video variant -> timeline -> voice/subtitle -> render/QC -> release/delivery。
- [ ] `CHK-8185` E2E 覆盖 LiveSession -> Exposure -> ingest -> attribution -> effect approval -> re-production。
- [ ] `CHK-8186` E2E 覆盖 Experiment Assignment -> Exposure reconciliation -> Analysis -> RoleEvaluation -> rollout/rollback。
- [ ] `CHK-8187` E2E 覆盖 rights revoke、release revoke、DeletionRun、projection rebuild、schedule conflict 和授权撤销。
- [ ] `CHK-8188` 验证刷新、重复点击、多标签、返回/前进、深链、并发编辑、离线恢复不会重复副作用或丢 revision。
- [ ] `CHK-8189` 在桌面和移动视口截图审查 loading/empty/error/warning/stale/partial/unauthorized；无文字遮挡、溢出或不可达控件。
- [ ] `CHK-8190` 验证键盘操作、焦点、语义标签、对比度、读屏名称和错误提示达到批准的无障碍基线。

### 8.6 安全、隐私与滥用测试

- [ ] `CHK-8200` 对 generation goal、ASR/OCR、模板例句、网页和 tool result 做提示注入与数据投毒测试。
- [ ] `CHK-8201` 对 IDOR、越权、跨 capability、token 重放、授权换目标/hash、过期和 clock skew 做安全测试。
- [ ] `CHK-8202` 证明模型无平台长期凭据、无任意网络/Browser-use 调用能力、无任意 selector/URL/operation 输出通道。
- [ ] `CHK-8203` 对日志、trace、截图、artifact、向量、图谱、导出和错误响应执行 cookie/token/个人信息泄漏扫描。
- [ ] `CHK-8204` 对素材、肖像、声音、AI 分析、衍生、渠道、地域、期限和撤销执行 rights matrix 测试。
- [ ] `CHK-8205` 对模板连续文本相似、品牌/主播模仿、版权画面和个人敏感信息执行红队测试。
- [ ] `CHK-8206` 对 ProtectedResourceRegistry、只读参考房间、禁写目标和 go_live kill switch 做独立绕过测试。
- [ ] `CHK-8207` 验证 DeletionRun 跨主库、对象存储、事件、feature、Milvus、Neo4j、缓存和备份政策传播。
- [ ] `CHK-8208` 完成第三方渗透/安全评审或内部独立复核，所有高危问题关闭后才继续最终验收。

### 8.7 性能、韧性、灾备与可复现性

- [ ] `CHK-8220` 按目标峰值 2 倍执行 API、数据库、对象存储、queue、采集、渲染、Browser-use 和投影压测。
- [ ] `CHK-8221` 验证 backpressure、priority、quota、budget、kill switch 和关键/派生队列隔离。
- [ ] `CHK-8222` 注入数据库主连接中断、对象存储故障、worker 大量崩溃、provider outage、授权服务故障和图投影损坏。
- [ ] `CHK-8223` 验证 error budget 规则能暂停扩量/高风险自动化，而不跳过安全门禁。
- [ ] `CHK-8224` 在干净机器按 reproducibility 文档启动基础设施、迁移、服务、worker 和前端。
- [ ] `CHK-8225` 用固定 RunManifest 重放内容、BuildPlan、timeline、render、attribution 和 graph projection，解释非字节级差异。
- [ ] `CHK-8226` 执行 PITR、对象版本恢复、outbox replay、projection rebuild、关键 release 导出和签名校验。
- [ ] `CHK-8227` 验证实际 RPO/RTO、恢复后业务不变量、checksum、行数和 release 可交付性，不以“服务启动”作为成功。

### 8.8 运营 UAT 与验收场景复核

- [ ] `CHK-8240` 由非研发运营人员完成素材分组、约束、素材包、选材解释和 gap 闭环。
- [ ] `CHK-8241` 完成多场录屏模板清洗、去事实、发布和主次贡献选择。
- [ ] `CHK-8242` 完成无房间内容确认、live_room 载体确认、草稿写入、clone 新房间和旧证据查看。
- [ ] `CHK-8243` 完成同内容成片、timeline 调整、QC 下钻、release 审批和 delivery readback。
- [ ] `CHK-8244` 完成 exposure 校正、metric/contract 查看、归因下钻、effect 审批和再生产。
- [ ] `CHK-8245` 完成 HumanTask、异常处理、reconcile、权利撤销、删除、projection rebuild 和 schedule 冲突处理。
- [ ] `CHK-8246` 运营人员能准确解释 workflow、release、delivery、exposure、correlation 和 causation 的区别。
- [ ] `CHK-8247` 收集任务完成率、耗时、误操作、求助点和认知错误，关闭阻断性体验问题。

### 8.9 测试反思与设计复盘

- [ ] `CHK-8260` 汇总所有失败用例，区分需求遗漏、设计错误、实现错误、测试错误、环境问题和外部平台变化。
- [ ] `CHK-8261` 复盘哪些 bug 被单元、契约、集成、E2E、UAT 或生产演练发现，找出本应更早发现的缺口。
- [ ] `CHK-8262` 复盘每个 mock/fake 是否忠实模拟外部失败；对过度宽松 mock 补契约或 sandbox 测试。
- [ ] `CHK-8263` 复盘状态机、revision、fingerprint、lineage 和 evidence 是否降低了排障成本；无法回答的问题补可观测性。
- [ ] `CHK-8264` 复盘是否存在“workflow succeeded 当作交付/曝光”“相关性当因果”“检索命中当批准事实”等语义回退。
- [ ] `CHK-8265` 复盘自动化是否出现自动化偏见、反馈回路、自我强化、样本选择偏差或新素材冷启动惩罚。
- [ ] `CHK-8266` 复盘权利、隐私、保留、授权和成本门禁是否存在业务绕过压力，并补技术约束而非只写流程提醒。
- [ ] `CHK-8267` 复盘 SLO 和容量目标是否来自实测；修正虚高、虚低或没有用户价值的指标。
- [ ] `CHK-8268` 对每个设计假设记录 `validated / falsified / still uncertain`、证据、影响和下一步实验。
- [ ] `CHK-8269` 对引入的 OR-Tools、Temporal、Neo4j、Feature Store、C2PA 等组件逐项复核采用门槛、真实收益、运维成本和退出能力。
- [ ] `CHK-8270` 召开跨 product/engineering/data/security/design/operations 复盘，形成 owner 和期限明确的改进项。
- [ ] `CHK-8271` 将证实的设计偏差回写权威设计、ADR、API schema、runbook 和本 checklist，保持文档与实现一致。
- [ ] `CHK-8272` 所有 P0/P1 缺陷关闭；接受的其他缺陷有风险、owner、到期和不影响最终定义的批准依据。

### 8.10 最终完成审计

- [ ] `CHK-8290` 权威设计第 29 节 11 个最终目标领域逐项有代码、UI/API、测试和验收证据。
- [ ] `CHK-8291` 权威设计第 30 节 10 条完成定义逐项演示通过并归档录像/manifest/报告。
- [ ] `CHK-8292` 本清单不存在未勾选项、无证据勾选项、过期 waiver、无人负责风险或无下线条件兼容接口。
- [ ] `CHK-8293` 发布生产 runbook、支持边界、on-call、告警、应急停止、恢复、删除和权利撤销手册。
- [ ] `CHK-8294` 发布最终 schema/API/事件/状态机/领域词汇/数据字典和用户操作文档。
- [ ] `CHK-8295` 执行最终 secret、license、dependency、provenance、数据保留和外部依赖审计。
- [ ] `CHK-8296` Product、Engineering、Data、Security、Design、Operations 最终负责人联合签字。
- [ ] `CHK-8297` 将本文件状态改为“全部完成”，记录最终 release、commit、验收包和签字日期。

---

## 附录 A. 每阶段固定交付物

- [ ] `CHK-A001` 该阶段 ADR 与领域/schema 变更记录。
- [ ] `CHK-A002` 追加 migration、数据回填/兼容报告和前向修复方案。
- [ ] `CHK-A003` OpenAPI、事件/队列 schema、错误码和 consumer contract。
- [ ] `CHK-A004` UI 深链、状态、权限、审计和可访问性验收。
- [ ] `CHK-A005` RunManifest、ArtifactRef、lineage、trace、指标、日志和告警。
- [ ] `CHK-A006` 单元、属性、契约、集成、E2E、性能和安全测试报告。
- [ ] `CHK-A007` 外部依赖、容量、SLO、成本、配额、降级和 kill switch 记录。
- [ ] `CHK-A008` 备份/恢复、回滚/reconcile、删除和权利撤销演练证据。
- [ ] `CHK-A009` 产品、数据、安全、设计和运营验收记录。
- [ ] `CHK-A010` 已知限制、未决问题、风险、owner、期限和下一阶段依赖。

## 附录 B. 执行日志

> 实施时按 `check_id` 追加记录，或填写指向项目管理系统中不可变记录的链接。不得只改 checkbox 而不保留证据。

| check_id | owner | evidence | approved_by | completed_at |
| --- | --- | --- | --- | --- |
| `CHK-0001` | coderdailyone | `docs/architecture/phase-0-baseline.md` | N/A: process ownership | 2026-07-23 |
| `CHK-0002` preparation (item remains open) | coderdailyone | reject-by-default `docs/operations/phase-owner-register.v1.example.json`; `scripts/assemble_phase0_acceptance.py` validates exact Phase 0-8 coverage and required product/engineering/data/security/design owner plus appointment reference for every Phase | N/A: no actual owner identities or appointment decisions are available; example nulls are tested non-approvable and checkbox remains open | 2026-07-23 |
| `CHK-0003`-`CHK-0010` | coderdailyone | 本文件 0.1 规则及本日志；未跨越 Phase 0 门禁 | N/A: process controls | 2026-07-23 |
| `CHK-0011` | coderdailyone | `docs/operations/phase-delivery-readiness-program.md`; Phase 0-8 dependency graph; 2-6 week acceptance slices; per-phase risks and capacity/cost kickoff schema; unknown future budgets fixed as blocking `blocked_unbudgeted`, with no fabricated dates, staffing, traffic or cost figures | N/A: planning control established; each phase's numeric budget and owners still require kickoff approval | 2026-07-23 |
| `CHK-0012`,`CHK-0296` preparation (items remain open) | coderdailyone | `scripts/assemble_phase0_acceptance.py`; `docs/operations/phase-0-acceptance-package.md`; reject-by-default five-party signoff example; fingerprinted full regression and preparation package; package binds checklist, owners, qualified capacity/production-copy migration/compatibility/DR reports and evidence file hashes before product/engineering/data/security/operations decisions; regression 643 backend + 385 browser + 30 research + 32 frontend tests, TypeScript, three builds, four Ruff checks, new-file format check, reproducibility and diff checks passed; package correctly reports `qualifies_for_chk_0296=false` | N/A: owner register, `CHK-0110`, `CHK-0260` and real five-party decisions are absent; examples cannot sign and both checkboxes remain open | 2026-07-23 |
| `CHK-0101`-`CHK-0108` | coderdailyone | `docs/architecture/phase-0-baseline.md`; `docs/adr/0001-closed-loop-domain-invariants.md` | N/A: architecture baseline | 2026-07-23 |
| `CHK-0110` preparation (item remains open) | coderdailyone | read-only `scripts/measure_capacity_baseline.py`; versioned reject-by-default `docs/operations/capacity-baseline-input.v1.example.json`; exact UTC/half-open queries for assets, daily recording hours, event/processing peaks, live/render concurrency, artifact growth, workflows and Browser-use leases; optional full ArtifactRef/object-store reconciliation; fingerprinted `phase-0-capacity-baseline-integration-2026-07-23.json` and four regression tests passed; integration classification is hard-coded `qualifies_for_chk_0110=false` | N/A: operational read-only snapshot/source reference, object-store reconciliation, approved concurrency/forecast/cost/threshold input and engineering/data/operations decisions are unavailable; checkbox remains open | 2026-07-23 |
| `CHK-0111`-`CHK-0112` | coderdailyone | `docs/operations/external-platform-register.md`; 未决区域/费用明确标记 pending | N/A: register only; production approval remains open | 2026-07-23 |
| `CHK-0113` | coderdailyone | migrations `043`-`044`; `docs/architecture/provider-adapter-boundary.md`; `docs/evidence/phase-0-provider-neutral-producers-validation-2026-07-23.md`; provider-neutral domain/OpenAPI contracts, governed processor authorization, pre-serialization redaction, confidential content-addressed invocation evidence, legacy-only supplier columns, and evidence-before-success worker protocol; clean `001`-`044` database migration; backend 625 passed, live-research worker 30 passed, frontend 32 passed, Ruff/typecheck and three Vite builds; runtime health/Console 200 and producer OpenAPI supplier-field scan empty; known limits: real processor regions, terms, credentials, evidence storage and production data/security approval remain deployment gates | N/A: automated technical evidence; production processor approvals explicitly remain open | 2026-07-23 |
| `CHK-0114` | coderdailyone | `app/services/maitu_authority.py`; strict MockTransport failure contract; 42 tests passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0120`,`CHK-0127`-`CHK-0129` | coderdailyone | migrations `027`; `app/repositories/content_core.py`; clean-DB tests 34 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0121`-`CHK-0126` | coderdailyone | migrations `027`,`034`; `app/repositories/content_production.py`; explicit block/shot sources, branch isolation and legacy Workbench/Video mappings; clean `001`-`034` batch 17 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0109`,`CHK-0130`,`CHK-0202`,`CHK-0290` | coderdailyone | audited transition framework; Hypothesis state/revision properties; transaction tests; 8 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0140`-`CHK-0146` | coderdailyone | migrations `028`; artifact/manifest/outbox/lineage services; clean-DB tests 34 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0147`,`CHK-0293` | coderdailyone | OpenTelemetry FastAPI/HTTPX; Workflow trace claim; provider/authority propagation; trace batch 46 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0148`-`CHK-0149` | coderdailyone | migration `035`; signed Run/Release manifests; authorization/effect hash chains; integrity alert/check/consumption ledger; regression 19 passed and migration-DB 7 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0160`-`CHK-0163`,`CHK-0165`,`CHK-0168` | coderdailyone | migration `028`; control-plane API/repository; route and PostgreSQL tests | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0164`,`CHK-0166`,`CHK-0169`,`CHK-0292` | coderdailyone | migration `033`; external Effect protocol; HumanTask SLA/timeout/cancel races; 12 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0167` | coderdailyone | migration `036`; read-only run/step views, compatibility repository/API; Workbench/Video/Capture/Analysis/Retry mapping tests; no fabricated WorkflowRun rows | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0170` | coderdailyone | `docs/adr/0002-defer-temporal-until-measured-thresholds.md`; measured validation complexity and explicit re-evaluation thresholds | N/A: infrastructure adoption decision; operations Phase exit approval remains open | 2026-07-23 |
| `CHK-0181`-`CHK-0185`,`CHK-0187`-`CHK-0188`,`CHK-0291` | coderdailyone | migrations `029`-`030`; policy/authorization services; attack-path tests | N/A: automated technical evidence; security sign-off remains open | 2026-07-23 |
| `CHK-0184` transaction hardening | coderdailyone | commit authorization and external Effect transition share one transaction; conflict rollback/policy-revision/single-use security batch 9 passed | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0180`,`CHK-0186` | coderdailyone | migration `037`; `docs/security/capability-matrix.md`; versioned ProtectedResource API/events; system-locked reference rooms; independent Preflight/PDP/Worker guards; main batch 46 passed and migration-DB batch 13 passed | N/A: automated technical evidence; independent security sign-off remains open | 2026-07-23 |
| `CHK-0200`-`CHK-0201`,`CHK-0203`-`CHK-0206` | coderdailyone | migration `029`; release service/repository; release/delivery/exposure tests | N/A: automated technical evidence | 2026-07-23 |
| `CHK-0220`-`CHK-0225`,`CHK-0240` | coderdailyone | migrations `029`,`031`,`032`; data-governance service; JSON Schema and catalog tests | N/A: automated technical evidence; data sign-off remains open | 2026-07-23 |
| `CHK-0241` | coderdailyone | migration `038`; purpose-bound access policy/service; append-only allow/deny decisions; all six governed actions and anti-role-composition tests; focused batch 6 passed | N/A: automated technical evidence; data/security sign-off remains open | 2026-07-23 |
| `CHK-0242` | coderdailyone | migration `038`; versioned field-redaction policy; recursive log/screenshot/prompt/model-response redactor; ProviderRouter pre-send/pre-return integration and evidence counts; focused batch 8 passed | N/A: automated technical evidence; privacy/security sign-off remains open | 2026-07-23 |
| `CHK-0243` | coderdailyone | migration `038`; six active versioned retention classes; policy-driven fixed/ranged/indefinite resolution and expiry tests; focused governance batch 6 passed | N/A: automated technical evidence; data/privacy sign-off remains open | 2026-07-23 |
| `CHK-0244` | coderdailyone | migrations `038`,`039`; audited DeletionRun state machine; expiring legal hold/release history; requester/approver separation; tombstones; per-target append-only receipt attempts; partial-failure retry/backoff/escalation; validation-DB governance batch 12 passed | N/A: automated technical evidence; privacy/operations sign-off remains open | 2026-07-23 |
| `CHK-0245` | coderdailyone | migration `038`; versioned external-processor terms and append-only call audits; exact minimum-field projection; ProviderRouter mandatory guard; secret-reference-only credential registry with due/rotate/revoke history; main governance batch 15 passed and validation-DB batch 7 passed | N/A: automated technical evidence; production processor approvals remain open | 2026-07-23 |
| `CHK-0261` | coderdailyone | migration `040`; read-only `legacy_asset_observations_v1`; geometry/duplicate semantics flags and mutation rejection; main and independent validation DB compatibility batches each 6 passed | N/A: automated compatibility evidence | 2026-07-23 |
| `CHK-0262` | coderdailyone | migrations `036`,`040`; read-only ContentProject/live-room variant/WorkflowRun/StepRun/delivery-unknown projections and API; explicit missing provenance; zero writes to canonical project/variant/delivery tables; main and validation DB batches each 6 passed | N/A: automated compatibility evidence | 2026-07-23 |
| `CHK-0263` | coderdailyone | migration `040`; `layout-hypothesis.v1`-only read projection fixed to approximate/reference-only/non-convertible; mutation rejection and source contract preservation; main and validation DB batches each 6 passed | N/A: automated compatibility evidence | 2026-07-23 |
| `CHK-0268` | coderdailyone | `docs/architecture/stable-error-contract.md`; global backward-compatible problem envelope; Console parser/presentation; state-semantics tests; clean 40-migration DB backend 598 passed and frontend 22 passed | N/A: automated API/UI contract evidence | 2026-07-23 |
| `CHK-0264` | coderdailyone | `/console/` plus stable SPA routes; memory-only bearer validation; authenticated search/tasks/notifications API; shared navigation and error boundary; backend 603 passed, frontend 27 passed, three Vite builds and desktop/mobile Playwright screenshots | N/A: automated API/UI/accessibility evidence | 2026-07-23 |
| `CHK-0265` | coderdailyone | Working tree (migration N/A: read-only surface); typed `/api/console/entities/{entity_type}/{entity_code}` for asset/content project/template/workflow run/release; canonical query deep links, revision timeline, selected structured diff, verified source/used-by relations; backend 606 passed, frontend 28 passed, three Vite builds; `docs/evidence/screenshots/phase0-console-entity-{desktop,mobile}.png`; known limit: browser requests an absent favicon only | N/A: automated API/UI/accessibility evidence; product approval remains open | 2026-07-23 |
| `CHK-0266` | coderdailyone | migrations `041`-`042`; optimistic shared drafts with append-only events, conflict/rebase protection and sensitive-field rejection; revision-bound idempotent confirm/publish/approve/reject/authorize commands and non-secret receipts; authorization derives scope from one approved HumanTask and exposes its token once; clean `001`-`042` database backend 614 passed, frontend 31 passed, typecheck and three Vite builds; `docs/evidence/screenshots/phase0-console-{draft,command}-{desktop,mobile}.png`; known limits: expired/lost raw authorization tokens require a newly approved task, and the absent favicon still returns 404 | N/A: automated API/UI/security evidence; product/security approval remains open | 2026-07-23 |
| `CHK-0267` | coderdailyone | exact browser-entry `308` redirects from `/maitu[/]` and `/live-research[/]` to stable Console routes; default/resources/Gemini/watch/sessions/drafts/published mappings preserve repeated and immutable-pin deep-link parameters; shared page components prove functional equivalence; production handoff and shared navigation now use stable URLs; `docs/operations/legacy-frontend-retirement-checklist.md`; backend 616 passed, frontend 32 passed, typecheck and three Vite builds; `docs/evidence/screenshots/phase0-console-legacy-redirect-{desktop,mobile}.png`; legacy API prefixes unchanged and static assets retained pending measured zero-use/approval gates | N/A: automated route/UI evidence; product/design/security retirement approval remains open | 2026-07-23 |
| `CHK-0260` preparation (item remains open) | coderdailyone | forward-only migration runner with per-migration receipts, advisory lock, lock/statement timeouts and checksum immutability; `scripts/rehearse_migrations.py`; versioned invariant contract and runbook; lock-timeout/concurrent-runner/retry/checksum tests; `phase-0-migration-rehearsal-synthetic-2026-07-23.json` passed but is hard-coded `qualifies_for_chk_0260=false` | N/A: tooling only; a traceable isolated production data copy, snapshot/sanitization evidence, measured locks/duration/growth and owner acceptance are still required before checkbox update | 2026-07-23 |
| `CHK-0294` | coderdailyone | `scripts/audit_legacy_compatibility.py`; `docs/operations/legacy-compatibility-audit.md`; fingerprinted `docs/evidence/phase-0-legacy-compatibility-audit-2026-07-23.json`; migrations `036`/`040` checksums and 7/7 read-only view guards passed; six legacy workflow source and step cardinalities matched; five domain projection types had representative coverage; eight uncertainty/non-fabrication assertions passed; authenticated paginated API plus all six detail types and stable 404 passed; six canonical fact-table counts unchanged; focused compatibility/audit batch 9 passed and reproducibility validation clean | N/A: representative integration compatibility evidence; explicitly not a production volume/data-quality or `CHK-0260` migration claim | 2026-07-23 |
| `CHK-0295` | coderdailyone | `scripts/rehearse_disaster_recovery.py`; `docs/operations/disaster-recovery-baseline-runbook.md`; `docs/evidence/phase-0-disaster-recovery-validation-2026-07-23.md`; successful fingerprinted report `phase-0-disaster-recovery-baseline-2026-07-23-attempt-4.json`; PostgreSQL 14 checksummed cluster with continuous WAL archive, verified 62,935,669-byte base backup, time-target recovery and promotion; DB RPO 1 ms/RTO 820 ms; versioned MinIO overwrite/delete/original-version restore with 0-byte RPO and 842 ms RTO; composite RTO 842 ms; 44 migration receipts, 12 business invariants, marker exclusion, `pg_verifybackup`, `pg_amcheck`, offline page checksums and ArtifactRef/object checksum all passed; three failed attempts retained and asserted non-qualifying; backend 632 passed and Ruff clean | N/A: automated local recovery baseline; production-scale topology and `CHK-7230` targets explicitly not claimed | 2026-07-23 |
| Phase 0 foundation batch | coderdailyone | `docs/evidence/phase-0-foundation-validation-2026-07-23.md`; migrations `001`-`032` clean apply; 34 passed | N/A: Phase exit approval not claimed | 2026-07-23 |
| `CHK-1101`-`CHK-1105`,`CHK-1141` preparation (items remain open) | coderdailyone | commit `12f2abd`; revision-required content-project PATCH and explicit confirm endpoint; full target document fields; approved FactCard version pinning with confirmation/generation revalidation of approval, validity and platform scope; target-and-facts Console surface; `docs/operations/phase-0-external-input-register.md`; isolated PostgreSQL tests `5 passed`, Ruff, Console TypeScript and build passed | N/A: this is a Phase 1 spike permitted by `CHK-0004`; Phase 0 `CHK-0110`, `CHK-0260`, `CHK-0296` are still open, DesignBrief artifact/provider/audit requirements and full target-and-fact conflict UX remain incomplete, so no Phase 1 checkbox is checked | 2026-07-24 |
| `CHK-1120`-`CHK-1125`,`CHK-1140`,`CHK-1141` preparation (items remain open) | coderdailyone | commits `7be69c8`, `207c4e8`; migration `051`; bounded deterministic DesignBrief parser, raw-input retention, parsed projection, at most three recommended open questions, explicit DesignBrief confirmation and Console view; generation rejects unconfirmed ContentProject or DesignBrief and records the confirmed DesignBrief code/revision/fingerprint in StoryBrief; isolated PostgreSQL tests `7 passed`, Ruff, Console TypeScript and build passed | N/A: local deterministic parsing is deliberately not a provider call and raw response is not persisted as an ArtifactRef; structured editing/diff, external provider authorization and production audit evidence remain incomplete. This remains a Phase 1 spike until Phase 0 exits. | 2026-07-24 |
| `CHK-1142`-`CHK-1145` preparation (items remain open) | coderdailyone | commit `1163ea7`; generation context is serialized as separate system baseline, approved facts, user goal and external-reference sections with a fingerprint; restricted price/promotion/inventory/gift/efficacy markers require a citation to an approved pinned fact; focused content tests `6 passed`, Ruff passed | N/A: the deterministic generator currently emits only constrained baseline copy; per-sentence character spans, provider output artifact and full FactCitation review UI are still absent. No production completion is claimed. | 2026-07-24 |
| `CHK-1142`-`CHK-1145`,`CHK-1160`-`CHK-1166` preparation (items remain open) | coderdailyone | commit `ee32ea0`; ContentProject template selections resolve to immutable published revision references rather than `selected`; ScriptBlock now persists template contributions and FactCitation field/character-span data; citation revision must match an approved pinned FactCard; live-room plans inherit only the ContentProject template set and reject downstream template substitution; content, live-room and rendered-video test fixtures now exercise explicit ContentProject and DesignBrief confirmation; focused PostgreSQL tests `12 passed`, Ruff, Console typecheck and build passed | N/A: external templates are still legacy/reference projections rather than the Phase 2 content-strategy template contract; no provider raw-response ArtifactRef, editable contribution-decision UI, authoritative room preflight, or production execution evidence exists. This is a Phase 1 spike under `CHK-0004`; no Phase 1 checkbox is checked while Phase 0 exit inputs remain open. | 2026-07-24 |
| `CHK-1180`-`CHK-1188` preparation (items remain open) | coderdailyone | commit `d71b50b`; migration `052`; production-only `maitu_scene_blueprints` and `layer_blueprints` separated from observed reference blueprints; live-room plan persists ordered scene/layer projections with geometry, role, selected asset, Shot/ProgramSegment and ScriptBlock provenance; every scene/layer receives an append-only `ShotProjectionLink`; deterministic script-layout BuildPlan is persisted with a real `build_plan_code`, fixed content/variant/configuration/inventory/blueprint/policy fingerprints and an allowlisted no-go-live operation sequence; PostgreSQL focused content/live/video/build-plan suite `16 passed`, Ruff, Console typecheck and build passed | N/A: title rename remains a planned contract gap in the shared script-layout operation schema; this spike has no authoritative room preflight, execution authorization, readback evidence, release candidate or full constraint solver. This is not a Phase 1 completion claim. | 2026-07-24 |
| `CHK-1187`,`CHK-1188`,`CHK-1200`,`CHK-1201` preparation (items remain open) | coderdailyone | commit `caf85bd`; migration `053`; functional live-room plans now persist seven named static gate results with stable rule codes/version/evidence/remediation plus a branch-quality report; input boundary, Shot projection, operation allowlist and no-go-live conditions can block; target-duration deviation over 50% remains warning-only; Console renders gate states and estimated duration; focused PostgreSQL suite `17 passed`, Ruff, Console typecheck and build passed | N/A: static gate evaluation cannot replace authoritative room readback, commit-time authorization, Worker reconciliation or release evidence. The evidence-completeness gate intentionally remains warning until those integrations exist; no Phase 1 checkbox is checked. | 2026-07-24 |
| `CHK-1220`,`CHK-1221` preparation (items remain open) | coderdailyone | migration `054`; ready functional live-room plans can create an idempotent signed `live_room_draft` release candidate. Its local content-addressed PostgreSQL snapshot fixes exact ContentProject/StoryBrief/Script/Program/ShotList/Variant/Configuration revisions, asset/group selection, `MaituSceneBlueprint`/Layer projection, BuildPlan, static gate report and execution state. The manifest references the snapshot ArtifactRef, carries fixed carrier facet and provenance, and Console displays the candidate/manifest/snapshot state. Candidate validation deliberately fails on pending rights, draft authorization and authoritative readback rather than approving or delivering it. Focused PostgreSQL content/live/video/production/build-plan/release suite `22 passed`; Ruff, Console typecheck and build passed. | N/A: this is a reviewable candidate only. No actual rights evidence, Worker preflight, short-lived write authorization, DeliveryAttempt, authoritative readback, execution reconciliation or exposure is created. Deployment signing material and operational evidence are tracked as `EXT-P1-RELEASE-SIGNING` and `EXT-P1-RELEASE-EVIDENCE`; no Phase 1 checkbox is checked while Phase 0 exits remain open. | 2026-07-24 |
| `CHK-1223`,`CHK-1224` preparation (items remain open) | coderdailyone | migration `055`; `/functional-live-room-plans/{plan_code}/clone` and Console clone controls recompile a source plan's current, revision-matched business inputs to a different target room. A clone creates a fresh Variant/Configuration/Blueprint/BuildPlan and records source provenance plus the explicit list of cleared target state. It cannot reuse the source target ID, and refuses a source whose ContentProject has advanced, preventing an old selection from silently mixing with newer content. It never carries release, delivery, authorization, execution or readback state. Focused PostgreSQL live-room suite `6 passed`; Ruff, Console typecheck and build passed. | N/A: this is a plan-only clone, not a confirmed-empty-room copy. Authoritative target identity/empty/unlive preflight, write authorization and Worker execution are still absent, so no non-empty-room protection or Phase 1 completion is claimed. | 2026-07-24 |
| `CHK-1225` preparation (item remains open) | coderdailyone | migration `056`; every generated BuildPlan operation is persistently linked to one or more generated `MaituSceneBlueprint` or `LayerBlueprint` targets with a typed preflight/configure/mutate/write/verify/save relationship. `/functional-live-room-plans/{plan_code}/trace` resolves those links through Shot, ProgramSegment and ScriptBlock to the fixed StoryBrief fact/template revision refs; Console loads the trace on demand. New plans link on compilation; older plans receive the same append-only relation projection when first traced. Focused PostgreSQL live-room and shared BuildPlan suites `8 passed`; Ruff, Console typecheck and build passed. | N/A: the trace proves planned provenance only. It does not yet connect executed operation results, authoritative room readback, delivery or exposure evidence, so the end-to-end Phase 1 trace/evidence gate remains open. | 2026-07-24 |
| `CHK-2140`-`CHK-2146`,`CHK-2307` preparation (items remain open) | coderdailyone | Functional live-room compilation now resolves each selected asset's current `AssetConstraintProfile` revision into the material snapshot and Variant constraint snapshot. It applies normalized allowed regions, forbidden-region rejection, size/scale bounds, named regions, `table_surface`, anchor placement, layer top/bottom and relative z-order, overlap rejection plus crop/rotation/audio policy projections to generated `LayerBlueprint` evidence. Hard profile failures block the branch. Asset Library exposes the added rule vocabulary. PostgreSQL integration proves table-surface product placement and missing hard-region rejection; focused live-room/material-library suites `9 passed`; Ruff and Console build passed. | N/A: this is a deterministic constraint compiler, not the required solver adapter. It lacks room-private override revisions, polygons, distance/objective optimization, minimal conflict sets, execution-side audio/property enforcement and real rights-aware material resolution. No Phase 2 checkbox is checked. | 2026-07-24 |
| `CHK-2180`-`CHK-2190`,`CHK-2200`,`CHK-2202` preparation (items remain open) | coderdailyone | migration `057`; AssetGroup continues to support many-to-many membership. Material packs now expose a `draft -> published` action and only published packs can enter a live-room plan. Planning resolves every selected pack once, records its pack code, revision, fingerprint, entries and exact active asset whitelist in the Variant/Configuration/BuildPlan inventory snapshot, then persists selected pack codes on the plan and release snapshot. Asset Library shows publish state; Live Room only offers published packs alongside separate group and loose-asset selection. PostgreSQL material-library/live-room suites `10 passed`; Ruff, Console typecheck and all three Vite builds passed. | N/A: this is a functional first slice, not full Phase 2 material resolution. It has no pack revision editing/supersession, nested total/classification packs, role-domain merge modes, exclusivity, occurrence enforcement, per-branch overrides, rights filtering, solver evidence, version diff or complete selection explanation. No strict checkbox is checked while Phase 0 exits remain open. | 2026-07-24 |
| `CHK-2263`-`CHK-2266`,`CHK-2269`,`CHK-2270`,`CHK-2272`,`CHK-2300` preparation (items remain open) | coderdailyone | migration `058`; a typed `content-strategy.v2` revision now keeps source CaptureSession bridge rows and rejects incomplete or cross-target session combinations with stable `CONTENT_STRATEGY_CROSS_ROOM_SOURCE`. Its immutable projection is content-only and forces `buildability=reference_only`; `content_readiness`, `layout_fidelity`, `buildability`, category/program outline/material cues/reviewed examples/layout reference/provenance are independently persisted and returned. `layout-hypothesis.v1` remains a separate layout template kind. ContentProject accepts only ready, published `content-strategy.v2` references, pins their revision/fingerprint, and cannot turn a reference layout into an executable Blueprint. Live Research provides a Content Strategy tab for selecting one watched target's completed sessions, creating the strategy template/revision and publishing its projection. Live-room `?run=` deep links now synchronize their selected plan, with a regression test proving `RUN-A -> RUN-B` switches the loaded detail. Clean migrations `001`-`058` and focused PostgreSQL suite `28 passed`; Ruff, Console test, TypeScript and all three Vite builds passed. | N/A: ASR/OCR extraction, timeline/player evidence linkage, source-fact redaction proof, similarity/copyright/PII review and full template review workflow remain unimplemented. This first usable authoring path must not be treated as a released source-analysis or safety-complete template capability; no strict checkbox is checked while Phase 0 exits remain open. | 2026-07-24 |
| `CHK-2206`-`CHK-2209` preparation (items remain open) | coderdailyone | migration `059`; `AssetGap` now persists gap type, impact, alternatives, resolution snapshot/evidence, resolver and waiver reason. An append-only `asset_gap_resolution_events` log records creation and every state change. The first operational lifecycle allows `open -> candidate_found -> resolved`, or scoped waiver/obsolete paths; direct resolution and illegal post-resolution transitions return stable validation errors. Candidate/resolution checks lock the gap and require an active asset with the requested `material_role` and a classified non-unavailable capability; resolution snapshots the asset classification, checksum, binding identifier, updated time and active constraint-profile revision. Asset Library supports creating typed gaps, role-filtered candidate selection, candidate confirmation, fixed resolution and reason-required waiver, with record count and snapshot indication. PostgreSQL material/live/content suites `19 passed`; Ruff, frontend 33 tests, TypeScript and all three Vite builds passed. | N/A: a candidate is not yet bound to a real ProductionVariant revision and has no RightsGrant or solver preflight because those domain capabilities are not implemented. Per-branch waiver scope, source StoryBrief/ScriptBlock/Shot links, alternate-candidate recomputation and release/selection filtering remain incomplete. No strict checkbox is checked while Phase 0 exits remain open. | 2026-07-24 |
| `CHK-2300` preparation update (item remains open) | coderdailyone | Content Project creation now queries Live Research and exposes only published, `content_readiness=ready`, `buildability=reference_only`, `template_kind=content_strategy` templates. The form uses one explicit primary-template radio choice plus independent secondary-template checkboxes, automatically removing the primary from secondary selections; each option shows category, fixed published revision and available program modules. The existing backend validates duplicate selections and pins the selected immutable published revision before confirmation/generation. Frontend TypeScript and focused Console regression passed. | N/A: no category/goal-based recommendation, compatibility scoring, duplicate module conflict explanation, editable module contribution decision or adopted/unadopted explanation exists yet. This is a selection surface only and no strict checkbox is checked. | 2026-07-24 |
| `CHK-2202`,`CHK-2205` preparation update (items remain open) | coderdailyone | Functional live-room material snapshots now preserve each selected asset's immutable selection sources: loose asset, expanded AssetGroup and/or published MaterialPack, in addition to the already fixed classification and constraint-profile revision. The plan detail visualizes the snapshot, source codes, execution capability, active constraint profile and fixed material-pack revisions. A PostgreSQL integration test proves pack-expanded assets carry the material-pack source and remain detached from later group edits; focused live-room suite `8 passed`, Ruff, TypeScript and Console regression passed. | N/A: there is still no resolver score, exclusion-code report, dedup/exclusivity explanation, RightsGrant merge or branch-specific selection override. This is transparent fixed-source evidence, not the full selection-ranking implementation, and no strict checkbox is checked. | 2026-07-24 |
| `CHK-3110`,`CHK-3181` preparation (items remain open) | coderdailyone | migration `060`; a queued unclaimed functional video plan now owns an append-only `functional_video_timeline_revisions` history. `PUT /functional-video-plans/{plan}/timeline` requires an expected revision and the complete current video clip set, validates clip duration/transition and 30-120 second total duration, reflows video and corresponding voice tracks, persists a new timeline revision, and synchronizes the Worker-facing `shot_list` and target duration in the same transaction. The UI provides per-clip duration/transition editing, visible revision and duration, stale conflict reporting, and locks the editor after the render job is claimed. PostgreSQL integration proves revision `1 -> 2`, Worker ShotList synchronization and stale rejection; Ruff, focused Console test, TypeScript and all three Vite builds passed. | N/A: the current functional timeline remains an OTIO-compatible JSON projection expressed in milliseconds. It does not yet expose RationalTime/TimeRange, source timebase/PTS mapping, arbitrary track/clip insertion, transitions with media handles, subtitle/audio-specific editing, formal timeline diff or full renderer manifest integration. No strict Phase 3 checkbox is checked. | 2026-07-24 |
| `CHK-2203`-`CHK-2205` preparation update (items remain open) | coderdailyone | Material Library now exposes a deterministic selection-preview API and UI for a requested role and carrier. It rejects role mismatches, unavailable/unclassified assets, and non-`maitu_bound` inputs for live-room candidates before sorting. Returned candidates include fixed score parts, selection reasons and current constraint-profile references; excluded assets carry stable exclusion codes. The UI shows candidate/exclusion sets and explicitly flags `RIGHTS_GRANT_NOT_IMPLEMENTED` and `CONSTRAINT_SOLVER_NOT_RUN` as unverified gates. PostgreSQL material/live-room suite `10 passed`; Ruff, focused Console test, TypeScript and all three Vite builds passed. | N/A: quality, semantic, aspect/duration, repetition and effect scores are not yet available. This endpoint does not select or mutate a branch, does not merge rights/pack occurrence/exclusivity, and cannot substitute for the planned solver. No strict checkbox is checked. | 2026-07-24 |
| `CHK-4102`-`CHK-4104`,`CHK-4120`-`CHK-4124` preparation (items remain open) | coderdailyone | migration `061`; operation sessions can now bind to an existing live-room plan, which supplies its fixed ContentProject/Variant/release reference when present. A new append-only content-exposure record requires a source type (`manual_observation`, `served_log`, or `recording_match`), evidence note, confidence, a session-contained time interval, referenced plan and one of that plan's scene codes. The service rejects unknown session/plan/scene, mismatched session-plan and overlapping active intervals. Operations UI supports both session registration and observed-scene evidence entry, and clearly marks exposures without a release as `未绑定 release 证据`; a plan by itself never creates exposure. PostgreSQL focused suite `30 passed`; frontend 33 tests, TypeScript and all three Vite builds passed. | N/A: this is manual/source-tagged observation only. It lacks immutable ReleaseManifest approval/delivery verification, platform adapter ingestion/idempotency, correction/supersession workflow, TimeMapping/drift, metric contracts and complete attribution snapshotting. No strict checkbox is checked. | 2026-07-24 |
| `CHK-4101`,`CHK-3183` preparation (items remain open) | coderdailyone | ReleaseRepository now provides a current-manifest release list and `GET /api/releases`, alongside the existing detail API. Console `/production/releases?release={release_code}` is a read-only ReleaseManifest workspace with stable deep-link selection, status distinction, immutable source/asset/rights/quality/lineage snapshots, approvals, delivery states and returned read-back evidence. PostgreSQL `test_release_postgres.py` `4 passed`; Ruff, frontend `33 passed`, TypeScript and all three Vite builds passed. Current-code validation on isolated `:8014` returned a real release list, and OpenAPI exposes both release list and detail routes; the old `:8000` process remains untouched. | N/A: the workspace deliberately does not expose approval, revoke or delivery mutation commands, and it does not yet show downstream impact/exposure, role permissions, authorization or full release history. No strict Phase 4 / Phase 3 checkbox is checked. | 2026-07-24 |
| `CHK-4160`-`CHK-4164`,`CHK-4167` preparation (items remain open) | coderdailyone | Descriptive attribution now groups a session metric only by source-backed active ContentExposure: one observed plan is grouped under that plan, multiple observed plans remain a session-scoped mixed-content group, and a session with no exposure remains its own `session_only` group rather than inheriting planned ContentProject identity. Each immutable report stores schema/version, method, metric grain, selected/observed/session-only counts, source kinds, release-bound exposure count, scene coverage and confidence. UI labels reports as either `有展示证据` or `无展示证据` and repeats that the metric remains session-grain and descriptive. PostgreSQL operations/live-room integration `9 passed`; Ruff, frontend `33 passed` and TypeScript passed. | N/A: there is no metric ingest/DataContract snapshot, event-time alignment, individual-scene metric allocation, intervals, review/publish state, causal method or correction/supersession workflow. Source-backed grouping does not imply delivered-release verification or causality. No strict Phase 4 checkbox is checked. | 2026-07-24 |
| `CHK-4103`,`CHK-4140` preparation (items remain open) | coderdailyone | migration `062`; an operation session now records optional external session ID, account/principal, target resource, valid IANA source timezone and structured source evidence. `(platform, external_session_id)` is a partial unique identity and the service uses `ON CONFLICT` replay resolution; binding a live-room plan also fixes the project, variant/release and default target resource, while conflicting explicit target input is rejected. Console adds the source-context fields and exposes them in the session list. PostgreSQL migration parse plus operations/live-room suite `25 passed`; Ruff, frontend `33 passed`, TypeScript and all three Vite builds passed. | N/A: this is an operator-entered source context, not a platform ingest adapter. It lacks source-system/event batch checksum, account authorization, event-time/processing-time separation, source-contract validation, update/delete/tombstone/late-event processing and immutable imported metric snapshots. No strict Phase 4 checkbox is checked. | 2026-07-24 |
| `CHK-4124`,`CHK-4162` preparation (items remain open) | coderdailyone | `GET /functional-operations/sessions/{session_code}/content-timeline` now resolves active source-backed exposure intervals into a read-only session timeline. Every span carries plan/variant/release, source kind/confidence, resolved `MaituSceneBlueprint` scene, Shot code and normalized layer asset/role/capability projection; summary exposes observed/unobserved seconds and coverage ratio. Console lets an operator open a session timeline and inspect its actual content spans. PostgreSQL operations/live-room suite `9 passed`; Ruff and TypeScript passed. | N/A: the projection is computed from current persisted Blueprint and does not yet persist ContentTimelineSpan revisions, time offset/drift, correction/supersession, ProgramSegment/ScriptBlock/product/CTA mapping, metric buckets or release delivery verification. No strict Phase 4 checkbox is checked. | 2026-07-24 |
| `CHK-1103`,`CHK-1105`,`CHK-7101` preparation update (items remain open) | coderdailyone | commit `35e1b3c`; `/knowledge/facts` is now the authoritative ProductFactCard workspace: it creates drafts, creates immutable content revisions and approves or rejects a selected revision with an explicit operator name/reason. Fact content captures product identity, positioning, verified facts, scenarios, material keywords, compliance notes and source references. ContentProject creation queries the same `/api/maitu/workbench/product-fact-cards` contract and only permits selection of a card whose current approved version is actually approved; the existing backend then pins and revalidates that exact version before confirmation/generation. The API regression covers the exact route plus reject payload; focused frontend tests `7 passed`, TypeScript and all three Vite builds passed. `EXT-P1-FACT-SOURCES` records the remaining real source-material requirement. | N/A: FactClaim relation, field-level effective period/platform scope editing, conflict detection, full use-record/lineage search and real source evidence still require the remaining knowledge-domain implementation and operator data. This unifies the actual content input path but does not qualify a Phase 1 or Phase 7 strict checkbox. | 2026-07-25 |
| `CHK-2180`-`CHK-2184`,`CHK-2188`,`CHK-2200` preparation update (items remain open) | coderdailyone | commit `667b16d`; the Material Library now exposes the existing many-to-many membership contract as an editable group workspace: an operator can select any group and independently add/remove each asset, while the same asset can remain selected in other groups. MaterialPack authoring now captures each entry's required/optional/alternative mode and min/max occurrence bounds, displays the exact persisted rule beside resolved assets, and still uses the existing immutable draft/publish API. Frontend regression proves group member replacement sends a complete membership set and pack creation submits `required` plus occurrence bounds; focused frontend tests `2 passed`, TypeScript and all three Vite builds passed. | N/A: packs still have no revision editing/supersession UI, role-domain merging, exclusivity enforcement or RightsGrant-aware resolution. The functional selection compiler remains a later task, so no strict Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-2182`,`CHK-2188`,`CHK-2189`,`CHK-2190` preparation update (items remain open) | coderdailyone | commit `5851845`; MaterialPack now has an operator-facing immutable revision workflow. `GET /assets/material-packs/{pack_code}/revisions` lists fingerprinted historical entries; `POST .../revisions` accepts `expected_revision`, locks the pack, returns stable `409 MATERIAL_PACK_REVISION_CONFLICT` for a stale editor, creates the next revision, and moves only the current pack state back to `draft`. The Asset Library opens the selected pack with its current entries as a new-revision draft, permits entry addition/removal, shows retained history, then requires an explicit publish action before the new revision can be selected by a new live-room plan. Existing plan/variant material snapshots retain their already-expanded revision/fingerprint/asset whitelist and are not rewritten by this action. Route regression proves history, draft creation and stale-write rejection (`2 passed`); Asset Library regression proves the revision payload (`4 passed`); Ruff, TypeScript and all three Vite builds passed. | N/A: a revision does not yet receive an independent `published/superseded` lifecycle record, diff view, rollback command, nested overall/classification packs, role-domain merge/replacement, exclusivity, occurrence enforcement, RightsGrant filtering or solver explanation. PostgreSQL persistence coverage was added but remains skipped in this environment because `ASSETGRAPH_TEST_DATABASE_URL` is not configured; provide a migrated disposable PostgreSQL database to run it. No strict checkbox is checked. | 2026-07-25 |
| `CHK-2190` preparation update (item remains open) | coderdailyone | commit `e851ce8`; the MaterialPack revision panel now compares the selected current revision with its immediately preceding immutable revision. It shows Entry-level additions and removals using the complete selection kind/code, mode and occurrence-bound semantics, so an operator can see changes before creating or publishing the next draft. The view explicitly says when no structured Entry changed. Asset Library regression covers both added and removed rules (`5 passed`); TypeScript and all three Vite builds passed. | N/A: this is a compact adjacent-entry diff only. It has no arbitrary revision comparison, role/description/status diff, inline decision/approval, rollback-as-new-revision, downstream usage impact, or release compatibility analysis. No strict checkbox is checked. | 2026-07-25 |
| `CHK-2200`-`CHK-2205`,`CHK-2307` preparation update (items remain open) | coderdailyone | commits `6a9408e`, `0d10849`; Live Room configuration now derives role candidates from the selected loose assets, expanded AssetGroups and published MaterialPacks. An operator may fix one asset per role or leave automatic selection. The backend rejects an override outside the selected snapshot or with a role mismatch; otherwise a deterministic score selects candidates by role, executable capability and bound constraint profile. Every Shot/Layer records the explicit or automatic decision, candidate scores and reasons in `constraint_evidence`; the fixed overrides and decision list are retained in the Variant/configuration snapshots, `MaituSceneBlueprint` v2 and branch-quality report, and rendered in the plan workspace. Clone recompilation preserves this business input while still clearing target execution/release state. PostgreSQL live-room suite `9 passed`, Ruff, frontend API/UI regression `9 passed`, TypeScript and all three Vite builds passed. | N/A: this is a deterministic local resolver only. It does not merge RightsGrant, pack exclusivity/occurrence, semantic/format/repetition/effect scores, room-private constraint revision diffs, solver evidence or AssetGap branch binding. It cannot satisfy the full Phase 2 solver/release gate, so no strict checkbox is checked. | 2026-07-25 |
| `CHK-2206`-`CHK-2209`,`CHK-2307` preparation update (items remain open) | coderdailyone | commit `c0363ac`; the live-room input form now permits an operator to explicitly associate open/candidate AssetGaps with a plan. The material library resolves a fixed gap-state snapshot containing role, severity, status, alternative/resolution data and fingerprint; the snapshot travels through the ProductionVariant, live-room configuration, BuildPlan inventory and release snapshot. An `open` or `candidate_found` gap intentionally adds `asset_gap_unresolved:{gap_code}:{status}` to the BuildPlan blockers even when the selected assets otherwise compile, while waived/resolved snapshots remain traceable without that blocker. Plan detail shows the fixed gap references; cloning carries the gap codes and re-resolves their current state for the new plan, so it cannot silently reuse a stale state. Schema regression and live-room UI/API regressions passed (`5 passed`); Ruff passed; focused PostgreSQL lifecycle coverage was added but skipped locally with the other `12` database cases because `ASSETGRAPH_TEST_DATABASE_URL` is not configured. TypeScript and all three Vite builds passed. | N/A: a gap is currently an explicit operator-associated input, not an automatically inferred per-variant requirement. There is no RightsGrant/preflight proof before `candidate_found`, per-branch waiver record, automatic candidate recomputation, impact graph, pack occurrence/exclusivity integration or release-time rights revalidation. No strict Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-2243`,`CHK-2244`,`CHK-2261`,`CHK-2262`,`CHK-2265`,`CHK-2267`,`CHK-2269`,`CHK-2271`,`CHK-2272` preparation update (items remain open) | coderdailyone | commit `1a55a3b`; Content Strategy authoring now supports multiple source-bounded modules and optional cleaned examples. Each module/example selects one of the template's completed same-room CaptureSessions and records a `[start_ms,end_ms)` interval; the write path rejects populated references outside that immutable source bridge or beyond a known normalized timeline duration, while incomplete drafts remain publish-blocked with stable evidence codes. The published projection verifies every module is traced, permits only `layout_fidelity=none|approximate`, and scans all strategy text for source price/promotion/inventory markers before refusing publication. The Console live-research route exposes the strategy workspace, preserves demo context while changing tabs, and renders source-session links for fixed module evidence. Focused backend contracts plus PostgreSQL integration `17 passed`; frontend API/Console/live-research regressions `16 passed`, TypeScript and all three Vite builds passed. | N/A: no real ASR/OCR/keyframe/visual provider execution or source-analysis artifact exists; the UI links evidence to a CaptureSession but does not yet seek a player/timeline range. The marker gate is not a sufficient fact-redaction, copyright, text-similarity, brand/host imitation or PII review. There is no issue workflow or complete human-review audit, so all strict Phase 2 items remain open. | 2026-07-25 |
| `CHK-2302`-`CHK-2306` preparation update (items remain open) | coderdailyone | commit `f03d3a8`; a ContentProject now persists a revision-bound contribution decision for each selected published content strategy: available, accepted and rejected module keys, selection role and generic material cues. The creation UI lets an operator keep the primary strategy's default skeleton, opt into secondary modules, or remove any contribution; duplicate adopted module keys are highlighted before submission and rejected by a stable backend rule. The compiler uses adopted modules for ScriptBlock source attribution, keeps an accepted primary strategy as the overall fallback skeleton, and turns allowlisted `material_cues` into Shot material-role requirements only. The project chain displays adopted/unadopted modules and cues alongside the existing fixed template revision references. PostgreSQL functional-content suite `9 passed`; frontend API/Console/live-research regressions `16 passed`, TypeScript and all three Vite builds passed. | N/A: no category/goal recommendation, semantic retrieval, template similarity scoring, LLM context compiler, program-outline-to-full-script transformation, or explicit secondary replacement mode exists. Generic material cues are not material selection, rights approval or an executable external layout. No strict Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-2243`,`CHK-2244`,`CHK-2267`,`CHK-2271` preparation update (items remain open) | coderdailyone | commit `9b63467`; fixed content-strategy module and cleaned-example evidence now links to `/research/live-sources?view=sessions&session=...&in=...&out=...`. The recording workspace parses that bounded interval, selects the referenced CaptureSession, initializes the player/timeline and IN/OUT fields at the exact evidence range, and clears the interval when an operator manually selects a different session. Console keys the sessions workspace by query string so an in-app deep link cannot retain an earlier evidence range. The frontend regression proves the generated URL and a `12.5-44` second direct-open selection; Console/live-research regressions `8 passed`, TypeScript and all three Vite builds passed. | N/A: this consumes existing normalized timeline coordinates only. It does not create capture media, normalize chunks/ASR/OCR/keyframes, prove media integrity, persist a review decision, redact source facts, or perform similarity/copyright/PII review. It therefore improves the evidence-review UI but does not complete any strict Phase 2 item. | 2026-07-25 |
| `CHK-1101`,`CHK-1102`,`CHK-1105` preparation update (items remain open) | coderdailyone | commit `03de7c2`; the ContentProject workspace now exposes an input-revision form for the project name, generation goal, platform, audience, persona, tone, duration, theme/story/design, must include/avoid, interaction, conversion, staging, visual and audio requirements. Saving always calls the existing expected-revision `PATCH` contract and creates a new draft revision; it never mutates a confirmed revision in place or clears existing fact/template pins that the form does not edit. The UI makes the required re-confirmation visible, and the frontend API regression verifies PATCH method, expected revision and structured input lists. Frontend focused regressions `9 passed`, TypeScript and all three Vite builds passed; Ruff passed. | N/A: FactCard/template selection changes, source/effective-scope conflict display, structured DesignBrief field overrides, provider artifacts and full revision/diff/lineage browsing remain incomplete. A local PostgreSQL instance at `:55433` predates migrations `051`/`058`, so it was not used as current-schema integration evidence. No Phase 1 checkbox is checked. | 2026-07-25 |
| `CHK-2301` preparation update (item remains open) | coderdailyone | commit `4f663f0`; ContentProject creation now ranks up to three published `content_strategy` templates against the current generation goal, theme, story and detailed design. The deterministic local ranking uses only explicit target-category substring matches, program module title/purpose matches and compatibility-tag matches; every displayed result shows those exact signals, and it also warns when the candidate's module keys overlap an already accepted selection. An operator can make a recommendation the primary template, after which the existing contribution checkboxes remain the decision authority. The regression proves a category/module/tag match scores above unrelated templates; frontend focused regressions `10 passed`, TypeScript and all three Vite builds passed. | N/A: there is no semantic embedding retrieval, learned similarity, goal taxonomy, effect-driven recommendation, automatic secondary-template adoption or server-side recommendation snapshot. This is an explainable UI aid only; no Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-2140`-`CHK-2149` preparation update (items remain open) | coderdailyone | commit `e41ab1d`; Material Library constraint authoring now uses typed fields rather than a raw JSON input. It covers named allowed/forbidden regions, normalized region geometry, aspect/size/scale, crop/rotation, top/bottom and role-relative layers, overlap/distance/anchor rules, loop/mute/volume, and a table-surface rule whose defaults bind `product_display` to `bottom_center`. Positional rules expose a fixed-aspect normalized region preview, and every save continues to create an immutable constraint-profile revision through the existing API. The frontend regression creates the table-surface constraint and asserts the exact structured payload; focused assets/content/research/Console regressions `13 passed`, TypeScript and all three Vite builds passed. | N/A: parameter legality is still generic JSON-schema validation; there is no polygon editor, source-pixel evidence, profile scope/room override workflow, hard-constraint intersection, solver adapter, infeasible conflict diagnosis or execution-time enforcement. No strict Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-2140`-`CHK-2146`,`CHK-2149` preparation correction (items remain open) | coderdailyone | commit `0ca3011`; the typed Material Library editor now emits the compiler contract exactly: region rules write normalized `x/y/width/height`, named regions and table surfaces use `name`, scale uses `min_scale/max_scale`, and anchors target an explicit named region. The compiler accepts legacy persisted `region` fields, records available table-surface policies in layer evidence, and automatically fits/anchors the declared `product_role` onto the surface, so a product asset no longer needs to repeat the background's table rule. Pure backend regressions prove automatic placement and legacy compatibility; focused frontend regressions `13 passed`, TypeScript and all three Vite builds passed; Ruff passed. | N/A: this corrects the deterministic compiler/editor contract only. There is still no polygon editor, source-pixel evidence, room-private override workflow, constraint solver, hard-constraint intersection, minimal conflict diagnosis or execution-time enforcement. No strict Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3108`,`CHK-3110`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `ac7ec73`; the rendered-video workspace now lets an operator reorder the fixed set of video segments with explicit up/down controls in addition to duration and transition edits. The optimistic-revision update preserves that requested order in the video track, synchronizes per-shot audio clip ranges/order while retaining unrelated audio such as BGM, and rebuilds the worker ShotList in that same order so narration, subtitle timing and FFmpeg concatenation consume the edit rather than the former source order. Backend regression proves `SHOT-02 -> SHOT-01` propagates across video/audio/render input; frontend regression asserts the exact ordered PUT body. Focused tests, TypeScript, all three Vite builds and Ruff passed. | N/A: this remains a constrained fixed-clip editor, not a generic NLE. Timeline time is still the pre-existing millisecond schema rather than OTIO `RationalTime/TimeRange`; source-range editing, visual/audio/subtitle track authoring, transition rendering semantics, revision diff UI, timeline artifact lineage and stale propagation are still incomplete. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3120`,`CHK-3125`,`CHK-3143`-`CHK-3146`,`CHK-3180`,`CHK-3183`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `1d456d1`; the functional rendered-video projection now exposes the already persisted Worker stages, stage attempt/error diagnostics, final-asset reference, QC report and artifact metadata. `/production/videos` renders ordered workflow state, explicit QC pass/blocked checks, preview when available, and artifact download entries annotated with producing stage, MIME type, file size and checksum prefix. The API is a read-only projection of the existing job/repository state, so it does not change Worker or release semantics. The frontend regression covers the workflow/QC display alongside timeline save; focused frontend regressions `14 passed`, backend unit regression, TypeScript, all three Vite builds and Ruff passed. | N/A: this is progress/QC observability only. It does not add content-addressed stage manifests, CPU/GPU/cost collection, real release gating, waiver approval, rights checks, quality policy coverage, artifact authorization or video delivery. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-4102`-`CHK-4104`,`CHK-4120`,`CHK-4124`,`CHK-4164` preparation update (items remain open) | coderdailyone | commit `6032723`; `/operations/live-sessions` now records the operator-entered source-timezone session start/end rather than silently replacing it with a client-side one-hour interval, and opens the resulting observed-content timeline after creation. Session summaries explicitly state `仅计划`, `已关联发布修订`, or `已观察展示`; the final state is derived only from an active ContentExposureEvent, so a plan/release link is never rendered as delivery or exposure proof. Frontend regressions assert the submitted interval and active-exposure-only label; focused frontend regressions `16 passed`, TypeScript and all three Vite builds passed. | N/A: this is still manually recorded operational evidence. It does not prove delivery, ingest served logs, enforce exact ReleaseManifest binding, implement exposure correction/revision or TimeMapping, create metric/data-contract snapshots, or turn descriptive attribution into causal claims. No strict Phase 4 checkbox is checked. | 2026-07-25 |
| `CHK-3110`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `b10b3ba`; the existing immutable `functional_video_timeline_revisions` records now have a typed read-only API and `/production/videos` history panel. Each revision shows the fixed clip order, total duration and author; the current revision also summarizes the ordering and duration change relative to its predecessor. Saving an editable queued plan invalidates/reloads that history, while historical revisions cannot be applied back to a Worker. The frontend regression loads the history alongside the timeline editor; focused test, TypeScript, all three Vite builds and backend Ruff passed. | N/A: historical records are view-only and still use the pre-existing millisecond document. There is no OTIO RationalTime mapping, full structured diff, restore-as-new-revision command, source-range/track comparison, stale propagation or artifact lineage UI. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3110`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `950a43e`; `/functional-video-plans/{plan_code}/timeline-revisions/{source_revision}/restore` now restores a historical constrained timeline as the next immutable revision. It accepts the loaded expected revision, derives its complete video clip order/durations/transitions from the selected historical document, then reuses the same transactional timeline editor to revalidate clip set/duration/transition, reflow linked audio, synchronize the queued Worker's ShotList and update the target duration. The Console exposes a restore icon on non-current history rows only while the job is still queued; restored history remains retained and the command returns `409` for a stale editor. HTTP route and timeline unit regressions passed (`3 passed`); the Console request regression passed (`2 passed`); Ruff, TypeScript and all three Vite builds passed. | N/A: restore currently reapplies only the constrained video edit surface, not arbitrary tracks/source ranges or a full OTIO document. It has no restore confirmation/approval, arbitrary diff, stale-impact graph, RationalTime mapping, artifact lineage or post-claim branch-copy workflow. The PostgreSQL persistence journey is added but skipped locally because `ASSETGRAPH_TEST_DATABASE_URL` is unset; run it against a current migrated database before any strict Phase 3 item is closed. | 2026-07-25 |
| `CHK-1103`,`CHK-1105` preparation update (items remain open) | coderdailyone | commit `578feea`; the ContentProject detail workspace now has an independent approved-FactCard editor. Narrative-input saves continue to omit fact-card fields, whereas an actual fact selection change submits only `expected_revision` plus `fact_card_codes`, causing the existing backend to create a new draft revision and pin the newly current approved versions. The UI initializes from immutable pinned facts, shows the exact fixed versions in the project summary, and separately warns for historical pins that are no longer currently selectable so they cannot be silently overwritten by an unrelated edit. Frontend API regression, TypeScript and all three Vite builds passed. | N/A: this does not yet expose fact-field source/effective-period/platform-scope detail, conflict diagnosis, FactClaim relations, full use-record search or source-evidence review. A fact selection change intentionally cannot retain a historical pin with no currently approved selectable version; operators must resolve that fact-card state before creating a replacement selection. No strict Phase 1 checkbox is checked. | 2026-07-25 |
| `CHK-2300`,`CHK-2302`-`CHK-2306` preparation update (items remain open) | coderdailyone | commit `c0d144d`; the ContentProject detail workspace now independently edits one primary content-strategy template, any number of secondary templates and each selected template's accepted module keys. A save sends the complete selection plus explicit contribution decisions in one expected-revision PATCH, so a template edit cannot accidentally discard prior module choices or fall back to the backend's primary/default and secondary/empty rules. Duplicate adopted module keys block the save, and immutable historical template selections that are not currently selectable are visibly retained and warned about. Frontend API regressions, TypeScript and all three Vite builds passed. | N/A: the editing surface does not implement semantic retrieval, recommendation snapshotting, source-text similarity review, structured template issue workflow, automatic module replacement or full revision/diff/lineage browsing. Historical pins remain usable only under the existing backend validation of their fixed revisions. No strict Phase 1 or Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-1121`,`CHK-1123`,`CHK-1125` preparation update (items remain open) | coderdailyone | commit `88002f2`; a draft DesignBrief now exposes its parsed objective/theme/story/audience/persona/tone/platform/duration and requirement lists as structured fields. `PATCH /content-projects/{code}/design-brief` accepts only bounded typed overrides, locks the current draft, writes a fresh immutable DesignBrief revision with retained raw input, parsed result, prior-plus-new `user_overrides`, recomputed fingerprint and unresolved questions, then supersedes the former draft. Confirmed briefs remain immutable; an operator starts a new parse before revising again. Frontend API regression, backend schema regression, Ruff, TypeScript and all three Vite builds passed; a PostgreSQL integration test covers draft replacement and confirmation of revision 2. | N/A: the parser remains local deterministic rather than an authorized provider call, and there is no raw model-response ArtifactRef, field-level visual diff, automatic save, provider/model evidence, question-specific response records or full audit UX. `ASSETGRAPH_TEST_DATABASE_URL` is not configured in this environment, so the new PostgreSQL integration test is present but skipped here; it must run against a schema through migration `051` or newer before any strict item is closed. | 2026-07-25 |
| `CHK-1103`,`CHK-7101` preparation update (items remain open) | coderdailyone | commit `db6639a`; ProductFactCard content now formally accepts `valid_from`, `valid_until` and `applicable_platforms`, with timezone and ordering validation. The facts workspace can create a revision with those values and exposes them beside the approved content and source reference. This makes the existing ContentProject confirmation/generation scope checks reachable through the same strict API contract instead of relying on undeclared JSON fields. Backend contract tests, frontend knowledge API regression, Ruff, TypeScript and all three Vite builds passed. | N/A: a source reference is still a declared URL/object rather than a managed SourceEvidence/FactClaim relation; there is no field-level provenance, conflict detector, multi-source review, expiration notification, scope query index or full use-record graph. No strict Phase 1 or Phase 7 checkbox is checked. | 2026-07-25 |
| `CHK-1149` preparation update (item remains open) | coderdailyone | commit `6474ef8`; `GET /content-projects/{code}/content-chain-revisions` and the ContentProject workspace now expose the immutable revisions across ContentProject, DesignBrief, StoryBrief, Script, Program and ShotList. Each record carries code, revision, status, actor/timestamp/fingerprint and direct upstream source reference, so an operator can inspect the actual history rather than inferring it from the current generated chain. Frontend API regression, backend Ruff, TypeScript and all three Vite builds passed; a PostgreSQL journey test asserts all six object kinds and the Script-to-Story source link. | N/A: this is a read-only summary timeline, not a structured field/block/segment/shot diff, per-revision detail drawer, restore-as-new-revision command, human script/segment/shot editing workflow or complete downstream stale-impact browser. The PostgreSQL journey test is skipped locally without `ASSETGRAPH_TEST_DATABASE_URL`; no strict Phase 1 item is checked. | 2026-07-25 |
| `CHK-1105` preparation update (item remains open) | coderdailyone | commit `e12f2f9`; both ContentProject creation and the independent FactCard-pin editor now display each currently approved FactCard's declared platform range, validity window and first source reference. Before saving, a deterministic conflict explanation flags a selected card whose platform scope excludes the project platform and flags divergent product name/brand/category/positioning values among cards with the same product code. The focused frontend regression asserts both conflict classes; TypeScript and all three Vite builds passed. | N/A: this is an operator-facing deterministic warning based on structured current-card fields. It is not a managed FactClaim/source-evidence graph, semantic contradiction engine, effective-period overlap solver, reviewer decision workflow, server-persisted conflict snapshot or source-evidence verification. Existing confirmation/generation scope revalidation remains the hard backend gate; no strict Phase 1 checkbox is checked. | 2026-07-25 |
| `CHK-1143`,`CHK-1146`,`CHK-1147`,`CHK-1149` preparation update (items remain open) | coderdailyone | commit `fac2d63`; the ContentProject workspace now supports a human Script revision after the current ContentProject, DesignBrief and matching StoryBrief have been confirmed. The endpoint validates current revision/fact/template pins and FactCitation ranges, creates and confirms a new `human_script_revision`, then deterministically creates/ confirms the dependent Program and ShotList revisions. The editor preserves citation/template/action fields; blocks carrying fixed FactCitation spans are read-only so their evidence cannot be silently invalidated. Frontend regressions include structured submission and citation-offset round-trip; backend schema tests, Ruff, TypeScript and all three Vite builds passed. A PostgreSQL journey test asserts Script/Program/ShotList move to revision 2. | N/A: this first editor only changes uncited existing text/duration blocks. It does not add/delete/reorder blocks, edit fact text/citations under a review workflow, offer manual Program/Shot mapping, provide a structured diff/restore action or calculate downstream branch impact before saving. The PostgreSQL journey remains skipped locally without `ASSETGRAPH_TEST_DATABASE_URL`; no strict Phase 1 checkbox is checked. | 2026-07-25 |
| `CHK-4142` preparation update (item remains open) | coderdailyone | commit `6c08876`; the Operations workspace now supports a validated list of operator-entered session metrics rather than one key/value pair. Operators can add/remove rows, and the form blocks empty, non-finite or duplicate metric keys before submitting the complete `metrics: dict[str, float]` contract. Attribution metric selection is now independent of the session-entry state. Focused Operations regression asserts the submitted `watchers` and `orders` payload; TypeScript and all three Vite builds passed. | N/A: this is still manual session-grain entry, not standard metric implementation. It has no platform ingest, MetricDefinitionRevision, DataContract, source batch/checksum, event-time/processing-time, units/currency, dedupe, retention-window, quality-SLO or metric snapshot. Required external/operator input is recorded here rather than blocking implementation: each platform must provide an approved metric dictionary mapping the eight target categories (exposure, dwell/retention, interaction, click, add-to-cart, order, refund, content quality) to source fields, grain, unit/currency, dedupe/window and owner before `CHK-4142` can be closed. No strict Phase 4 checkbox is checked. | 2026-07-25 |
| `CHK-1143`,`CHK-1146`,`CHK-1147`,`CHK-1149` preparation update (items remain open) | coderdailyone | commit `6bb1cc1`; the human Script editor now exposes ordered block authoring: an operator can append a non-fact block, choose its module type, edit content/duration, move blocks up/down, and delete an ordinary block. Saving submits that complete order to the existing revision endpoint, which creates immutable Script/Program/ShotList revisions and preserves the new order through each projection. Blocks carrying fixed FactCitation spans remain text/module locked and cannot be deleted from this first editor. Frontend API regression asserts multi-block ordered submission; TypeScript and all three Vite builds passed. The PostgreSQL journey test now covers append/reorder plus synchronized segment/shot counts. | N/A: there is still no structured FactCitation edit/review flow, no independent ProgramSegment or Shot mapping editor, no manual material-role/composition/audio/continuity editor, no visual diff/restore command or downstream impact preview. `ASSETGRAPH_TEST_DATABASE_URL` is absent here, so the PostgreSQL journey is present but skipped; it must run against a current migrated schema before any strict item is closed. No strict Phase 1 checkbox is checked. | 2026-07-25 |
| `CHK-1146`-`CHK-1149` preparation update (items remain open) | coderdailyone | commit `8192cf3`; `POST /content-projects/{code}/program-shot-revision` now creates a coupled immutable Program/ShotList revision from the current confirmed project, DesignBrief, StoryBrief and Script. Segment inputs carry explicit adopted ScriptBlock codes; every Shot chooses one submitted segment plus source blocks that segment adopted. The service validates all submitted references before creating the Program, rejects mutable runtime locators and invalid branch values, and preserves metadata not edited by the UI. The ContentProject workspace now edits segment goal/phase/duration/block adoption and one-or-more Shot goal/material-role/duration/source mappings per segment. API regression, schema tests, Ruff, TypeScript and all three Vite builds passed; the PostgreSQL journey covers custom grouping and Shot-to-segment/source projection. | N/A: the UI does not yet add/delete/reorder ProgramSegments, edit entry/exit conditions, product/interaction/CTA policies, composition, audio, continuity, acceptance criteria or branch applicability; it intentionally keeps those existing fields unchanged. This is a coupled revision command rather than a generic timeline editor, does not create `MaituSceneBlueprint`/`TimelineSegment` projections, and has no diff/restore or downstream stale-impact preview. `ASSETGRAPH_TEST_DATABASE_URL` remains unavailable, so the persistence journey is skipped locally and must run on a current migrated database before any strict checkbox is closed. | 2026-07-25 |
| `CHK-4122`,`CHK-4124`,`CHK-4162`,`CHK-4164` preparation update (items remain open) | coderdailyone | commit `b566924`; migration `063` adds an append-only correction record for operational ContentExposure. An active observation can now be superseded by a separately stored replacement, with bidirectional exposure links and a reason, or retracted without inventing replacement evidence. The Operations workspace exposes both paths from the active-exposure list. Replacement validation keeps the source session, interval, plan and scene constraints intact while ignoring only the source interval; both actual content timeline and descriptive attribution continue to consume active records only. Schema/API/UI regressions passed (`1` backend schema test; `4` focused frontend tests), Ruff, TypeScript and all three Vite builds passed. A PostgreSQL journey asserts the prior observation becomes `superseded`, the replacement is the sole timeline span and coverage reflects its corrected duration. | N/A: this is an operator-authored correction prototype, not a platform-event reconciliation system. Required external input is recorded rather than treated as a blocker: each platform adapter must supply immutable event IDs, source batch/version/checksum, processing-time, event-time and correction/tombstone semantics before automated served-log or recording corrections can close the strict checklist items. `ASSETGRAPH_TEST_DATABASE_URL` is unavailable in this environment, so the migration `063` PostgreSQL journey is present but skipped locally; it must be run on a current migrated database before any strict checkbox is closed. No strict Phase 4 checkbox is checked. | 2026-07-25 |
| `CHK-4121`,`CHK-4124`,`CHK-4162` preparation update (items remain open) | coderdailyone | commit `94b34bf`; the actual ContentTimelineSpan now resolves through the Exposure's fixed `variant_code`, not through a mutable current ContentProject. Each resolved span returns its ProgramSegment (phase, goal, product refs and CTA actions) and its source ScriptBlocks (module type, product ref, pinned template/revision/module and CTA intent), alongside the existing Shot, scene and layer projections. The Operations timeline displays this content chain compactly and explicitly reports an unresolved projection rather than inferring one. Backend service regression proves template-module/product/CTA projection; frontend API/UI regressions `5 passed`, Ruff, TypeScript and all three Vite builds passed. The PostgreSQL journey asserts the span's ProgramSegment and ScriptBlock codes match the fixed ShotList. | N/A: this is a content-lineage projection only, not per-span metric allocation or a published attribution result. It does not create TimeMapping, metric buckets, numerator/denominator, confidence intervals, result revisions, source snapshot pinning or causal claims. Required platform input remains documented rather than blocking implementation: event-time/processing-time semantics, clock-drift evidence and immutable metric batches are needed before fine-grained measured effects can be implemented and strict Phase 4 items can close. `ASSETGRAPH_TEST_DATABASE_URL` is absent locally, so the PostgreSQL assertion is present but skipped until run on a schema through migration `063`; no strict checkbox is checked. | 2026-07-25 |
| `CHK-1146`,`CHK-1147`,`CHK-1149` preparation update (items remain open) | coderdailyone | commit `7fc6cd1`; the human Program/Shot editor now permits structural ProgramSegment changes. An operator can add a segment with a linked Shot, move a segment up/down while remapping all affected Shot segment indexes, or remove a non-final segment together with its Shots and compact later indexes. The existing validation still requires every remaining segment to adopt a ScriptBlock and retain at least one Shot, and save produces the same coupled immutable Program/ShotList revision command. The page regression adds, edits, reorders and removes a segment, then proves the POST body preserves the new Shot-to-segment mapping; focused frontend tests `11 passed`, backend schema/content tests `3 passed` with `12` PostgreSQL tests skipped without the configured database, TypeScript and all three Vite builds passed. | N/A: controls still leave entry/exit conditions, product/interaction/CTA policies, composition, audio, continuity, acceptance criteria and branch applicability unchanged. There is no visual diff, restore-as-new-revision, downstream stale-impact preview, per-field review or a generic timeline editor. The formatting-only rewrite of the editor is part of this change to make the formerly one-line component maintainable. `ASSETGRAPH_TEST_DATABASE_URL` remains absent, so the existing persistence journey must run against a current migrated database before any strict Phase 1 checkbox is closed. | 2026-07-25 |
| `CHK-1146`,`CHK-1147` preparation update (items remain open) | coderdailyone | commit `3726dcf`; ProgramSegment editing now exposes entry condition, exit condition and de-duplicated product references, while Shot editing exposes must-include/must-avoid requirements and ordered up/down controls. These values were already part of the typed coupled revision contract; the UI now makes them authorable without raw JSON or loss of the remaining segment/shot data. The page regression proves the saved revision carries conditions, two product refs, must-include/must-avoid arrays and the manually reordered Shot sequence. Focused frontend tests `11 passed`, backend schema/content checks `3 passed` with `12` database journeys skipped locally, TypeScript and all three Vite builds passed. | N/A: composition intent, audio actions, continuity, acceptance criteria, interaction actions, CTA actions, branch applicability and metadata still lack structured editors. There is no visual layout/timeline preview, review workflow, diff/restore, downstream stale-impact preview or production projection regeneration from the editor. Database persistence journeys require `ASSETGRAPH_TEST_DATABASE_URL` and a current migrated schema before any strict Phase 1 checkbox can close. | 2026-07-25 |
| `CHK-4143`,`CHK-4144`,`CHK-4147` preparation update (items remain open) | coderdailyone | commit `3438b9b`; the existing immutable `MetricDefinitionRevision` and `DataContract` service is now reachable through versioned `/data-governance` APIs and the Console's `数据治理` workspace. Operators can list current metric definitions and active contracts, inspect every historical revision, compare the current revision's changed fields, view contract-to-metric consumers, and create a new typed metric or contract revision without editing database JSON directly. Metric entry explicitly captures owner, meaning, grain, unit/currency, aggregation, numerator/denominator when ratio, event-time field, timezone/boundary, dimensions, dedupe, refund window, contract refs and quality rules. Contract entry explicitly captures source/schema, identity/time/operation paths, primary keys, upsert/delete semantics, lateness/compatibility, classifications, volume and quality SLO. Backend route tests and catalog service regressions passed (`4 passed`, `4` PostgreSQL cases skipped without configured database); focused Console/governance/Operations tests passed (`10 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: this is a local catalog and governed revision UI, not an actual platform ingestion adapter or completed quality pipeline. Required external/operator input is documented here rather than blocking the implementation: every source platform must provide an approved versioned event/schema dictionary, source event ID and primary-key semantics, event-time/processing-time/timezone meaning, insert/upsert/delete/tombstone/refund/backfill behavior, late-event window, enum/classification mapping, source batch ID/checksum/watermark, expected volume and field owner. Those inputs are required to implement `CHK-4140`-`CHK-4146`, validate contract references against live provider payloads and close the strict Phase 4 items. The PostgreSQL journeys require `ASSETGRAPH_TEST_DATABASE_URL` against a database migrated through `031` or newer; none was configured in this environment. | 2026-07-25 |
| `CHK-1146`,`CHK-1147` preparation update (items remain open) | coderdailyone | commit `55d5e92`; the human Program/Shot revision editor now authorably preserves and changes the remaining production constraints already accepted by its typed API. A ProgramSegment can carry structured interaction actions and CTA actions (`动作 | 补充说明`) plus explicit live-room/rendered-video applicability. A Shot can carry composition style/focus, structured audio actions, acceptance criteria, continuity with the previous shot and transition cue, as well as explicit branch applicability. Saving still creates coupled immutable Program and ShotList revisions, so these fields are not patched into historical production records. The focused page regression edits all of these values while adding/reordering segments and proves the submitted payload retains the program-to-shot mapping and structured action/constraint values. Focused frontend checks passed (`3 passed`), backend content schema checks passed (`3 passed`), TypeScript and all three Vite builds passed. | N/A: action objects are intentionally the minimal local `{action, detail?}` editor projection; there is no action taxonomy registry, material/layout preview, generic metadata editor, visual timeline, field-level diff/restore, review workflow, production projection regeneration or downstream stale-impact browser. PostgreSQL persistence journeys still require `ASSETGRAPH_TEST_DATABASE_URL` and a current migrated schema before strict Phase 1 validation can close. | 2026-07-25 |
| `CHK-1146`,`CHK-1147` preparation update (items remain open) | coderdailyone | commit `27afcd2`; the editable Program/Shot workspace now has a live local timeline preview of the unsaved draft. Segment bars show calculated start/end/phase and planned duration; child shot bars show each segment's ordered shots and duration allocation. A compact status distinguishes a fully aligned draft from pending calibration, reporting every unset shot duration and every segment whose declared duration differs from the sum of its shots. The preview reacts to editor changes before a revision is saved. The focused ContentProject regression checks total duration and the live mismatch indication; TypeScript and all three Vite builds passed. | N/A: this is a Program/Shot planning preview, not the authoritative ProductionTimeline or a visual layout renderer. It lacks OTIO/RationalTime, source media ranges, layer preview, audio/subtitle tracks, persisted preview artifact, review/restore diff and downstream stale-impact graph. No strict Phase 1 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-4142`-`CHK-4144` preparation update (items remain open) | coderdailyone | migration `064` and the Operations workspace now connect manual session metrics to the existing immutable MetricDefinition catalog. An operator may select an active catalog metric revision per metric row; the API resolves and snapshots its code, revision, definition fingerprint, grain, unit/currency, aggregation and time semantics in the OperationSession. Custom metrics remain supported and explicitly unpinned. Descriptive AttributionReport snapshots the definition only when every selected session has the same pin; otherwise its metadata records `metric_unpinned`, `metric_missing_in_session` or `metric_pin_mismatch`, and Console presents the absence instead of inventing a definition. External-session retries return the already imported source row before resolving a now-superseded catalog revision. Backend schema/migration regressions passed (`14 passed`, `2 skipped`); focused Operations UI regressions (`4 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: this is governed manual entry and revision provenance, not platform ingestion or a quality pipeline. No standard event batch, source event ID/checksum, processing time, event-time reconciliation, contract-to-payload validation, dedupe/window calculation, quality-SLO evaluation, event-level metric computation, time allocation, causal method or published AttributionResult exists. `ASSETGRAPH_TEST_DATABASE_URL` is absent here, so the real PostgreSQL journey for migration `064` is present but skipped; run migrations through `064` and that journey before any strict Phase 4 item is checked. Each platform must still supply the approved event/schema dictionary, field mapping for every target metric, identity/upsert/delete/refund/backfill semantics, time/boundary rules, batch watermark/checksum and ownership. | 2026-07-25 |
| `CHK-4125`,`CHK-4126`,`CHK-4127` preparation update (items remain open) | coderdailyone | migration `065` introduces append-only `TimeMapping` revisions for an OperationSession. Each revision fixes source-clock name/type, source offset, drift ppm, a half-open session-millisecond coverage interval, evidence note and actor; coverage outside the session and stale expected revisions are rejected, while the preceding active mapping becomes `superseded`. The actual content timeline projects each active exposure as `aligned`, `partially_aligned`, `outside_coverage` or `unmapped` and exposes mapped source-clock milliseconds plus quantified calibrated coverage. The Operations timeline now supports manual/recording/platform anchor calibration, preserves the revision history and never rewrites exposure evidence. Backend schema/migration regressions passed (`15 passed`, `3 skipped`); focused Operations API/UI regressions (`7 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: this is a bounded manual alignment prototype, not the complete TimeMapping contract. It does not map OTIO RationalTime or media PTS, fit clock drift from multiple automatic anchors, persist ContentTimelineSpan revisions, model gaps/discontinuities, enforce an evidence-quality threshold or allocate metric buckets; unaligned/partial spans therefore remain descriptive-only. `ASSETGRAPH_TEST_DATABASE_URL` is absent, so the PostgreSQL journey for migration `065` is present but skipped; apply migrations through `065` and run it before strict Phase 4 claims. Required platform/operator input is recorded rather than blocking work: exact clock semantics/epoch, recording PTS anchors, platform event-time/processing-time fields, allowed drift and alignment acceptance thresholds. | 2026-07-25 |
| `CHK-4160`,`CHK-4164`,`CHK-4167` preparation update (items remain open) | coderdailyone | the Operations attribution workspace now makes the report population explicit instead of silently submitting every registered session. It initializes to the loaded sessions, lets an operator include/exclude individual sessions, shows the selected metric value per session, warns when selected sessions lack that metric, and submits only the selected stable session codes. Report cards now surface the persisted metric-definition alignment state as well as the existing descriptive evidence label and observed-content coverage. Focused Operations regressions (`8 passed`), TypeScript and all three Vite builds passed. | N/A: this is a report-input and evidence-qualification UI improvement over the existing descriptive report, not a versioned AttributionRun workflow. It has no snapshot selector, review/publish/failed/insufficient_data state machine, metric buckets, per-span allocation, statistical interval, comparison/diff workflow, backfill revision, causal method, published result gate or operator approval. Strict Phase 4 items remain open until platform metric snapshots, exposure/time-alignment thresholds and result evidence are implemented and validated. | 2026-07-25 |
| `CHK-5120`,`CHK-5122`,`CHK-5146` preparation update (items remain open) | coderdailyone | the Learning workspace now exposes the existing optional `DecisionLog.attribution_report_code` field. An operator can select a real descriptive attribution report while recording an observation and reproduction recommendation; the exact report code is persisted and displayed with the decision. The focused regression proves the selected report is sent in the typed decision request; TypeScript and Console build passed. | N/A: this is a manual provenance link, not a point-in-time FeatureSnapshot or an EffectEligibilityPolicy. It does not verify report publication/quality, preserve a report revision/fingerprint, compute an estimate, expose evidence tier/interval/drift, create a new ContentProject/Variant revision, automatically change selection or run an experiment. Strict Phase 5 items remain open. | 2026-07-25 |
| `CHK-5120`,`CHK-5122` preparation hardening (items remain open) | coderdailyone | `FunctionalLearningService.create_decision` now verifies that an optional `attribution_report_code` resolves to a persisted AttributionReport before creating the DecisionLog; absent reports return stable `LEARNING_ATTRIBUTION_REPORT_NOT_FOUND` through the API rather than creating a dangling link. The PostgreSQL journey covers both the valid persisted reference and rejected missing report; Ruff passed. | N/A: report existence is the only backend integrity guarantee at this stage. It does not require a report to be published, validate its input snapshots/alignment/quality, pin a report revision/fingerprint, enforce evidence eligibility or create an effect/reproduction artifact. `ASSETGRAPH_TEST_DATABASE_URL` is absent locally, so the PostgreSQL journey is skipped and must run on a migrated database before strict Phase 5 verification. | 2026-07-25 |
| `CHK-5120`,`CHK-5122`,`CHK-5140`-`CHK-5146`,`CHK-5185`,`CHK-5188` preparation update (items remain open) | coderdailyone | migration `066` adds a functional effect-evidence record. Creating an effect requires a persisted descriptive AttributionReport and, for a ContentProject subject, freezes that project's current immutable revision, content, prior source refs and fingerprint into the effect payload. The candidate stores the report groups/metadata, metric-definition pin state, explicit descriptive-only qualification, operator context/note and a canonical fingerprint; an operator can approve it through the typed API and Learning workspace. Only an approved ContentProject effect can create a fresh draft: the new project copies the frozen content rather than rereading the mutable source and appends both the frozen source revision and approved effect fingerprint to `source_revision_refs`. The Learning UI selects real reports/projects, creates candidates, confirms them and creates the new draft. Backend migration/reproducibility checks passed (`11 passed`, PostgreSQL journey `1 skipped` without `ASSETGRAPH_TEST_DATABASE_URL`); focused Learning UI checks passed (`2 passed`), Ruff, TypeScript and Console build passed. | N/A: this is a minimal descriptive manual workflow, not the planned PerformanceProfile/AssociationalEstimate/CausalEstimate model. It has no point-in-time FeatureSnapshot, metric-window/interval/drift calculation, eligibility policy, automatic ranking, revoke/supersede/expiry propagation, DecisionLog linkage on the new draft, Variant reproduction, experiment assignment or automated release. `EXT-P5-OBSERVATION-DATA` is recorded without blocking further implementation: each source owner must later provide versioned immutable metric snapshots, event/processing-time semantics, exposure reconciliation identifiers, deletion/refund/backfill behavior, minimum sample/alignment/freshness thresholds and approved decision-use rules before associational or causal eligibility can be implemented. No strict Phase 5 checkbox is checked. | 2026-07-25 |
| `CHK-7101`,`CHK-7105`,`CHK-7122`-`CHK-7124` preparation update (items remain open) | coderdailyone | ProductFactCard now has a typed exact-version usage API and a Knowledge workspace projection. The traversal begins only from `content.fact_card_refs` that pin the selected card and version, then follows immutable ContentProject revision IDs through StoryBrief, Script, Program, ShotList and exact carrier variants; linked functional video/live-room plans and effect estimates are shown only when their fixed source chain or frozen effect snapshot contains that same card version. The page displays object type, relation, code/revision, status and creation time. Contract and route regressions passed (`10 passed`); focused Knowledge UI regression passed (`1 passed`), Ruff, TypeScript and Console build passed. The PostgreSQL scenario proves that a generated chain is returned for v1 while a later v2 card revision returns no false historical usage; it is present but skipped without `ASSETGRAPH_TEST_DATABASE_URL`. | N/A: this is a direct relational lineage projection, not the planned FactClaim/SourceEvidence model or a graph database. It does not store field-level citations/effective windows as graph edges, index all releases/sessions/metrics, support lexical/vector retrieval, rebuild via outbox/watermark, reconcile projection counts or decide authorization from search hits. `EXT-P7-KNOWLEDGE-SOURCES` is recorded without blocking local work: source owners must later provide approved documents/URLs, extraction policy, retention/access classification, quoted spans, validity/platform scope and evidence-review ownership before SourceEvidence and FactClaim completion. No strict Phase 7 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3108`,`CHK-3110`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `655b56d`; the constrained rendered-video timeline now lets an operator edit each fixed video clip's source in/out seconds as well as order, duration and transition. The Console displays the immutable available interval, submits complete source ranges with the optimistic timeline revision, and disables edits after the Worker claims the job. The service rejects incomplete, reversed and out-of-bound ranges, preserves the original fixed availability boundary in the next timeline revision, and synchronizes the selected range to the Worker-facing ShotList in the same transaction. Backend range regressions, focused Console regression, Ruff, TypeScript and all frontend builds passed; the full backend suite passed (`584 passed`, `115 skipped`). | N/A: this is source trimming on fixed video clips, not generic media editing. It still uses millisecond timeline values rather than OTIO `RationalTime/TimeRange`, has no source PTS/handle detection, playback-rate/crop/transform controls, audio/subtitle track editing, formal visual diff or RenderManifest integration. `EXT-P3-RENDITION-METADATA` is recorded without blocking local implementation: before the fixed boundary can be derived rather than declared, asset ingestion must provide the approved rendition checksum, media duration, frame rate/timebase, PTS start/end and probe evidence for every selectable source. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3103`,`CHK-3110`,`CHK-3122`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `01259d9`; a render plan whose Worker has started, completed or failed now exposes `创建可编辑分支`. The command creates a new `ProductionVariant`, queued video job, timeline revision and Worker ShotList from the original plan's exact confirmed ContentProject/StoryBrief/Script/ShotList source chain, current constrained timeline and render profile; source ranges, ordering, durations and transitions are retained, while the source job is never modified. The Console immediately selects the new queued plan. Route, frontend interaction and PostgreSQL journey regressions were added; Ruff, focused UI tests, TypeScript, all frontend builds and the full backend suite (`585 passed`, `115 skipped`) passed. | N/A: this is a constrained branch copy, not a general revision graph. Branching currently requires the original chain's source revisions to remain confirmed because ProductionVariant creation rejects superseded sources; it has no branch comparison/merge, source-plan graph UI, selective stage/artifact reuse, branch deletion/archive or post-claim branch policy. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3160`,`CHK-3161`,`CHK-3183`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `024e7fb`; migration `067` adds immutable rendered-video release snapshots and links a functional video plan to its ReleaseManifest. A plan can create a candidate only after its Worker is `succeeded`, its QC report explicitly passes and a checksummed video artifact exists. The candidate freezes exact content/variant revisions, current timeline revision and fingerprint, render profile, video artifact metadata and QC report in a content-addressed local snapshot, then creates a signed generic `rendered_video` ReleaseManifest. The Console shows explicit candidate eligibility/status and links the existing release workspace; it never approves, uploads or delivers. Route/static-contract/frontend interaction/PostgreSQL journey regressions were added; Ruff, all frontend builds and the full backend suite passed (`587 passed`, `116 skipped`). | N/A: the candidate snapshot currently references the frozen local summary rather than registering each media artifact as a first-class ArtifactRef ingredient. It has no approval command, C2PA/IPTC sidecars, rights validation, waiver, delivery adapter, channel read-back or revoke propagation. `EXT-P3-VIDEO-DELIVERY-AUTHORIZATION` is recorded without blocking local implementation: every target channel must later provide an approved account/principal, upload/delivery authorization, API capability and scope, object identity/checksum read-back contract, rate/error/idempotency semantics, rights declaration requirements and revoke/deletion procedure before release approval or delivery can be implemented. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2300`,`CHK-2302`,`CHK-2303` preparation update (items remain open) | coderdailyone | commit `9fd6f99`; the live-room configuration form now loads the selected ContentProject's pinned template contribution decisions and displays the immutable primary template, all secondary templates, revisions and adopted modules before BuildPlan generation. Its request explicitly repeats exactly those inherited primary/secondary codes, and the existing server-side mismatch rule remains the authority that rejects any downstream substitution. The focused live-room Console regression verifies the rendered primary/secondary revisions and request payload; TypeScript and all three Vite builds passed. | N/A: templates are intentionally not editable in the live-room form: selection and module decisions remain ContentProject revision data. This does not add semantic recommendation, template retrieval, source similarity/copyright review, production-template re-ranking, or a full decision-diff workflow. No strict Phase 2 checkbox is checked. | 2026-07-25 |
| `CHK-2147`,`CHK-2148` preparation update (items remain open) | coderdailyone | commit `ba6027a`; the live-room form now supports an opt-in, plan-local normalized geometry and optional z-order override for every selected loose/group/pack material. Each override requires an operator reason, is constrained to the normalized canvas and selected asset set, records actor plus the exact base constraint-profile reference, and is frozen in the inventory/constraint snapshots, ProductionVariant/room configuration, Blueprint evidence, BuildPlan and clone inputs. The compiler starts from that room-private placement but then applies the asset Profile's hard rules, including named regions, table surfaces and locked top/bottom layer rules, so the override cannot relax global safety/placement constraints. The plan detail displays the resulting room-private decision. Schema, compiler and API/UI regression tests passed; the full backend suite passed (`591 passed`, `116 skipped`) and all three Vite builds passed. | N/A: this is an immutable plan-local placement prototype, not a general constraint-override system. It has no per-rule `constraint_key` replacement, polygon/visual editor, solver/minimal-conflict diagnosis, room override revision history, review/approval workflow, or the deliberate “promote to a new global Profile revision” confirmation flow. The PostgreSQL persistence journey must still run with `ASSETGRAPH_TEST_DATABASE_URL` before any strict Phase 2 item is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3107`,`CHK-3108`,`CHK-3110`,`CHK-3124`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `ee3c94c`; every newly compiled functional rendered-video timeline now owns a one-to-one subtitle track bound to its fixed Shot codes. The queued-plan editor exposes bounded caption and headline text for each shot alongside the existing order/duration/transition/source-range controls, submits them in the same optimistic revision transaction, and disables the command when a caption is blank. The service rejects missing, duplicate or mismatched subtitle clips; reflows their time ranges whenever video order/duration changes; synchronizes the chosen text into the Worker ShotList; and restores subtitle text together with historical video revisions. The deterministic ASS builder consumes `subtitle_text` and the headline projection, so the edit changes rendered subtitle output rather than only an API document. Focused subtitle/timeline/Worker tests (`13 passed`), Console regression (`4 passed`), Ruff, TypeScript, all frontend builds and full backend suite (`593 passed`, `116 skipped`) passed. | N/A: this is a constrained fixed-shot subtitle surface, not a general NLE or complete ProductionTimeline. It lacks RationalTime/TimeRange and PTS mapping, arbitrary caption/track insertion, word timing, caption style/safe-area controls, audio gain/BGM/SFX editing, overlays/digital-human controls, transform/crop/playback controls, formal source/artifact lineage diff, RenderManifest integration and stale-impact graph. The PostgreSQL restore journey was extended but is skipped locally until `ASSETGRAPH_TEST_DATABASE_URL` points to a migrated database; run it before any strict Phase 3 item is checked. | 2026-07-25 |
| `CHK-3121`,`CHK-3125`,`CHK-3142`,`CHK-3147`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `678d340`; migration `068` adds a distinct `render_manifest` video artifact so the pre-existing download route now exposes both the sanitized subprocess command log and the manifest. The rendering stage creates `render-manifest.v1` only after final video, poster and command log exist; it freezes fingerprints for the Worker ShotList, selected asset plan, voice manifest and subtitle manifest, input/output checksums, output-relative paths, encoding summary and the command-log checksum, then fingerprints the manifest itself. Machine-local roots are deliberately absent from this identity. Artifact-schema, manifest integrity, migration parsing and reproducibility discovery tests passed (`36 passed`), and the full backend suite passed (`594 passed`, `116 skipped`). | N/A: this is a local FFmpeg input/output manifest, not the complete RenderManifest contract. It has no OTIO RationalTime/PTS mapping, captured FFmpeg/ffprobe/font build versions, render seed/GPU/CPU/cost capture, structured filter-graph model, first-class ArtifactRef ingredients, deterministic cross-host replay comparison or external tool-difference report. `ASSETGRAPH_TEST_DATABASE_URL` is not set here, so migration `068` must be applied with the PostgreSQL artifact registration journey before a strict Phase 3 item can be checked. | 2026-07-25 |
| `CHK-3142`,`CHK-3147` preparation update (items remain open) | coderdailyone | commit `cbc4e3f`; the local render pipeline now probes and caches the actual `ffmpeg` and `ffprobe` version identifiers once per Worker pipeline instance, then embeds them in every `render-manifest.v1`. Version lookup fails the render with a stable code rather than silently omitting reproducibility identity. The pure manifest and cache regressions passed (`10 passed`), and the full backend suite passed (`595 passed`, `116 skipped`). | N/A: tool version identity is only one RenderManifest ingredient. Font-file resolution/version, FFmpeg filter-graph structure, asset rendition PTS/timebase, TTS runtime version, seed, CPU/GPU/memory/cost usage and same-manifest cross-host output comparison remain unimplemented. The worker requires local FFmpeg/FFprobe, which is already a documented local deployment prerequisite rather than an external API dependency. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3143`-`CHK-3145`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `fbd72b2`; `/production/videos` now preserves and exposes the existing QC report details instead of reducing it to booleans. Each rendered-video plan shows probed duration, resolution, video/audio codecs and sample rate, integrated loudness/true peak/LRA, plus every detected black-frame, silence and freeze interval alongside the blocking rule list. The UI test asserts the typed quality diagnostics; TypeScript and all three Vite builds passed. | N/A: this is read-only QC observability. It does not add new detection algorithms, per-frame thumbnail evidence, subtitle safe-area visual inspection, required ScriptBlock/rights checks, waiver authorization, policy-versioning, automated release approval or artifact access control. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3101`,`CHK-3103`,`CHK-3180` preparation update (items remain open) | coderdailyone | commit `aa3a339`; `/production/videos` can now choose either a confirmed ContentProject or a confirmed live-room plan as its source. When a live-room plan is selected, the service joins its confirmed ProductionVariant and the exact confirmed ContentProject/StoryBrief/Script/ShotList revisions, then creates an isolated rendered-video Variant, queued render job and render profile that record `source_live_room_plan_code`. The client enforces exactly one source before issuing the typed create request. Schema/route/timeline regressions passed (`11 passed`, `3 skipped`); full backend suite passed (`597 passed`, `117 skipped`); focused Console test (`5 passed`), TypeScript and Vite build passed. | N/A: this fixes only the frozen content-source boundary. It does not make a live-room plan's material snapshot or room-private overrides a rendered-video material selection, model OTIO/RationalTime, provide all video tracks, or complete cross-branch release/isolation acceptance. The PostgreSQL end-to-end journey is present but skipped until `ASSETGRAPH_TEST_DATABASE_URL` is configured with a migrated test database; this prerequisite is recorded here and does not block further local implementation. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3107`,`CHK-3110`,`CHK-3121`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `189b5e9`; the constrained rendered-video timeline now exposes a bounded `-24 dB` to `+12 dB` gain slider for every fixed voice clip. A save submits the complete voice set with the optimistic timeline revision; the service rejects incomplete/mismatched sets and out-of-range gains, preserves voice-to-Shot links through reordering, branches and historical restoration, and projects the chosen gain into the Worker ShotList. Voice synthesis applies the dB value through a structured FFmpeg filter and freezes it in the voice manifest, therefore the edit affects rendered output and RenderManifest input identity. Audio/timeline/media regressions passed (`31 passed`); focused Console regression (`5 passed`); full backend suite passed (`600 passed`, `117 skipped`); TypeScript and all three Vite builds passed. | N/A: this is per-voice gain only. It has no BGM or sound-effect selection, independent overlay/digital-human tracks, fades/crossfades, ducking, pan, mix bus, loudness review override, rights-aware audio asset resolution or arbitrary audio clip insertion. `EXT-P3-AUDIO-ASSETS` is recorded without blocking local work: approved local BGM/SFX renditions need checksum, duration/timebase, rights scope and a stable material binding before those tracks can be selected and rendered. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-4160`,`CHK-4162`,`CHK-4163`,`CHK-4164`,`CHK-4167` preparation update (items remain open) | coderdailyone | commit `8718532`; every newly created descriptive AttributionReport now includes a deterministic scene allocation derived only from active ContentExposure durations within each selected OperationSession. The result freezes per-scene estimated metric value, observed duration, source sessions, source kinds, release refs and duration-weighted confidence, then the Operations workspace shows the ranked scene list under an explicit “observed-duration proportional estimate” label. The allocator omits sessions with no observed duration and never treats the estimate as a measured scene metric or causal result. Pure allocation regressions passed (`2 passed`); Operations schema/route regressions (`6 passed`, `3 skipped`); Operations UI/API checks (`9 passed`); full backend suite passed (`602 passed`, `117 skipped`); TypeScript and all three Vite builds passed. | N/A: allocation is a transparent descriptive convenience over a session-grain manual metric, not an AttributionRun/AttributionResult state machine. It has no immutable raw metric batch/snapshot, event-time bucket attribution, TimeMapping quality threshold, numerator/denominator/statistical interval, backfill/supersede result revision, review/publish state, causal language, or automatic LearningPolicy input. `EXT-P4-EVENT-GRAIN-METRICS` is recorded without blocking local work: source owners must later supply event/bucket metric IDs, event and processing time, source watermark/checksum, delete/refund/backfill semantics, release/exposure reconciliation keys, grain and allocation rules before measured span-level attribution can replace this proportional estimate. No strict Phase 4 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3108`,`CHK-3141`,`CHK-3292` preparation update (items remain open) | coderdailyone | commit `2785c2b`; rendered-video source in/out edits now constrain the actual FFmpeg source window rather than only the persisted timeline. The renderer seeks to the fixed in point, trims to the exact selected out point, and when a longer visual duration is required loops only the selected frame window before its final trim. Invalid non-positive source windows fail with a stable error; the prior full-file `stream_loop` path is removed. Command-level regression proves a `2s-5s` choice produces a 3-second bounded filter window and loops only its 90 frames; media/timeline/preset regressions passed (`27 passed`) and the full backend suite passed (`604 passed`, `117 skipped`). | N/A: this makes second-based source range execution match the constrained editor, but still does not use OTIO RationalTime/TimeRange, source PTS/timebase/handles, frame-accurate VFR mapping, visual diff, source-rendition identity or cross-host RenderManifest replay. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3110`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `999dc66`; every newly compiled video clip now records its visual `fit`, and the constrained timeline editor lets an operator choose `cover` or `contain` for each queued shot. The typed edit is revisioned with the clip, survives ordering/restoration/branching, projects to the Worker ShotList and drives the existing FFmpeg cover/contain filter path. Older timelines with no fit remain readable and preserve their prior worker setting until explicitly changed. Backend timeline/schema regressions (`26 passed`), focused Console test (`5 passed`), full backend suite (`604 passed`, `117 skipped`), TypeScript and all three Vite builds passed. | N/A: visual fit is only a binary aspect strategy. It has no normalized crop/position/scale/rotation/keyframes, transform track, per-layer overlays, speed ramp, source PTS mapping or playback preview. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3110`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `1781b61`; each fixed rendered-video clip can now persist a normalized `crop_x/crop_y` focus pair in `[0,1]` when its fit is `cover`. The queued-plan editor exposes bounded horizontal and vertical focus controls, timeline validation rejects incomplete, out-of-range or `contain` crop input, and switching to `contain` clears obsolete crop focus. The coordinates survive timeline revisions, restore and branching through the complete video-clip update projection, enter the Worker ShotList, and become deterministic FFmpeg `crop` expressions; a command-level regression asserts the exact `0.250/0.750` filter parameters. Focused backend regressions (`21 passed`), focused Console regression (`5 passed`), Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`605 passed`, `117 skipped`). | N/A: this is only an anchor point for a fixed cover crop. It has no crop rectangle/scale/rotation, keyframes or animation, per-layer transforms, source-coordinate preview, safe-area/face/object tracking, VFR/PTS mapping, visual-diff approval or general NLE controls. The PostgreSQL timeline journey remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` names a migrated database; this is recorded as a verification prerequisite and does not block local implementation. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3108`,`CHK-3110`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `b7dad10`; fixed rendered-video clips now have an optional bounded visual `playback_rate` from `0.5x` to `2x`. The queued timeline editor exposes the allowed speeds, persists them with optimistic revision control, preserves them across restore/branching, projects them into the Worker ShotList and records the resolved rate in the selected-asset plan. FFmpeg now loops only the selected source window for `timeline duration * playback rate`, applies the deterministic PTS speed transform, then trims back to the unchanged timeline duration. Regressions cover normal/slow/fast rate filters and invalid rate rejection. Focused backend regressions (`23 passed`), focused Console regression (`5 passed`), Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`607 passed`, `117 skipped`). | N/A: this is video-only constant speed for fixed clips. It has no audio time-stretch or pitch policy, variable/speed-ramp curve, source PTS/timebase mapping, media handles, transition-handle calculation, reverse playback, retime preview or general NLE timing model. The PostgreSQL timeline journey remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` names a migrated database; this is recorded as a verification prerequisite and does not block local implementation. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3107`,`CHK-3110`,`CHK-3124`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `d7b22a0`; each fixed subtitle clip now has a revisioned `caption_position` of `bottom` or `center`. The queued-plan editor selects it per Shot, the service validates/normalizes legacy values and preserves it through reordering, timeline restoration and branches, and the Worker projection selects an actual ASS `Caption` or `CaptionCenter` style. The subtitle manifest freezes each event's placement and position counts, so the selected placement changes the rendered subtitle artifact rather than only API state. Subtitle/timeline/Worker regressions (`20 passed`), focused Console regression (`5 passed`), Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`608 passed`, `117 skipped`). | N/A: this is a constrained placement choice, not a subtitle authoring system. It has no font/color/size controls, per-word timing or karaoke, collision detection with product/logo/face regions, safe-area visual analysis, multilingual shaping/fallback policy, arbitrary subtitle tracks or preview editor. The PostgreSQL timeline journey remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` names a migrated database; this is recorded as a verification prerequisite and does not block local implementation. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3107`,`CHK-3110`,`CHK-3121`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `31ff27c`; a fixed rendered-video clip can now opt into the already approved baseline `product_sticker` through a timeline checkbox. The value is revisioned, preserved through restore/branching, projects to the Worker ShotList and selected-asset plan, and FFmpeg conditionally opens only the fixed sticker asset as a second image input before overlaying it at a deterministic canvas position. Legacy timelines without overlay state remain untouched until an operator changes the checkbox. Timeline/media regressions (`26 passed`) prove the selected overlay role and exact FFmpeg input/filter graph; focused Console regression (`5 passed`), Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`608 passed`, `117 skipped`). | N/A: this is one existing product-sticker overlay, not a general layer editor. It has no arbitrary asset choice, per-layer transform/keyframes, z-order composer, opacity/mask/blend modes, digital-human overlays, collision avoidance, preview canvas, material-rights resolver or multi-track overlay timeline. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3141`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `5d8be75`; the existing fixed transition enum now has real rendering behavior for every exposed value: `cut` is direct, `fade_out` fades the final interval to black, and `fade` applies bounded fade-in plus fade-out inside the selected clip. The fade duration is capped at 0.3 seconds and automatically shortened for short clips, preserving the fixed timeline duration. The Console labels the semantics as direct cut, fade in/out and fade out rather than exposing opaque enum values. Command-level media regression asserts the exact fade filters; focused backend regressions (`23 passed`), focused Console regression (`5 passed`), Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`608 passed`, `117 skipped`). | N/A: `fade` is a deterministic fade-through-black within an individual clip, not a cross-dissolve. It has no overlap/handle model, transition duration editor, audio crossfade, wipe/mask transition, motion-aware transition, preview graph or arbitrary transition plugins. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3180`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `e0da8af`; the constrained video-timeline editor now changes from its desktop control grid to a three-column flow below 1500px, with clip identity/actions retained as a stable header and crop/source-range controls spanning a full row. This prevents duration, transition, fit, speed, sticker and crop fields from being squeezed into the narrower main pane created by the Console's persistent desktop navigation. Existing mobile rules continue to collapse the controls further. Focused Console timeline regression (`5 passed`), TypeScript and all three Vite builds passed. | N/A: this is a responsive layout hardening only; it does not provide visual playback/canvas preview, keyboard NLE navigation, drag timeline, or cross-browser/device screenshot acceptance. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3106`,`CHK-3110`,`CHK-3125`,`CHK-3141`,`CHK-3142`,`CHK-3181`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `cbdae65`; a queued rendered-video plan now has one revisioned millisecond `poster_time_ms` selected through the timeline editor. The service rejects a frame outside the final timeline, preserves it through history restore and branch projection, and propagates its normalized seconds into the Worker ShotList. Rendering passes that exact value to the structured FFmpeg poster command; the poster artifact and `render-manifest.v1` record the selected timestamp, making the image, revision and reproducibility evidence agree. New plans default to a valid two-second frame (or the final valid millisecond for a shorter timeline). Timeline, render-manifest and FFmpeg-command regressions passed (`19 passed`); focused Console interaction passed (`5 passed`); Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`610 passed`, `117 skipped`). | N/A: this is a single fixed poster timestamp, not thumbnail/contact-sheet generation, a visual frame picker, scene scoring, per-channel poster cropping, approval workflow, artifact access policy or visual-diff review. Existing millisecond timeline semantics remain a prototype rather than the required OTIO RationalTime/PTS mapping. The PostgreSQL journey remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` names a migrated database; this verification prerequisite is recorded without blocking further local implementation. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3184` preparation update (item remains open) | coderdailyone | commit `7fee7ef`; `/production/videos` now renders the already registered poster artifact directly in the Render Preview section. It works when only the poster is available and presents video and poster together when both artifacts exist, so an operator can inspect the selected poster frame without navigating to a raw artifact URL. The focused Console regression asserts the image URL and accessible name; TypeScript passed. | N/A: this is a local artifact preview only. It does not introduce authorization checks beyond the existing download URL, poster approval, visual-diff/QC evidence, contact sheets, responsive image rendition generation, or a frame-selection canvas. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2201`,`CHK-2203`,`CHK-3106`,`CHK-3108`,`CHK-3110`,`CHK-3121`,`CHK-3181` preparation update (items remain open) | coderdailyone | commit `17fdb38`; `/production/videos` now lists optional material-library visual sources and accepts at most six explicitly selected local videos when creating either content-project or live-room derived plans. The service accepts only existing `VID` assets classified as `media_kind=video`, `execution_capability=local_only`, with a safe relative path and SHA-256 checksum. It freezes those refs in the Variant material snapshot, render profile and Worker ShotList, while every generated shot resolves deterministically from the selected pool. The Worker constrains resolution below the configured materials root, rehashes every selected source before rendering, and fails closed on path conflict or checksum mismatch. Media selector regressions (`21 passed`), route contract regressions and Console interaction (`5 passed`) passed; Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`612 passed`, `118 skipped`). | N/A: this is an explicit local-video pool, not the full material-package/group resolver or ranked selection system. It does not expand group/pack revisions, carry per-asset constraint profiles/rights into the rendered-video plan, select image/BGM/SFX/digital-human assets, preflight source duration/PTS/timebase, derive source windows from renditions, or offer a visual candidate preview. `EXT-P3-RENDITION-METADATA` remains required before strict source-duration/PTS reproducibility can be completed: each selectable rendition needs immutable duration, frame rate/timebase, PTS start/end, probe evidence and approved availability semantics. The PostgreSQL creation journey is present but skipped until `ASSETGRAPH_TEST_DATABASE_URL` names a migrated database. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2205`,`CHK-3106`,`CHK-3121`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `b1d0ce6`; the rendered-video plan detail now exposes its frozen local visual asset codes and checksum prefixes directly beside the execution summary. These values are written when the plan is created from the validated material snapshot, then copied with the render profile on a branch; the Console does not re-read mutable library rows to describe the selected evidence. The focused Console regression asserts the visible immutable identity; backend media/route regressions and TypeScript passed. | N/A: this is a compact frozen-identity readout, not a full material selection explanation. It does not display title/path/rendition probe evidence, pack/group expansion, constraint/rights decisions, exclusion scores, per-shot source mapping, stale state, artifact-level ACL or an approval view. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2200`,`CHK-2201`,`CHK-2202`,`CHK-2203`,`CHK-2205`,`CHK-3106`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `19d05f3`; `/production/videos` now keeps loose local-video assets, AssetGroup selections, and published MaterialPack selections as separate inputs. The API validates each list, expands groups and published packs once at plan creation, de-duplicates the resolved pool with a six-source ceiling, rejects empty/missing groups, unpublished/invalid packs, and non-renderable members, then freezes direct source codes, expanded group members, pack code/revision/fingerprint/entries, per-asset source reasons, checksum, relative path, Variant material snapshot, render profile and Worker ShotList. The Console only offers groups and published packs whose current members are all eligible local videos, enforces the same unique-source ceiling before submit, and shows frozen group/package evidence on the plan. Route/Schema, media/timeline, Console interaction and full backend regressions passed (`614 passed, 119 skipped`); Ruff, TypeScript and all three Vite builds passed. The PostgreSQL expansion journey is implemented but skipped locally because `ASSETGRAPH_TEST_DATABASE_URL` is not configured; this is recorded as a test-environment prerequisite and does not block local feature work. | N/A: this is a constrained rendered-video material resolver, not the complete Phase 2 selection engine. It lacks rights grants, per-branch constraint overrides/solver evidence, role occurrence or exclusivity evaluation, ranking/score explanations, non-video material tracks and general pack semantics. `EXT-P3-RENDITION-METADATA` remains a non-blocking external-input requirement for strict reproducibility: ingestion must provide approved rendition checksum, duration, frame rate/timebase, PTS bounds and probe evidence for every selectable source. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2201`,`CHK-2202`,`CHK-2203`,`CHK-2205`,`CHK-3106`,`CHK-3107`,`CHK-3108`,`CHK-3110`,`CHK-3121`,`CHK-3141`,`CHK-3142`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `e1989f1`; rendered-video creation now offers one optional local material-library BGM with a bounded `-36 dB` to `-6 dB` gain. The service accepts only a checksummed, safe-relative-path `media_kind=audio`, `material_role=background_music`, `execution_capability=local_only` asset; it freezes the source in the Worker ShotList, Variant material snapshot, render profile, timeline `BGM-01` track and later release-rights input list. Timeline duration edits reflow `BGM-01` to the final range. The Worker rehashes and probes the audio source, rejects missing/changed/non-audio/invalid-duration inputs, loops and trims the source to the final timeline, applies the fixed gain, mixes it with narration, and records the BGM in the asset plan, render result and content-addressed RenderManifest inputs. Console selects the BGM/gain and displays the frozen checksum/gain. Route/Schema, timeline, media-command, Console interaction and full backend regressions passed (`619 passed, 119 skipped`); Ruff, TypeScript and all three Vite builds passed. PostgreSQL plan-creation coverage is present but skipped locally because `ASSETGRAPH_TEST_DATABASE_URL` is not configured; this remains non-blocking. | N/A: this is one fixed, local BGM lane rather than an audio editor. It has no SFX, multiple/music crossfades, ducking, pan, automation, arbitrary BGM timing, audio preview, rights grant/approval or complete audio quality review. `EXT-P3-AUDIO-ASSETS` remains a non-blocking strict-acceptance prerequisite: audio ingestion must provide an approved rendition checksum, duration/timebase/PTS, rights scope, stable binding and probe evidence before a source can satisfy full reproducibility and release gates. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2205`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `2088657`; the frozen-material evidence panel now renders whenever either visual selections or a BGM exists, rather than using local video selection as an accidental prerequisite. A BGM-only plan therefore exposes its immutable asset code, checksum prefix and gain, and the input-state notice now distinguishes an actual frozen material input from an all-baseline visual plan. Focused Console regression (`6 passed`) and TypeScript passed. | N/A: this corrects immutable-input visibility only. It does not add complete selection explanations, Shot-to-asset/source-artifact graph navigation, rights evidence, stale-impact analysis, ACLs or review approval. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-3121`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `8a18550`; `/production/videos` now derives a read-only `固定镜头输入` projection from the same frozen timeline revision and render profile consumed by the Worker. Each Shot shows its timeline range, visual asset and selected source window, transition, subtitle headline/text and linked narration gain; the independent `BGM-01` row shows the immutable music asset checksum prefix and gain. The responsive projection has no write path or mutable library lookup, so it cannot drift from the selected timeline input. Focused Console regressions (`6 passed`) and TypeScript plus all three Vite builds passed. | N/A: this is a compact timeline-level trace, not the complete required Shot/ScriptBlock/source-artifact graph. It has no explicit ScriptBlock/FactCard IDs, source rendition/probe or rights evidence, per-stage content-addressed artifact links, stale-impact computation, graph navigation, review/approval status or access control. No strict Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `0554e3b`; rendered-video creation now offers one optional `media_kind=image`, `material_role=product_display`, `execution_capability=local_only` product sticker. The service rejects missing, unclassified, non-local, unsafe-path or unchecked images; it freezes the exact asset code, relative path and checksum in the Variant material snapshot and Worker ShotList, includes the code in later release-rights input, and projects immutable identity into the render profile, input trace and plan detail. The existing per-Shot `商品贴片` switch now renders that selected image through the actual FFmpeg overlay input, while a plan without selection retains the fixed baseline sticker. The Worker resolves only beneath its materials root and rehashes the selected image before producing the asset plan; changed files fail closed. This also corrects a pre-existing per-Shot playback-rate projection error by reading each Shot's own rate. Targeted backend regressions (`29 passed`), focused Console regression (`6 passed`), Ruff, full backend suite (`621 passed, 119 skipped`), TypeScript and all three Vite builds passed. | N/A: this is one fixed product-image overlay and no proof that a selected image satisfies a scene's table/region or layer constraints. It has no per-layer transform, opacity, z-order, arbitrary overlay track, multi-product selection, visual preview/collision check, rights grant, constraint solver, approved image rendition/probe evidence, review/approval or general compositor. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3110`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `b2453e6`; a queued rendered-video Shot can now be rebound to another member of the plan's immutable local-video pool from the timeline editor. Creation records the complete source pool (asset code, safe relative path, checksum) in the Worker ShotList; a source change resets the clip to that source's fixed `0-6s` window, persists as a new optimistic timeline revision, and changes the Worker-facing Shot asset/path/checksum rather than only the screen label. The service rejects any source outside the frozen pool and the Worker continues to confine/re-hash the selected file before FFmpeg reads it. Existing plans with incomplete legacy Shot projections remain editable without forcing a synthetic pool. Pure service regressions prove pool freezing, source rebind and rejection; focused backend regressions (`42 passed`), focused Console regression (`6 passed`), Ruff, full backend suite (`623 passed, 119 skipped`), TypeScript and all three Vite builds passed. | N/A: this is a constrained rebinding of preselected local videos, not material search or a full source editor. It has no ranked candidate selection, preview/contact sheet, source rendition duration/PTS metadata, arbitrary import, per-Shot constraint/rights solver, media handles, VFR frame accuracy, stale impact/review, visual-diff approval or general NLE asset browser. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |
| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `7ca9fdf`; rendered-video creation now also accepts one optional local brand visual classified as `media_kind=image`, `material_role=brand_title`, `execution_capability=local_only`. It is frozen as asset code/path/checksum in the Variant snapshot and Worker ShotList, included in later release-rights inputs, shown in the plan evidence and fixed-input trace, and replaces the baseline brand Logo in the actual FFmpeg overlay path while existing plans retain that baseline. The Worker constrains the source under the materials root and fails on a checksum change. Route/schema, Worker and Console regressions cover selection and immutable display; targeted backend regressions (`45 passed`), focused Console regression (`6 passed`), Ruff, full backend suite (`625 passed, 119 skipped`), TypeScript and all three Vite builds passed. | N/A: this is a single static brand image, not a brand system or arbitrary layer track. It has no independent per-Shot logo toggle/placement, transform/keyframes, opacity, multiple brand marks, color/style variant generation, rights grant, constraint/overlap solver, preview, approval, visual regression or access control. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `e200381`; material-library selection in `/production/videos` now includes a controlled local-media preview. `ASSETGRAPH_ASSETS_ROOT` is a shared API/Worker material-root configuration (the Worker keeps `--assets-root` as an explicit override). `GET /assets/{asset_code}/preview` serves only an existing `local_only` image/video/audio asset whose relative path remains beneath that root and whose current SHA-256 equals the registered checksum; local paths are never returned, changed files return conflict, and responses are private/no-store. The creation form renders video source thumbnails, and exposes the selected visual, brand image, product image and BGM with native media controls before task creation. Backend preview/config/file-route regressions (`7 passed`), focused Console interaction (`6 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: preview is a local selection convenience only, not frozen render evidence, a visual approval artifact, a proxy/download authorization service or a rights/constraint decision. It does not inspect a group/package before it is selected, generate contact sheets/posters, validate source duration/PTS/timebase, solve product/table/layer constraints, detect collisions, carry rights grants, offer review/approval, provide per-user asset ACL, or run a visual regression. `EXT-P3-RENDITION-METADATA` remains required for strict rendition reproducibility; PostgreSQL creation journeys remain pending until `ASSETGRAPH_TEST_DATABASE_URL` points at a migrated database. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3107`,`CHK-3108`,`CHK-3110`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `d3c127d`; rendered-video creation now accepts one optional local `media_kind=audio`, `material_role=sound_effect`, `execution_capability=local_only` sound effect with bounded `-24 dB` to `+6 dB` gain. The plan freezes code/path/checksum/gain into the Variant material snapshot, render profile, Worker ShotList and release-input list. A selected source is enabled at the first Shot by default; the queued timeline editor can enable or disable it independently on each fixed Shot. The Worker confines and rehashes the source, verifies an audio stream/duration, then FFmpeg re-inputs the effect at every selected Shot start and mixes it with narration and optional looping BGM. Render evidence records the exact asset/gain/trigger starts, while the Console exposes the selected source, native preview, gain and fixed-input trace. Schema/route/timeline/Worker regressions (`49 passed, 5 PostgreSQL skipped`), focused Console interaction (`6 passed`), Ruff, full backend suite (`631 passed, 119 skipped`), TypeScript and all three Vite builds passed. | N/A: this is one fixed sound-effect source triggered only at Shot starts. It has no multi-SFX palette, arbitrary clip offset/duration, fades, envelope/pan/ducking, mix bus, beat/voice-aware placement, waveform, audio waveform preview, SFX timeline clips, effects chain, rights grant, loudness approval or complete audio review. The PostgreSQL creation journey was extended but requires `ASSETGRAPH_TEST_DATABASE_URL` and a migrated database to run; `EXT-P3-AUDIO-ASSETS` still requires approved rendition duration/timebase/PTS, rights scope and stable binding before strict reproducibility or release conditions can close. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3108`,`CHK-3110`,`CHK-3121`,`CHK-3141`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `d4edca8`; the queued rendered-video timeline now exposes per-Shot product-sticker X/Y placement and width controls after the existing `商品贴片` switch is enabled. All three values are normalized and bounded (`X/Y 0-100%`, width `10-100%`), saved through the existing optimistic timeline revision, and cleared atomically when the sticker is turned off. The API rejects partial, out-of-range or disabled-sticker layouts; a valid layout is copied to the immutable Worker ShotList and asset plan. FFmpeg consumes that frozen object to scale the image and calculate the overlay position, so a saved edit changes the actual rendered frame. Legacy plans without a layout retain their established centered, fixed-size overlay. Schema/timeline/Worker regressions (`44 passed`), focused Console interaction (`6 passed`), Ruff, TypeScript and all three Vite builds passed; full backend suite passed (`634 passed, 119 skipped`). | N/A: this is one static product image per fixed Shot with normalized placement, not a complete scene constraint/compositor. It has no table-surface detection or authored table region, safe-area/polygon constraints, collision/occlusion check, z-index policy beyond the existing render order, opacity/rotation/keyframes, multi-product layer tracks, automatic layout, visual approval or rights decision. The PostgreSQL journey remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` names a migrated database. `EXT-P3-RENDITION-METADATA` remains recorded for strict source reproducibility, but neither prerequisite blocks the local placement workflow. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2140`,`CHK-2141`,`CHK-2143`,`CHK-2146`,`CHK-2201`,`CHK-2202`,`CHK-2205`,`CHK-3106`,`CHK-3110`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `165d4f5`; the rendered-video branch now consumes the versioned constraint profiles that already exist in the material library. Creating a plan reads the selected local visual-video and product-image profile revision, then freezes profile code/revision/fingerprint/full rules in `ProductionVariant.constraint_snapshot_ref`; it does not make a mutable library lookup at render time. For every Shot whose background source provides a `table_surface` for `product_display`, the compiler derives a bounded X/Y/width suggestion, honors `bottom_center`/`bottom_left`/`bottom_right`/`center`, and narrows width with the product image's `size_range`. The timeline and Worker asset plan retain the suggestion; enabling the product sticker applies it as the actual frozen FFmpeg overlay layout, while the Console names the originating table region. Timeline/constraint/Worker regressions (`41 passed`), focused Console interaction (`6 passed`), Ruff and TypeScript passed; all three Vite builds passed; full backend suite passed (`636 passed, 119 skipped`). | N/A: table placement is a deterministic normalized approximation because the current local image contract does not yet freeze the selected image rendition's pixel aspect ratio/alpha bounds. It is not table detection, polygon/safe-area solving, collision or occlusion analysis, z-order solving, multiple-product layout, keyframe animation, visual approval, rights evaluation or the required general solver/unsat explanation. PostgreSQL integration journeys remain skipped until `ASSETGRAPH_TEST_DATABASE_URL` points to a migrated database; `EXT-P3-RENDITION-METADATA` still needs approved dimensions/aspect, duration/timebase/PTS and probe evidence before strict geometric reproducibility can close. Neither prerequisite blocks this local, revision-frozen placement workflow. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2140`,`CHK-2144`,`CHK-2201`,`CHK-2202`,`CHK-2205`,`CHK-3106`,`CHK-3107`,`CHK-3110`,`CHK-3121`,`CHK-3141`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `ccd4f49`; video-plan creation now reads and freezes the selected brand-logo constraint profile alongside the visual and product profiles. Each Shot carries a deterministic `overlay_z_order`: defaults preserve the established brand-then-product render order, while `pin_layer_top` resolves to `1000` and `pin_layer_bottom` to `-1000`. The immutable value is copied through the timeline, branch/restore projection, Worker ShotList and asset plan. FFmpeg now sorts the actual logo/product overlay filters by that order, so a bottom-pinned product is composed before a top-pinned brand; the Console shows the resulting product top/bottom/default state when its sticker is enabled. Constraint/timeline and FFmpeg filter-order regressions (`43 passed`), focused Console interaction (`6 passed`), Ruff and TypeScript passed; all three Vite builds passed; full backend suite passed (`638 passed, 119 skipped`). | N/A: this only covers deterministic `pin_layer_top`/`pin_layer_bottom` for the current two overlay roles. It does not solve `above_role`/`below_role`, `avoid_overlap`, distance/anchor relationships, conflicting hard rules, arbitrary z-index authoring, multiple products/brands, opacity/blend/mask/video overlays, generic track compositing, visual QC or a solver certificate. PostgreSQL integration journeys remain skipped until `ASSETGRAPH_TEST_DATABASE_URL` points to a migrated database. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-2102`,`CHK-2104`,`CHK-2105`,`CHK-2201`,`CHK-2205`,`CHK-3106`,`CHK-3107`,`CHK-3110`,`CHK-3121`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `838996c`; the queued rendered-video timeline now exposes an independent `品牌标识` switch for every fixed Shot, alongside the existing product and sound-effect controls. The complete edit payload stores the role in the optimistic time-axis revision, preserves it through restore/branch projection, copies it to the Worker ShotList, and makes FFmpeg include or omit the already frozen selected/baseline logo in the rendered frame. It shares the immutable logo identity and prior z-order rather than creating a second brand source. Timeline/schema regressions (`48 passed`), focused Console interaction (`6 passed`), Ruff and TypeScript passed; all three Vite builds passed; full backend suite passed (`639 passed, 119 skipped`). | N/A: this is a per-Shot boolean for one static logo, not a brand compositor. It has no logo placement/scale/opacity/keyframes, multiple brand marks, color variants, independent logo audio/animation, brand review/rights approval, collision checks or visual approval. PostgreSQL integration journeys remain skipped until `ASSETGRAPH_TEST_DATABASE_URL` points to a migrated database. No strict Phase 2 or Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-3106`,`CHK-3107`,`CHK-3110`,`CHK-3124`,`CHK-3125`,`CHK-3142`,`CHK-3145`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `fcee508`; the queued rendered-video timeline now freezes a global subtitle style object with one of `compact`/`standard`/`large` presets and a bounded `80-360px` bottom safe margin. The Console validates and saves it in the same optimistic revision as clip text/position; restore and branch preserve it, and the Worker ShotList receives that exact object. The deterministic ASS builder changes caption/center/headline font sizes and caption margin from that frozen object, records the selected style and safe margins in its subtitle manifest, and the existing FFmpeg burn-in consumes the generated ASS artifact. Legacy timelines fall back deterministically to `standard/160px`. Timeline, ASS and Console regressions (`59 passed`), Ruff and TypeScript passed; all three Vite builds passed; full backend suite passed (`641 passed, 119 skipped`). | N/A: this is a small global style vocabulary, not a full subtitle editor. It has no per-clip font/color/background/outline controls, arbitrary fonts or font-license checks, word-level timing, karaoke/highlight, animation, safe-area collision/occlusion detection, visual readability scoring, locale typography, caption import/export or human approval. PostgreSQL integration journeys remain skipped until `ASSETGRAPH_TEST_DATABASE_URL` points to a migrated database. No strict Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-3125`,`CHK-3142`,`CHK-3143`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `9783c37`; after local video rendering, the Worker now creates a checksummed JPEG `contact_sheet` artifact using a fixed six-frame, 3x2 equal-interval FFmpeg grid. The artifact metadata freezes the sampling rule, grid, planned sample times and count; `RenderManifest.outputs` includes its relative path and checksum. `/production/videos` reuses the existing private artifact URL to show the contact sheet beside the video and selected poster, giving an operator a rapid multi-Shot visual check without downloading the full video. Command, manifest/artifact and Console preview regressions (`34 passed`), Ruff and TypeScript passed; all three Vite builds passed; full backend suite passed (`642 passed, 119 skipped`). | N/A: this is a fixed contact sheet, not visual QC. It does not score frames, detect black/freeze/occlusion/subtitle overlap, label exact decoded PTS, permit a frame picker, generate per-channel poster crops, support scene-aware sampling, produce human review/approval evidence, or replace a media player. The PostgreSQL artifact-registration journey remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` points to a migrated database. No strict Phase 3 checkbox is checked. | 2026-07-25 |

| `CHK-4160`,`CHK-4161`,`CHK-4164`-`CHK-4167` preparation update (items remain open) | coderdailyone | commit `cea1d79`; migration `069` turns each local descriptive AttributionReport into a frozen run record: it snapshots selected sessions, pinned metric definitions and active exposure facts, stores a canonical input/result/quality fingerprint, and evaluates a transparent local publication check. Reports without a resolved metric definition, complete observed-session coverage and release-bound exposure are `insufficient_data`; otherwise they are `review_required`. The Operations workspace shows status, fingerprint, reasons and predecessor, can re-run from current evidence into a new record linked by `supersedes_report_code`, and only lets an operator publish an eligible result as `published_descriptive`. The API never labels this result causal. Backend operations/migration/reproducibility tests passed (`43 passed`, `4 skipped` without `ASSETGRAPH_TEST_DATABASE_URL`); Operations API/UI tests passed (`10 passed`); Ruff, TypeScript and all three Vite builds passed. | N/A: this is a minimal local AttributionRun lifecycle, not event-grain measured attribution. It has no immutable platform batch/checksum or data-contract validation, event/processing-time bucket allocation, TimeMapping-quality threshold, numerator/denominator, interval/effect estimate, published-result review history, delete/refund/backfill result revision, comparison workflow or causal method. `EXT-P4-EVENT-GRAIN-METRICS` remains required before measured results: each source owner must provide immutable event/bucket IDs, event/processing time, watermark/checksum, delete/refund/backfill semantics, release/exposure reconciliation keys, grain, quality thresholds and approved allocation rules. No strict Phase 4 checkbox is checked. | 2026-07-25 |

| `CHK-4140`-`CHK-4146`,`CHK-5103`,`CHK-5105` preparation update (items remain open) | coderdailyone | commit `72e365f`; migration `070` connects the existing contract-validated StandardEvent model to `DataQualityBatch`. `POST /data-governance/batches` accepts up to 1,000 source-identified JSON events under one active DataContract revision, freezes a canonical source checksum/watermark, is idempotent for the same `(contract, source_batch_id, checksum)`, and stores each accepted or quarantined normalized event against the batch. JSON-schema, source/schema/operation, late/future-clock and source-event-reuse failures are isolated per row into `data_quality_violations`; the completed batch records accepted/quarantined/rejected counts, status and rule-code summary instead of silently zero filling. The Data Governance workspace selects an active contract, imports a batch and shows the checksum and quality counts. Backend full suite passed (`647 passed`, `121 skipped`); data-governance batch route/schema/migration tests passed; governance UI tests passed (`3 passed`); TypeScript and all three Vite builds passed. | N/A: this is a local JSON/import-adapter foundation, not a finished platform integration or metric engine. It does not yet ingest provider credentials or served logs, map platform event payloads to all eight standard metrics, model update/delete/refund materialization, create raw/standard/aggregate snapshot layers, perform source-to-standard reconciliation, evaluate volume/distribution/freshness SLOs, expose batch violation drill-down, or allocate event-time metric buckets to content spans. `EXT-P4-EVENT-GRAIN-METRICS` remains recorded without blocking: each source owner must provide approved source schema and field mapping, immutable source batch ID/checksum/watermark, event/processing time and timezone semantics, primary keys, update/delete/refund/backfill rules, release/exposure reconciliation keys, expected volume/quality thresholds and owner before strict Phase 4 items can close. No strict checkbox is checked. | 2026-07-25 |

| `CHK-4141`,`CHK-4143`,`CHK-4145`,`CHK-4160`,`CHK-4161`,`CHK-4164`,`CHK-4167` preparation update (items remain open) | coderdailyone | commit `8854b33`; migration `071` adds append-only `functional_session_metric_snapshots`. An operator can choose an active `live_session` MetricDefinitionRevision and create a local event-derived value from accepted, checksummed DataQualityBatches. The snapshot freezes the session interval, definition fingerprint, selectors, all candidate event identities/payload fingerprints, effective event IDs, batch checksums, aggregation result, quality summary and canonical fingerprint. `count` plus `sum`/`min`/`max`/`average`/`last` and ratio selectors are supported; catalog `deduplication_keys` choose the last event per business key and a final tombstone removes that key rather than silently retaining prior value. Descriptive reports prefer the newest ready matching snapshot without mutating the original session import, and freeze the source snapshot into report input. The Operations UI creates and inspects snapshots per selected session. Backend schema/migration/aggregation regressions passed (`35 passed`, `5 skipped` without `ASSETGRAPH_TEST_DATABASE_URL`); Operations API/UI regressions passed (`12 passed`); Ruff, TypeScript and all three Vite builds passed. | N/A: this is a local session-grain aggregate snapshot, not the full raw/standard/aggregate metric platform. It lacks provider adapters, configured metric extraction mappings, complete update/delete/refund materialization across source systems, event-time buckets and span allocation, TimeMapping quality gate, statistical intervals, source-to-standard reconciliation/SLO incidents, backfill result revisions and comparison/drill-down workflows. `ASSETGRAPH_TEST_DATABASE_URL` is unavailable here, so the added end-to-end PostgreSQL journey (contract -> batch -> snapshot -> report) remains skipped and must run against a database migrated through `071` before any strict item is checked. `EXT-P4-EVENT-GRAIN-METRICS` remains required, but does not block further local implementation. No strict checkbox is checked. | 2026-07-25 |

| `CHK-4141`,`CHK-4145`,`CHK-4161`-`CHK-4164`,`CHK-4167`,`CHK-4291`-`CHK-4293` preparation update (items remain open) | coderdailyone | commit `ba82fff`; migration `072` persists immutable per-event `functional_session_metric_buckets` beneath each ready metric snapshot. `count`/`sum`/`average`/`ratio` buckets are marked allocatable; `min`/`max`/`last`, and zero-denominator ratio observations, are retained as session-only rather than being falsely assigned to a scene. Creating a descriptive report intersects allocatable bucket event times with exactly one active `ContentExposure` interval. The report freezes the measured scene allocation with source snapshot/bucket codes, event count, aggregation and release context alongside the pre-existing duration-proportional estimate; Console renders the two results separately. Events outside observed intervals remain unallocated, and all measured output remains descriptive. Full backend suite passed (`654 passed`, `122 skipped`); focused Operations API/UI regressions passed (`12 passed`); Ruff, TypeScript and all three Vite builds passed. | N/A: this is event-point intersection only, not a full bucket model or complete attribution engine. It has no configurable bucket windows, fractional/latency attribution, recording PTS/TimeMapping quality threshold, persisted ContentTimelineSpan revisions, refunds/backfills as report superseding revisions, interval/statistical estimates, causal method, source-to-standard reconciliation or comparative drill-down. The PostgreSQL chain still requires `ASSETGRAPH_TEST_DATABASE_URL` and migrations through `072`; strict Phase 4 items remain open. `EXT-P4-EVENT-GRAIN-METRICS` records the required platform grain, delay and allocation semantics without blocking the next local work item. | 2026-07-25 |

| `CHK-4140`,`CHK-4144`,`CHK-4146`,`CHK-5103`,`CHK-5105` preparation update (items remain open) | coderdailyone | commit `70f3737`; the data-governance API now exposes persisted `DataQualityViolation` rows per source-identified batch, including rule, severity, affected field path, source event ID, structured error evidence and timestamp. Batch ingestion writes the first JSON-schema violation path where one is available, rather than leaving the existing database column empty. The Data Governance workspace only fetches and displays the detail when an operator expands a quarantined/rejected batch, keeping list reads small while making a concrete failed event diagnosable. Route, schema and PostgreSQL-journey regressions passed (`4 passed`, `5 skipped` without `ASSETGRAPH_TEST_DATABASE_URL`); full backend suite passed (`655 passed`, `122 skipped`), frontend suite passed (`81 passed`) and all three Vite builds passed. | N/A: this is a local read-only diagnostic surface, not an incident, replay or reconciliation system. It does not create alerts/tickets, preserve multiple field paths as independently queryable rows, offer event correction/retry, compare raw platform evidence, enforce PII-specific redaction at the detail boundary, evaluate freshness/distribution SLOs, or perform source-to-standard reconciliation. `ASSETGRAPH_TEST_DATABASE_URL` must be configured and migrated through `072` to execute the skipped PostgreSQL journey. `EXT-P4-EVENT-GRAIN-METRICS` still requires source owners to provide immutable platform batches, watermarks, schemas, event semantics and quality thresholds. No strict checkbox is checked. | 2026-07-25 |

| `CHK-4164`,`CHK-4167`,`CHK-4291`,`CHK-4293` preparation update (items remain open) | coderdailyone | commit `f58004a`; the Operations attribution workspace now compares two frozen descriptive reports. When metric keys match, it presents the baseline and candidate sample/observed-session/status/fingerprint context, then computes visible differences for persisted report groups and measured event-time scene values. When metric keys differ it preserves only the structural evidence comparison and explicitly withholds numeric conclusions. The comparison reads the stored reports only; it never recomputes a report or changes publication state. Focused Operations API/UI regressions passed (`13 passed`); full frontend suite passed (`82 passed`), TypeScript and all three Vite builds passed. | N/A: this is a side-by-side descriptive report comparison, not a statistical comparison engine. It has no confidence intervals, standardized effect size, cohort/matched comparison, pre-registered method, covariate balance, multiple-testing correction, causal wording, reviewer workflow, report diff API, benchmark normalization or automatic decision/reproduction. Cross-metric reports intentionally expose no numeric delta. Strict Phase 4 items remain open until complete event/release/exposure evidence, quality gates and result-revision handling are implemented and validated. | 2026-07-25 |

| `CHK-4125`,`CHK-4126`,`CHK-4161`,`CHK-4163`,`CHK-4167`,`CHK-4291`,`CHK-4293` preparation update (items remain open) | coderdailyone | commit `80448a0`; every newly created frozen session metric snapshot now records an `event_time_clock`. The default `session_utc` retains direct event-time intersection. A non-default clock is allocated to an observed scene only when its exact name matches the active revisioned TimeMapping for that session and the inverse-mapped event lies within the map's coverage. The AttributionRun input snapshot freezes the active mapping alongside the metric snapshot; scene results expose mapping codes and the UI reports direct, mapped, missing, clock-mismatched and out-of-coverage buckets separately. Missing/mismatched/partial calibration is unallocated rather than silently falling back to a scene value. Full backend suite passed (`656 passed`, `122 skipped`); focused functional-operations schema/PostgreSQL checks passed (`11 passed`, `5 skipped` without `ASSETGRAPH_TEST_DATABASE_URL`); focused Operations frontend checks passed (`13 passed`), the four concurrent-suite timeout cases passed when rerun serially, and all three Vite builds passed. | N/A: this is a single affine manual clock mapping selected by an operator, not automatic timestamp reconciliation. It has no source-clock registry/adapters, multi-anchor residual/error model, mapping confidence threshold in publication gates, recording PTS/frame mapping, live drift detection, persisted ContentTimelineSpan revision, cross-source clock reconciliation, reconciliation incident, refund/backfill superseding report automation, interval estimate or causal method. A parallel all-frontend Vitest run hit four unrelated 5-second UI-test timeouts under resource contention; those files passed individually/serially and should be investigated in the Phase 8 test-reliability work before using parallel execution as a release gate. `ASSETGRAPH_TEST_DATABASE_URL` must be configured with migrations through `072` to execute skipped PostgreSQL journeys. No strict checkbox is checked. | 2026-07-25 |

| `CHK-4161`,`CHK-4163`,`CHK-4167`,`CHK-4291` preparation update (items remain open) | coderdailyone | commit `73915f9`; each AttributionReport card now has a structured frozen-input evidence drill-down. It reads the report's persisted `input_snapshot`, displaying the session metric value, snapshot code/fingerprint/event clock, TimeMapping revision, and each active exposure's plan, scene, release, source kind and confidence. This is deliberately report-local: viewing it never re-queries mutable current evidence or recalculates a result. Focused Operations API/UI regressions passed (`13 passed`) and TypeScript passed. | N/A: this is a Console rendering of the existing frozen input, not a full lineage/evidence browser. It does not verify artifact checksums on read, author evidence, expose raw event/bucket rows, show source-document excerpts, enforce record-level PII redaction, compare old/new evidence snapshots, provide export or review signatures, or replace the planned graph/lineage APIs. Historical reports without structured input accurately show empty historical evidence rather than invented details. Strict Phase 4 items remain open. | 2026-07-25 |

| `CHK-5144`,`CHK-5145`,`CHK-5146`,`CHK-5181`,`CHK-5187`,`CHK-5295` preparation update (items remain open) | coderdailyone | commit `55d1e2a`; local descriptive effect estimates now support an explicit revoke transition with a required human reason and actor. Migration `073` persists `revoked_by` and `revoked_reason`; the locked service transition only permits candidate/approved -> revoked, is idempotent for an already revoked row, and rejects later approval or reproduction. Existing reproduction drafts are intentionally immutable and remain intact. `POST /functional-learning/effects/{effect_code}/revoke` exposes the typed contract, and the Learning workspace displays the reason/status while allowing an operator to revoke an active candidate or approved effect. Route, migration-order and UI payload regressions passed (`13 passed` backend focused, `3 passed` frontend); Ruff, TypeScript, all three Vite builds and the full backend suite (`658 passed`, `122 skipped`) passed. | N/A: this is a local manual stop-use lifecycle, not EffectEligibilityPolicy or complete effect dependency management. It has no PerformanceProfile/AssociationalEstimate/CausalEstimate separation, entity/context/window/interval/drift eligibility evaluation, expiry/supersede workflow, DecisionLog or recommendation propagation, automatic invalidation from data deletion/rights/metric changes, impact task queue, LearningPolicy eligibility filter, point-in-time feature snapshot, actual exposure linkage or causal claims. The PostgreSQL lifecycle journey remains skipped without `ASSETGRAPH_TEST_DATABASE_URL`, which must be configured and migrated through `073` before a strict checklist item can close. No strict Phase 5 checkbox is checked. | 2026-07-25 |

| `CHK-5160`,`CHK-5162`,`CHK-6290`-`CHK-6292` preparation update (items remain open) | coderdailyone | commit `dc50594`; new local A/B experiments require a typed registration with hypothesis, treatment mechanism, target estimand, inclusion rules, observation window, covariates, identification assumptions and analysis plan. Migration `074` stores that registration, its canonical fingerprint and the immutable `stable_hash_sha256_v1` assignment strategy. The assignment endpoint returns the exact variant and registration fingerprint for an experiment/subject pair; the Learning workspace makes an operator confirm this visible assignment before enabling metric-result entry, and each experiment retains independent subject/value state. Legacy rows remain readable as explicitly unregistered history. Route, migration-order and UI regressions passed (`14 passed` backend focused, `4 passed` frontend); Ruff, TypeScript, all three Vite builds and the full backend suite (`659 passed`, `122 skipped`) passed. | N/A: this is a pre-registered local descriptive A/B prototype, not a completed experiment system. There is no revisioned registration review, assignment persistence table, exposure/release reconciliation, A/A or SRM check, guardrail, traffic allocation, actual served-log verification, windowed metric snapshot, statistical interval, covariate analysis, sensitivity/negative-control diagnosis, causal estimate or policy change. The PostgreSQL journey remains skipped without `ASSETGRAPH_TEST_DATABASE_URL`; it must run after migration `074` before strict items can close. No strict Phase 5 or Phase 6 checkbox is checked. | 2026-07-25 |

| `CHK-5120`,`CHK-5122`,`CHK-5160`,`CHK-5162`,`CHK-6290`-`CHK-6292` preparation update (items remain open) | coderdailyone | commit `be29150`; confirming a local experiment subject now writes immutable `functional_experiment_assignments` evidence instead of merely returning a deterministic preview. Migration `075` creates the Assignment record with code, subject, variant, strategy, registration fingerprint and timestamp, then backfills existing outcome rows as explicitly labelled `legacy_outcome_backfill_v1`. Assignment creation is idempotent under a locked experiment row; result entry now rejects an unassigned subject and reads the persisted variant rather than recalculating it. The Learning workspace calls the explicit command before it enables result entry. Focused route/migration checks passed (`14 passed`); learning UI checks passed (`4 passed`); Ruff, TypeScript, all three Vite builds and the full backend suite (`659 passed`, `122 skipped`) passed. | N/A: this is local assignment provenance only. It does not log a FeatureSnapshot/candidate set/propensity, bind Assignment to a specific content strategy/revision or release, reconcile actual exposure, persist traffic allocation/holdout, verify SRM/A-A/guardrails, model exposure mismatch, support stop/rollback, calculate intervals/effects, or produce a causal estimate. Backfilled assignments cannot claim the original assignment time because historic outcomes lacked that field. `ASSETGRAPH_TEST_DATABASE_URL` with migrations through `075` remains required to run the skipped PostgreSQL journey. No strict Phase 5 or Phase 6 checkbox is checked. | 2026-07-25 |

| `CHK-5120`,`CHK-5121`,`CHK-5145`,`CHK-5185`,`CHK-5186`,`CHK-5188` preparation update (items remain open) | coderdailyone | commit `df3e091`; an approved effect reproduction now requires a human change hypothesis and completes as one transaction: it reserves a DecisionLog code, writes that code into the new ContentProject's immutable source references, creates the draft without an intermediate commit, and persists a fingerprinted `effect_reproduction` DecisionLog containing source effect/project revisions, the resulting project revision and the change hypothesis. The response and Learning workspace show the new decision code. Any failure rolls back both the project creation and the decision record. Migration `076` adds typed decision payload/source-reference/fingerprint fields while preserving legacy manual decisions. Route/migration checks passed (`15 passed`); learning UI checks passed (`5 passed`); Ruff, TypeScript, all three Vite builds and the full backend suite (`660 passed`, `122 skipped`) passed. | N/A: this covers one manual effect-reproduction decision, not a complete point-in-time DecisionLog system. It has no candidate/exclusion snapshot, FeatureSnapshot, model/strategy/weight/exploration/propensity, release/exposure attachment, automated dependency invalidation, reproduction diff/rollback UI, independent release/evaluation creation, policy-based ranking, or transaction-safe link to an actual external delivery. The PostgreSQL lifecycle journey remains skipped without `ASSETGRAPH_TEST_DATABASE_URL`, which must be migrated through `076`; no strict Phase 5 checkbox is checked. | 2026-07-25 |

| `CHK-7140`-`CHK-7144` preparation update (items remain open) | coderdailyone | commit `da4ef36`; migration `077` introduces append-only local `BroadcastSchedule` revisions with every planned lifecycle status, a frozen Release fingerprint at validation, target account/room, platform, timezone/window, owner, promotion/inventory dependencies, conflict strategy, stop conditions, canonical validation evidence and revision fingerprint. `/operations/schedules` creates a draft from an existing Release and validates current-release availability plus overlapping current local room plans. The Console presents the local revision, validation evidence and an explicit disabled go-live capability; there is no platform submission or go-live command. Route/migration checks passed (`13 passed`); the schedule and Console UI tests passed (`6 passed`); Ruff, full backend suite (`662 passed`, `122 skipped`), full frontend suite (`86 passed`) and all three Vite builds passed. | N/A: only local Release state and local room-time conflicts are currently verified. Rights channel/region/term, account credentials/capability, real target availability, inventory/promotion facts, platform rules, calendar synchronization, approval/authorization, lifecycle transitions beyond local draft/validation, cancellation/completion, actual platform submission and all go-live behavior remain open. `docs/operations/external-platform-register.md` records `Platform scheduling and go-live APIs`: each platform owner must provide approved target/credential scope, availability and rule contract, promotion/inventory source semantics, quota/cost/region/retention terms and a sandbox or contract fake before strict schedule/go-live items can close. No strict Phase 7 checkbox is checked. | 2026-07-25 |

| `CHK-7101`,`CHK-7103`-`CHK-7105` preparation update (items remain open) | coderdailyone | commit `dd4cfce`; migration `078` adds locally managed SourceEvidence and FactClaim records beside the existing versioned ProductFactCards. A source draft stores type, title, optional URL, cited excerpt, access scope, capture time and canonical checksum; only an explicitly approved source can create a claim, and each claim preserves exact source code, citation excerpt, validity window, field path and fingerprint. An approved claim promotes its linked local fact while retaining the source join. The Knowledge workspace provides a separate `来源证据` view for creating/approving sources, selecting an approved source, creating a claim and approving it; it never fetches a URL. Contract/migration checks passed (`13 passed, 2 skipped`); Knowledge API/UI checks passed (`5 passed`); Ruff, full backend suite (`664 passed, 123 skipped`), full frontend suite (`88 passed`) and all three Vite builds passed. | N/A: this is a compact local evidence and claim flow, not the complete FactCard/FactClaim governance or graph projection. It has no source connector/extraction run, immutable captured artifact, citation character offsets, field-level claim conflict resolution, source revoke/review/rejection UI, effective-time search filtering, access-control enforcement, content/compliance/terminology modules, vector/graph retrieval, outbox/watermark/rebuild, or complete lineage to releases/sessions/effects. `docs/operations/external-platform-register.md` records the `Knowledge source systems and controlled documents` inputs that source owners must approve before any connector or strict knowledge item can close. The PostgreSQL evidence lifecycle test remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` is set to a database migrated through `078`; no strict Phase 7 checkbox is checked. | 2026-07-25 |

| `CHK-7180`,`CHK-7182`,`CHK-7185` preparation update (items remain open) | coderdailyone | commit `21c7967`; `/governance/runs?run=...&task=...` now renders the selected authenticated control-plane `WorkflowRun` rather than only the dashboard queue. It shows the subject, status, progress, priority, queue/wait/error context, budget/actual-cost object, every Step's attempt/dependency/side-effect/worker/error projection, and HumanTask owner/revision/decision state. Queue entries, alerts and task deep links select the same run; non-terminal runs expose the pre-existing cancel endpoint with a required reason and refresh the run/task projections after completion. Focused governance/Console tests passed (`7 passed`), TypeScript and all three Vite builds passed. | N/A: the workspace does not yet list all historical WorkflowRuns independently of the current Console queue, expose worker retry/reconcile/timeout controls, display task claim/decision forms, show workflow artifacts/trace/cost line items, queue position, sample impact diff, notifications/preferences, bulk workflows or an auditable approve/reject/waive surface. Those capabilities must use the existing controlled commands and remain open; no strict Phase 7 checkbox is checked. | 2026-07-25 |

| `CHK-1103`,`CHK-1105`,`CHK-7101`,`CHK-7103`-`CHK-7105` preparation update (items remain open) | coderdailyone | commit `bce347d`; ContentProject now accepts approved `FactClaim` selections independently of ProductFactCards. Creation, draft revision, confirmation and generation resolve the exact claim only while its claim/source/fact are approved, enforce its validity window, and freeze the claim code, local fact code, source-evidence code/checksum, cited excerpt, field path and fingerprint in the immutable project input. Generation rechecks that fingerprint and source checksum before use, records the frozen relation in source/fact-revision references, and emits a ScriptBlock citation with the claim and source codes. Both ContentProject creation and its independent fact-input editor list only approved claims backed by an approved source, retain the pinned claim in the project summary, and display claim citations in generated or human script views. Focused frontend regression (`13 passed`) covers parsing and saving a selected claim; full backend suite passed (`664 passed, 124 skipped`), full frontend suite passed (`92 passed`), TypeScript and all three Vite builds passed. | N/A: this is a local frozen input path, not complete FactClaim governance or graph lineage. It has no immutable captured source artifact, citation character offsets, source revoke/reject workflow, field-level conflict resolution, effective-time search/index, record-level access enforcement, content/compliance/terminology separation, complete lineage to releases/sessions/effects, graph projection or retrieval. The PostgreSQL source-backed project-to-script journey is present but skipped because `ASSETGRAPH_TEST_DATABASE_URL` is not configured; provide a disposable database migrated through `078` to execute it. External source acquisition remains recorded in `docs/operations/external-platform-register.md`; no strict checkbox is checked. | 2026-07-25 |

| `CHK-1102`,`CHK-1121`,`CHK-1140`,`CHK-1143`,`CHK-1146` preparation update (items remain open) | coderdailyone | commit `292b9e5`; ContentProject creation now exposes the full locally supported production-input set: ordered product identifiers, conversion, staging, visual and audio requirements in addition to the existing goal/fact/template fields; the immutable input editor exposes and revises the same product order. The deterministic content chain carries that frozen order into its generation context, StoryBrief and Script revision metadata. When generated product-fact blocks exist, ProgramSegments bind the first ordered product identifiers in their declared order, without fabricating a reference after the supplied order is exhausted. A backend unit regression covers StoryBrief/context and segment ordering; a frontend regression covers the complete create payload. Full backend suite passed (`665 passed, 124 skipped`), full frontend suite passed (`93 passed`), TypeScript and all three Vite builds passed. | N/A: this is a deterministic local projection, not a product catalog or semantic product-to-claim resolver. It does not validate identifiers against a product master, assign every fact to a product, resolve duplicate/ambiguous products, derive dynamic pacing, create provider artifacts, expose field-level input diffs, or regenerate downstream variants automatically after a revision. PostgreSQL end-to-end persistence continues to require `ASSETGRAPH_TEST_DATABASE_URL`; no strict Phase 1 checkbox is checked. | 2026-07-25 |

| `CHK-1103`,`CHK-1105`,`CHK-7101`,`CHK-7103`-`CHK-7105` preparation update (items remain open) | coderdailyone | commit `9ec6f17`; migration `079` adds attributable source/claim revocation fields and database constraints. An approved SourceEvidence or FactClaim can be revoked only with a nonblank operator and reason; the locked transition is idempotent after revocation, never deletes source/citation/fingerprint history, and rejects an invalid lifecycle transition. Revoking a source immediately makes its dependent claims unavailable to new ContentProject selection, confirmation and generation through the existing approved-source resolver; revoking a claim also recalculates its local fact status without disabling a fact that still has another approved active source. The Knowledge workspace exposes both revoke operations, their reasons and current source state. Route/migration checks (`20 passed, 3 skipped`), Knowledge API/UI checks (`7 passed`), Ruff, full backend suite (`667 passed, 125 skipped`), full frontend suite (`95 passed`), TypeScript and all three Vite builds passed. | N/A: this is a local corrective lifecycle, not complete knowledge governance. It has no source connector/extraction run, immutable captured artifact, citation character offsets, source/claim rejection or appeal workflow, field-level conflict resolution, expiration/search index, record-level access-control enforcement, content/compliance/terminology modules, outbox/watermark graph projection or complete lineage to releases/sessions/effects. `docs/operations/external-platform-register.md` remains the required source-owner input for connectors and controlled documents; no external request was made. The PostgreSQL revocation journey is present but skipped until a disposable `ASSETGRAPH_TEST_DATABASE_URL` is configured and migrated through `079`; no strict checkbox is checked. | 2026-07-25 |

| `CHK-1103`,`CHK-1105`,`CHK-7101`,`CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | commit `695f493`; the Knowledge evidence workspace now creates FactClaims with explicit UTC validity start/end values, renders their fixed effective window, and searches locally managed declarations through the existing `q` API without changing the zero-argument list contract already consumed by ContentProject. A revoked source is no longer an enabled form selection, so a stale browser selection cannot create a new declaration locally. Focused Knowledge and ContentProject UI/API regressions (`12 passed`), full frontend suite (`97 passed`), TypeScript and all three Vite builds passed. | N/A: this is a UI/API projection of existing validity rules, not effective-time conflict resolution or a complete search system. It has no source/claim rejection or appeal, automatic expiration transition/notification, access filtering, field-level fact provenance, vector/graph retrieval, release/session/effect lineage projection or completeness watermark. Strict items stay open because the local PostgreSQL journeys and the remaining governance/graph requirements have not been completed. | 2026-07-25 |

| `CHK-2102`,`CHK-2106`-`CHK-2109`,`CHK-2389` preparation update (items remain open) | coderdailyone | commit `dbdb365`; `/assets/batch-classification` now applies one explicit `media_kind`, multi-value `material_roles` and `execution_capability` decision to up to 100 selected active assets as a single locked transaction. Duplicate target codes are normalized; an empty/unknown target set fails before any update, so a partial bulk correction cannot leave mixed state. The Material Library exposes a multi-select batch editor and a `仅看待确认分类` filter, while each list row shows its independent media kind, roles and capability rather than reusing the legacy file type. Route/schema regression, PostgreSQL all-or-nothing journey (present but skipped without local database), Asset Library UI regression, Ruff, full backend suite (`668 passed, 126 skipped`), full frontend suite (`98 passed`), TypeScript and all three Vite builds passed. | N/A: this is a current-asset batch correction, not the full AssetVersion/Rendition/synchronization lifecycle. It has no revision history or correction task ownership per asset, binding discovery/soft invalidation, RightsGrant enforcement, perceptual duplicate merge, bulk undo/diff, selection effect evidence or production-data migration report. The skipped PostgreSQL journey needs a disposable `ASSETGRAPH_TEST_DATABASE_URL`; no strict checkbox is checked. | 2026-07-25 |

| `CHK-2206`,`CHK-2209`,`CHK-2393` preparation update (items remain open) | coderdailyone | commit `a3a4b14`; the Material Library now exposes the existing terminal `obsolete` AssetGap transition for every non-terminal/active gap. The operator must enter a reason, which is persisted in the already append-only resolution evidence/event payload and rendered when the gap is viewed later; this does not remove prior candidate, resolved or waiver history. Asset Library regression covers the exact `PATCH` payload and reason; full frontend suite (`99 passed`), TypeScript and all three Vite builds passed. | N/A: obsolete remains a local manual conclusion. There is no automatic stale-gap detection, per-variant impact task, rights/constraint preflight, granular waiver scope, candidate rescan, complete solver conflict provenance or external procurement workflow. No strict checklist item is checked. | 2026-07-25 |

| `CHK-7101`,`CHK-7103`,`CHK-7104` preparation update (items remain open) | coderdailyone | commit `58e85b5`; migration `080` completes the local draft-review rejection path for SourceEvidence and FactClaim. A locked `draft -> rejected` transition requires a nonblank reviewer and reason, stores reviewer/time/reason, is idempotent for the same terminal rejection and rejects invalid lifecycle transitions. Rejected claims set their linked local fact to `rejected`; the migration also corrects the pre-existing fact-status constraint so both `rejected` and the earlier `revoked` service transition are representable. The Knowledge workspace now exposes source and claim rejection forms with the persisted reason, while API clients receive the full rejection projection. Route/migration checks (`33 passed`), Knowledge API/UI checks (`11 passed`), full backend suite (`670 passed, 127 skipped`), full frontend suite (`101 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: this is a local review decision only. It has no appeal/reopen workflow, field-level conflict resolution, immutable captured source artifact, citation character offsets, source connector/extraction run, automatic expiration, record-level access control, graph/vector retrieval, projection watermark or complete release/session/effect lineage. No external API or credential is needed for this increment. The PostgreSQL lifecycle test is present but skipped until a disposable `ASSETGRAPH_TEST_DATABASE_URL` is configured and migrated through `080`; source-system connector inputs remain registered in `docs/operations/external-platform-register.md`. No strict Phase 7 checkbox is checked. | 2026-07-25 |

| `CHK-2140`,`CHK-2149`,`CHK-2205` preparation update (items remain open) | coderdailyone | commit `780c7a0`; AssetConstraintProfile now exposes a newest-first immutable revision projection at `/assets/{asset_code}/constraint-profile/revisions`. The Material Library renders every stored revision, its Profile code/fingerprint and normalized rule summaries, then shows the exact rule additions/removals against the prior revision. Editing remains an append-only `保存新修订` action, so operators can inspect the constraint revision already fixed in selection snapshots without rewriting it. Route/PostgreSQL lifecycle checks (`4 passed, 3 skipped`), Asset Library UI checks (`8 passed`), full backend suite (`671 passed, 127 skipped`), full frontend suite (`102 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: this adds revision visibility and local diff only. It does not yet model constraint scope/key templates, polygons/anchor evidence, hard-constraint intersection, an OR-Tools adapter, unsatisfiable conflict sets, rights-aware selection, per-variant impact tasks or a two-step “promote room override to global revision” workflow. No external API or credential is needed. PostgreSQL persistence coverage remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `080`; strict Phase 2 items remain open. | 2026-07-25 |

| `CHK-2140`,`CHK-2147`-`CHK-2149`,`CHK-2201`,`CHK-2205` preparation update (items remain open) | coderdailyone | commit `489e99d`; a room-local geometry override can now be deliberately promoted from a frozen LiveRoom plan into a new global AssetConstraintProfile revision. The command reads only the persisted `BuildPlan.inventory_snapshot`, verifies the selected override geometry, locks the target Profile, requires `expected_revision`, appends an `allowed_region` hard rule and records actor, change reason, originating plan, prior revision and original override in migration `081`. Repeating the same plan/asset promotion returns the original revision rather than creating another one. The plan detail requires a separate reason and confirmation before exposing the command; z-order-only overrides remain room-local because they cannot be represented safely as a global profile rule. Route/migration/constraint checks (`39 passed`), LiveRoom UI checks (`2 passed`), full backend suite (`673 passed, 128 skipped`), full frontend suite (`103 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: promotion supports only persisted normalized geometry and one explicit hard `allowed_region`; it is not a general override merger. It does not support polygon/scope/key selection, review/approval, reversal, source-plan permissions, impact tasks for existing variants, rights checks, solver-based intersection/unsatisfiable explanations, global z-order semantics or automatic rollout. No external API or credential is required. PostgreSQL lifecycle coverage is present but skipped until `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `081`; strict Phase 1/2 items remain open. | 2026-07-25 |

| `CHK-2264`,`CHK-2265`,`CHK-2266`,`CHK-2272` preparation update (items remain open) | coderdailyone | commit `4faee83`; the Content Strategy workspace now exposes all locally supported `content-strategy.v2` policy inputs instead of silently sending empty objects: compatibility tags, per-module reusable guidance, target duration/pacing, product rotation, interaction, conversion and host delivery. The frontend normalizes and renders the complete immutable policy projection along with module/example evidence; creation writes these named fields to the existing revision contract. The session/module cleanup effects now retain their state reference when nothing changed, preventing the strategy tab from entering a rerender loop. API normalization/payload and strategy-page interaction regressions cover the policy contract and tab lifecycle. Full frontend suite (`104 passed`), TypeScript and all three Vite builds passed. | N/A: policy objects remain a compact local authoring vocabulary rather than a complete versioned policy DSL with per-field review, compatibility/conflict rules or contextual compiler evaluation. This does not add provider-backed ASR/OCR/keyframe extraction, player-range seeking, durable source-fact redaction proof, text/brand/host similarity, copyright/PII screening, issue workflow or an end-to-end human review audit. No strict Phase 2 checkbox is checked; no external platform credential or source request is required for this increment. | 2026-07-25 |

| `CHK-2264`,`CHK-2303`-`CHK-2306` preparation update (items remain open) | coderdailyone | commit `1dd9a45`; a selected content-strategy revision now freezes its supported duration, module-recipe, product-rotation, interaction, conversion and host-style policies into the ContentProject contribution decision. The deterministic compiler carries module-specific guidance and immutable source policy into ScriptBlock sources, emits explicit interaction/CTA actions, and retains applicable rotation/duration/host policy as ProgramSegment metadata. Primary-template source precedence remains stable when policy objects merge. The ContentProject chain exposes the frozen strategy summary plus per-block recipe, interaction and conversion decisions; unit/API regression verifies the projection. Full backend suite (`674 passed, 128 skipped`), full frontend suite (`105 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: the compiler still uses a deterministic skeleton, does not compile arbitrary `program_outline` entries into a semantic script/program, perform category retrieval or score compatibility/conflicts, generate a human-readable adopted/unadopted conflict rationale, or enforce policy semantics beyond preserving the structured instruction. Template examples remain non-factual sources and `reference_only` layout remains non-executable, but provider-backed generation, full source-review gates and database-backed end-to-end coverage require the separately recorded work/external test database. No strict checkbox is checked. | 2026-07-25 |

| `CHK-2264`,`CHK-2303`-`CHK-2306` preparation update (items remain open) | coderdailyone | commit `43de91c`; each selected `content-strategy.v2` revision now freezes its reviewed `program_outline` stages (module key, title, purpose and bounded source-session interval) into the ContentProject contribution decision. The deterministic compiler emits the adopted primary stages in order, inserts explicitly adopted secondary stages only before a terminal primary `conversion` stage, and turns every stage into a ScriptBlock/ProgramSegment with stage provenance, semantic goal, entry/exit condition and fixed policy actions. Approved fact blocks remain independent project inputs and are inserted before a named conversion stage; they do not inherit source-recording facts. The ContentProject workspace displays fixed stages and ScriptBlock evidence ranges. Unit, API and UI regressions cover the primary/secondary ordering and evidence display. Full backend suite (`675 passed, 128 skipped`), full frontend suite (`105 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: compilation is still deterministic and recognizes only a named terminal `conversion` stage; it has no semantic stage taxonomy, arbitrary outline conflict/placement solver, category retrieval, similarity scoring, contextual LLM compiler, generated-artifact/provider evidence, or per-policy review/compatibility engine. Existing projects receive the new outline snapshot when a subsequent confirmation regenerates their current revision; no historical revision is mutated. Strict completion and source-analysis gates remain open. | 2026-07-25 |

| `CHK-2265`,`CHK-2304`,`CHK-2305` preparation update (items remain open) | coderdailyone | commit `1597218`; reviewed template examples are now frozen with module key and source-session interval in every selected ContentProject contribution decision. The context compiler exposes only adopted stage summaries and reviewed example wording under an explicit `non_authoritative_reference_only` fact boundary. ScriptBlock template-source evidence carries matching examples for the adopted module, while the generated block text and `fact_citations` remain independent of those examples. The ContentProject workspace renders the frozen examples and their bounded evidence ranges. Unit/API/UI regressions prove the example is retained as reference evidence but never copied into the deterministic script text or fact citations. Full backend suite (`675 passed, 128 skipped`), full frontend suite (`105 passed`), Ruff, TypeScript and all three Vite builds passed. | N/A: examples are still authored/reviewed locally and are not consumed by a provider-backed context compiler. This does not implement ASR/OCR/keyframe extraction, durable source-redaction proof, full text-similarity/brand-host imitation/copyright/PII screening, granular example adoption/rejection or human-review issue workflow. No strict Phase 2 checkbox is checked. | 2026-07-25 |

| `CHK-2243`,`CHK-2244`,`CHK-2265`,`CHK-2271`,`CHK-2272` preparation update (items remain open) | coderdailyone | commit `980214c`; the ContentProject chain now turns every frozen strategy-stage and reviewed-example source interval into a direct `/research/live-sources?view=sessions` deep link. The link carries the immutable CaptureSession code plus exact IN/OUT seconds, so an operator can return from the generated ScriptBlock to the bounded recording interval rather than manually searching for it. The compact links coexist with the existing stage/example text and preserve the non-factual reference boundary. ContentProject UI regression verifies the exact stage and example URLs; full frontend suite (`105 passed`), TypeScript and all three Vite builds passed. | N/A: this is review navigation over already-normalized coordinates. It does not fetch/seek raw media itself, create timelines/ASR/OCR/keyframes, prove media integrity, record a reviewer decision, redact source facts, or carry out similarity/copyright/PII review. It therefore cannot close a strict capture-cleaning or template-review checkbox. | 2026-07-25 |
| `CHK-2182`-`CHK-2185`,`CHK-2187`-`CHK-2189`,`CHK-2200`-`CHK-2203` preparation update (items remain open) | coderdailyone | commit `87a1e06`; migration `082_material_pack_resolution_contract` adds stable pack kind, published-revision pointer and per-revision `draft/published/superseded/archived`, exclusivity and constraints. New classification packs are role-scoped; total packs may include only published classification packs, and every group/category entry is expanded into explicit asset codes before its revision is stored. `POST /assets/material-packs/resolve` returns pinned revision refs, per-entry candidate sets, merged material rules/source lists, conflict records and one fingerprint. Live-room planning freezes that result, prefers applicable required/alternative rules deterministically, then counts emitted layer occurrences against required/alternative/min/max rules and blocks unmet plans with fixed evidence. The workbench authors total/classification packs, scope, alternative set and exclusivity, distinguishes current drafts from the revision actually selectable by planning, and performs resolve preview before generating. Pure backend coverage verifies rule-strength merge, exclusivity conflict and occurrence checks; focused backend `19 passed`, focused frontend `10 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: role-domain `inherit/append/replace`, arbitrary pack-constraint execution/solver integration, RightsGrant filtering, semantic/format/effect ranking, generic infeasibility explanations, arbitrary revision rollback/impact analysis and migrated PostgreSQL integration evidence remain incomplete. The available test environment has no `ASSETGRAPH_TEST_DATABASE_URL`, so PostgreSQL pack persistence suites remain skipped. No strict checkbox is checked. | 2026-07-25 |
| `CHK-2186`,`CHK-2189`,`CHK-2200`,`CHK-2202` preparation update (items remain open) | coderdailyone | commit `6f52b83`; resolve and live-room create contracts now accept a role map of `inherit`/`append`/`replace`. The resolver applies the mode against pinned refs: inherit removes same-role classification-pack entries; replace removes same-role total-pack entries and their exclusive role declaration; append keeps both. The exact mode and resulting resolution fingerprint are frozen in Variant/configuration/BuildPlan snapshots, carried to Blueprint/quality report, displayed in plan detail and copied only as business input by clone. Replace additionally filters compilation candidates to classification-pack sources, producing `missing_replacement_role` instead of quietly using a loose or total-pack asset. The Live Room workbench shows separate total/classification selections and an explicit per-role segmented mode control that re-runs resolution before generating. Backend API/unit and frontend UI regression cover validation, merge behavior, candidate filtering and request payload; full backend `682 passed, 128 skipped`, frontend `106 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: mode composition currently operates on material-pack entries. Loose assets and groups are not yet role-scoped inputs for inherit/append, pack constraint bodies are frozen/merged rather than generically solved, and rights/semantic/effect ranking remain absent. PostgreSQL integration needs a migrated `ASSETGRAPH_TEST_DATABASE_URL`; no strict checkbox is checked. | 2026-07-25 |

| `CHK-2208` preparation update (item remains open) | coderdailyone | commit `d63923f`; a LiveRoom plan can now carry an explicit `asset_gap_waivers` map only for selected `open`/`candidate_found` gaps. The resolver preserves the library `source_status`, freezes only the current plan reference as effective `waived`, and records the reason plus a `current_live_room_configuration_revision` scope in the snapshot fingerprint. The service rejects waivers for unselected, resolved or terminal gaps; clone deliberately copies the gap selection but omits waiver input so the target plan must make its own decision. The Live Room workbench presents an optional reason only for selected active gaps, describing its plan-only scope. Schema, isolated snapshot and UI request regressions passed; full backend `686 passed, 128 skipped`, Ruff, TypeScript and all Vite builds passed. | N/A: legacy global AssetGap lifecycle still exposes a global `waived` state for prior library workflows, and this increment has no migrated PostgreSQL create/clone lifecycle proof. There is no per-variant impact queue, candidate rescan, rights/constraint preflight or external procurement integration. The strict checkbox remains open until those state-boundary and PostgreSQL journeys are completed. | 2026-07-25 |

| `CHK-2201`,`CHK-2202`,`CHK-2205` preparation update (items remain open) | coderdailyone | commit `2e5585c`; directly selected loose assets now have an explicit `至少使用一次` control in the Live Room workbench. Only a current direct selection may be marked required; the server freezes that normalized list into Variant/configuration/ResolvedMaterialSnapshot, carries it when cloning business inputs, then counts the emitted MaterialSelectionDecision layers. A zero-occurrence must-use asset blocks the BuildPlan with `required_loose_asset_unsatisfied_minimum:<asset_code>` and records reproducible observed-count evidence in the Blueprint and quality report. The plan detail shows frozen must-use inputs. Schema, compiler-unit and UI payload regressions passed; full backend `689 passed, 128 skipped`, full frontend `106 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: loose assets currently support optional versus minimum one occurrence for the `live_room` branch only. They do not yet have per-asset maximum, scope, alternative-set or rendered-video time/format overrides; group and pack membership remain declarative sources. Rights, semantic/format/effect ranking and solver-based hard-constraint feasibility are still absent, so strict items remain open. | 2026-07-25 |

| `CHK-2204`,`CHK-2205` preparation update (items remain open) | coderdailyone | commit `a30be23`; live-room material selection now calculates and freezes named score parts for every candidate: role match, execution capability, bound constraint profile, frozen material-pack requirement strength and a deterministic repeat penalty. During compilation, each emitted layer increments the selected asset count, so otherwise tied candidates rotate before previously used assets; the candidate list, selected score parts and stable reasons are persisted in Blueprint/quality evidence and rendered in the plan detail. The implementation deliberately does not invent semantic, format, quality or effect scores when those evidence sources do not exist. A focused unit regression proves the second tied selection chooses the unused candidate and preserves the negative repeat component; full backend `690 passed, 128 skipped`, full frontend `106 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: no RightsGrant, semantic embedding, rendition quality, time/format compatibility, soft solver objective or qualified effect-evidence source is present yet. The ranker therefore remains a deterministic local prototype, and strict selection/diagnostic checkboxes stay open. | 2026-07-25 |

| `CHK-2210` preparation update (item remains open) | coderdailyone | commit `a514882`; the Material Library now includes an `效果与关系` panel on each selected asset. It projects the current asset's group memberships, resolved material-pack membership/revision and AssetGap role (fixed solution versus alternative), so an operator can inspect the existing local use relationships without navigating each workbench manually. With no direct attribution evidence modelled for the asset, it explicitly displays `样本不足` and does not permit automatic recommendation. Asset Library regression covers the relationship projection and no-sample state; full frontend `107 passed`, TypeScript and all Vite builds passed. | N/A: this is a local-library relationship projection, not an effect lineage API. It does not yet traverse frozen live/video plans or releases, calculate sample size/confidence/drift, persist asset-level exposure outcomes, distinguish descriptive/associational evidence, or feed a qualified effect score into the selector. The strict item remains open. | 2026-07-25 |

| `CHK-2206`,`CHK-2208` corrective update (items remain open) | coderdailyone | commit `0d551dc`; the Material Library can no longer create a new global `AssetGap.status=waived` transition. The global editor now exposes only candidate/resolved/obsolete lifecycle commands and renders any pre-existing global waiver as historical audit data. Branch-level `asset_gap_waivers` remains the sole writable waiver surface: the PostgreSQL lifecycle journey was changed to prove that a new plan freezes `source_status=open` with effective `status=waived`, while the library gap itself remains `open`. Schema and lifecycle tests, full backend `692 passed, 128 skipped`, full frontend `107 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: legacy database rows and schema constraint still retain `waived` so their history can be read and terminally marked obsolete; no migration attempts to rewrite historic evidence. The candidate/resolved flow still lacks RightsGrant, authorization and constraint preflight, branch impact work and procurement integration, so strict items remain open. | 2026-07-25 |

| `CHK-2265`,`CHK-2266`,`CHK-2300`,`CHK-2307` superseded preparation update (items remain open) | coderdailyone | commit `cbca523`; this initial template-handoff implementation used the generic `reference_template_*` contract and incorrectly required a `layout_hypothesis` handoff to match a ContentProject `content_strategy` selection. It is superseded by `548f896` below and must not be used as implementation guidance. | N/A: retained only as an audit record of the corrected design error. | 2026-07-25 |

| `CHK-2265`,`CHK-2266`,`CHK-2307` corrective preparation update (items remain open) | coderdailyone | commit `548f896`; published layout-template handoffs now use the explicit `layout_reference_template_*` navigation contract and persist a validated `{template_code, revision, projection_fingerprint}` as `layout_reference_handoff` in the frozen material snapshot, ProductionVariant, live-room configuration and clone business inputs. The Live Room workbench displays the immutable near-layout reference and rejects only malformed handoffs. It never requires a ContentProject to select that layout template, never injects it into content-template selection and explicitly states that it cannot produce executable Maitu layers. Plan detail projects the frozen reference for review. Schema, focused UI/navigation tests, full backend `694 passed, 128 skipped`, full frontend `107 passed`, Ruff, TypeScript and Vite builds passed. | N/A: a handoff proves only the selected layout-template projection identity; no current API independently reads/revalidates that template projection or performs similarity, copyright/PII, source-analysis, human review or provider gates. The stored layout remains reference-only and has no executable BuildPlan influence. Strict items stay open. | 2026-07-25 |

| `CHK-3104`,`CHK-3106`,`CHK-3109`,`CHK-3110`,`CHK-3181`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commit `a7d10db`; migration `083_functional_video_timeline_segments` adds an immutable `TimelineSegment` projection for every fixed video clip and every newly created video-timeline revision. New plan creation, queued edits, restore-as-new-revision and branch creation persist separate segment rows and `ShotProjectionLink` evidence in the same transaction. Each row freezes the confirmed source Shot, edit start/end interval, source range, transform/crop/overlay/audio projection, transition, visual artifact reference and canonical fingerprint. The plan API returns the current revision's segments; the Video workspace shows each clip's source Shot and TimelineSegment code in its fixed-input trace. Regression proves compiled clips retain their source Shots, and the PostgreSQL journey asserts an edit plus restore keeps three independent projection revisions rather than overwriting the original. Full backend `695 passed, 128 skipped`, full frontend `107 passed`, Ruff, TypeScript and all three Vite builds passed. | N/A: this is a persistent partial projection, not yet the full ProductionTimeline contract. The timeline remains OTIO-compatible JSON with millisecond fields rather than authoritative OTIO objects/RationalTime; it has no media PTS/session TimeMapping, AssetFile checksum/version per segment, direct ScriptBlock/voice/subtitle artifact links, formal timeline diff/stale graph or historical-plan backfill. The PostgreSQL integration test is present but skipped locally because `ASSETGRAPH_TEST_DATABASE_URL` is unset. It requires a current local test database migrated through `083` before any strict Phase 3 item may be checked; no external API or credential is required. | 2026-07-25 |

| `CHK-3110`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `73aa1dc`; the Video workspace now calculates a concise structural delta for the current revision and every historical revision against its direct predecessor. The history exposes fixed clip order, duration, visual source/range, transition, fit, playback rate, crop focus, overlay roles, subtitle/headline changes, voice gain, poster frame and global subtitle style, with a bounded summary for larger revisions. The restore journey UI regression proves a prior revision's reordered clips, source range and transition are rendered as concrete differences before restore is invoked. Focused Console regression (`6 passed`) and TypeScript passed. | N/A: the comparison is a local UI summary over stored JSON revisions. It is not a formal OTIO/TimeMapping diff, has no source PTS/frame comparison, artifact checksum/voice/subtitle lineage comparison, changed-field approval, stale-impact graph, visual frame diff or server-side diff artifact. No external API, credential or source sample is needed for this increment; strict Phase 3 checkboxes remain open. | 2026-07-25 |

| `CHK-3106`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commit `844c871`; migration `084_functional_video_timeline_segment_script_sources` freezes each TimelineSegment's authoritative source Shot-to-ScriptBlock mapping. Segment persistence reads `shot_script_block_sources` from the confirmed source ShotList in the same transaction, includes the ordered block codes in the canonical segment fingerprint and API projection, and the fixed-input trace displays them next to the source Shot. PostgreSQL regression requires every newly created, edited and restored segment projection to retain ScriptBlock codes; the Console regression verifies that an API projection is visible to the operator. Full backend `695 passed, 128 skipped`, full frontend `107 passed`, Ruff, TypeScript and all three Vite builds passed. | N/A: existing segment rows intentionally remain readable with an empty code list; no backfill invents historical evidence. This establishes Shot/ScriptBlock provenance only. AssetFile checksum/version and render-time voice/subtitle artifacts are not yet direct TimelineSegment links, and PTS/TimeMapping, OTIO RationalTime, stale graph and artifact-level diff remain open. The PostgreSQL journey is present but skipped locally until `ASSETGRAPH_TEST_DATABASE_URL` targets a database migrated through `084`; no external API or credential is required. | 2026-07-25 |

| `CHK-3106`,`CHK-3121`,`CHK-3124`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commit `d6c7e2d`; migration `085_functional_video_timeline_segment_execution_artifacts` adds an append-only per-TimelineSegment execution-evidence table. Completing a functional rendered-video job's `voice_synthesis` or `subtitle_generation` stage now resolves the current frozen TimelineSegment revision and records the current job attempt, registered artifact identity, relative path, checksum and segment-scoped evidence. Voice records freeze the actual per-shot synthesized segment checksum/path and fitting data; subtitle records freeze that shot's timed caption events and text-complete state. Existing planning-time TimelineSegment rows are never overwritten, duplicate stage completion cannot replace a prior link, and the plan API plus Video workspace expose the linked evidence beside the Shot and ScriptBlock trace. The pipeline now emits `shot_code` in each voice manifest to make this mapping explicit. Focused backend regression: `52 passed, 9 skipped`; Video workspace regression: `6 passed`; the complete backend suite (`695 passed, 129 skipped`), complete frontend suite (`107 passed`), Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: links currently cover registered voice-manifest and shared subtitle-track artifacts for the current plan revision only. They do not yet freeze source AssetFile/rendition version, visual clip, render manifest/video/poster, word-level subtitle timing, media PTS/TimeMapping, OTIO RationalTime, artifact-level revision diff or a stale-impact graph. The PostgreSQL end-to-end journey is implemented but skipped locally because `ASSETGRAPH_TEST_DATABASE_URL` is not configured; it requires a local migrated test database through `085`, not an external API, credential or sample. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3106`,`CHK-3121`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commit `392914b`; migration `086_functional_video_timeline_segment_asset_files` adds a direct `TimelineSegment -> AssetFile` relation for source video, with a foreign key plus frozen asset code, file role, object key, source path, MIME type, byte size and checksum. The compiler carries a selected local visual source's checksum/path into the clip source range, then resolves only a checksum-identical `AssetFile` and persists that immutable snapshot alongside the segment fingerprint. The plan API and Video fixed-input trace expose the source-file identity. PostgreSQL regression creates an AssetFile, verifies every segment is linked to its exact frozen file metadata, and keeps database-generated time outside the deterministic comparison. Focused backend suite (`38 passed, 6 skipped`), Video workspace regression (`6 passed`), complete backend suite (`695 passed, 129 skipped`), complete frontend suite (`107 passed`), Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: legacy/local visual assets without a matching `AssetFile` remain usable for now and deliberately yield no direct source-file relation; the UI does not pretend that they have one. There is no AssetFile revision model, rendition selection, historic plan backfill, PTS/TimeMapping, OTIO RationalTime, visual/render artifact link or database integration evidence in this environment. The PostgreSQL journey needs a local `ASSETGRAPH_TEST_DATABASE_URL` migrated through `086`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3121`,`CHK-3125`,`CHK-3142`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commit `389edfd`; migration `087_functional_video_timeline_segment_render_artifacts` extends the append-only execution-evidence roles with `render_manifest`, `rendered_video` and `poster`. At rendering-stage completion, each current TimelineSegment receives the registered manifest and final-video artifact with the frozen attempt/path/checksum and RenderManifest fingerprint. A poster is linked only to the half-open segment interval containing its declared poster time. Duplicate completion remains non-destructive through the existing `(segment, attempt, role)` uniqueness rule; the Video fixed-input trace shows all linked roles. The PostgreSQL journey completes voice, subtitle and rendering stages, proves every segment receives manifest/video lineage, proves only the poster's segment receives the poster role and asserts the exact link count. Focused backend suite (`25 passed, 9 skipped`), Video workspace regression (`6 passed`), complete backend suite (`695 passed, 129 skipped`), complete frontend suite (`107 passed`), Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: RenderManifest content is still the current worker manifest, not an OTIO/RationalTime manifest with PTS/TimeMapping; contact sheet, render log, individual frames and QC diagnostics are not linked per segment. This work does not implement a render diff, strict quality/release gate, approval/delivery or historic-plan backfill. The PostgreSQL journey requires a local `ASSETGRAPH_TEST_DATABASE_URL` migrated through `087`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3106`,`CHK-3109`,`CHK-3121`,`CHK-3142`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commits `95a30bf`,`679fc1a`; migration `088_functional_video_timeline_segment_source_media_probes` adds the `source_media_probe` execution-evidence role. The local AssetSelector now carries real `ffprobe` video-stream time base, start PTS, start-time and average frame-rate into its checksummed asset-plan artifact. Completing asset selection maps that evidence by frozen Shot/TimelineSegment and records selected source interval, playback rate and segment's half-open edit interval. The link is written only when time base and start PTS are actually available, so unavailable probe fields never become fabricated PTS evidence. The fixed-input trace now projects the safe PTS/time-base, source range and playback-rate fields next to that artifact role. Media unit regression verifies all probe fields pass through the selected asset plan; the PostgreSQL journey verifies six segment mappings together with voice, subtitle and render evidence. Focused backend suite (`48 passed, 9 skipped`), Video workspace regression (`6 passed`), complete backend suite (`695 passed, 129 skipped`), complete frontend suite (`107 passed`), Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: source PTS metadata is local-file probe evidence, not a full frame-accurate OTIO RationalTime editing model or a mapping to a LiveSession/recording clock. The mapping does not yet model discontinuities, VFR frame PTS, source rendition versions, drift/anchor confidence, rendered output PTS, stale impact or historical backfill. PostgreSQL verification requires local `ASSETGRAPH_TEST_DATABASE_URL` migrated through `088`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3104`,`CHK-3105`,`CHK-3106`,`CHK-3108`,`CHK-3110`,`CHK-3181`,`CHK-3182` preparation update (items remain open) | coderdailyone | commit `c8b7979`; every newly compiled or edited functional ProductionTimeline now persists `otio-compatible-production-timeline.v2` with `OTIO_SCHEMA:Timeline.1`, an explicit 1000 Hz editorial rate and rational `{value, rate}` TimeRange projections. Timeline, Track and Clip ranges are materialized from the stable millisecond worker projection; video source ranges receive a rational source range and every transition has an explicit rational duration. The update path reprojects all tracks after order/duration/audio/subtitle changes, so revision records and TimelineSegment fingerprints carry the same time semantics rather than a one-time create-only field. Unit coverage verifies reordered video/audio/subtitle tracks, source range and fade ranges; the PostgreSQL journey verifies a created plan's global RationalTime contract. Focused backend suite (`38 passed, 6 skipped`), complete backend suite (`695 passed, 129 skipped`), complete frontend suite (`107 passed`), Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: these are OTIO-compatible JSON data objects, not instantiated OpenTimelineIO objects. Millisecond values remain the worker compatibility boundary; the model does not yet quantify VFR frames, media handles, cross-clip transition overlap, PTS-to-edit transforms, discontinuities, external session clock alignment, imported historical-plan backfill or an OTIO file interchange adapter. The PostgreSQL journey is skipped without local `ASSETGRAPH_TEST_DATABASE_URL`; no external API, credential or sample is required. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3104`,`CHK-3105`,`CHK-3106`,`CHK-3110`,`CHK-3121`,`CHK-3142`,`CHK-3147`,`CHK-3182`,`CHK-3292` preparation update (items remain open) | coderdailyone | commit `fb4eea9`; Functional Video plan creation now writes the complete frozen ProductionTimeline into the queued Worker ShotList. Timeline edits, restore-as-new-revision and branches regenerate that field from the current immutable timeline, so the Worker cannot silently render an earlier timeline while the plan API shows a later revision. RenderManifest now detects this functional input, records the exact OTIO-compatible RationalTime document, its independent canonical fingerprint and the enclosing Worker ShotList fingerprint. Legacy jobs without the field retain a declared `worker_shot_list.v1` manifest path. Unit regressions cover timeline re-projection, legacy compatibility and the exact functional manifest contract; the PostgreSQL journey asserts a newly seeded job holds the same timeline as its plan. Complete backend `696 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: the frozen document is still an in-process JSON projection rather than a serialized OpenTimelineIO interchange file, and FFmpeg consumes the derived Worker ShotList rather than an OTIO adapter. There is no retry-to-retry rendered-media structural diff, frame/PTS output comparison, external tool resource capture, VFR transform, media-handle/rendition model or release replay verdict. The PostgreSQL assertion requires a local `ASSETGRAPH_TEST_DATABASE_URL` migrated through `088`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3121`,`CHK-3122`,`CHK-3125`,`CHK-3142`,`CHK-3147`,`CHK-3184`,`CHK-3292` preparation update (items remain open) | coderdailyone | commit `8ebb2c5`; when a rendering retry finds a prior RenderManifest under the same local job, it emits a downloadable `render_manifest_diff` artifact. The deterministic report freezes both manifest fingerprints, the prior relative path, input-section changes (renderer, timeline, inputs, commands, toolchain and encoding), per-output checksum changes and a deliberately conservative classification: `identical`, `input_changed` or `output_changed_with_fixed_inputs`. It does not invent a root cause for differing bytes. Migration `089` also repairs the production database artifact-key constraint to accept the already-emitted `contact_sheet` and the new diff artifact, aligning it with the API/schema contract. Unit coverage verifies fixed-input output divergence and input/toolchain change classification; migration inventory is updated. Complete backend `697 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: a report compares manifest structure and checksums only. It does not execute a replay from a sealed environment, compare decoded frames/audio/PTS, capture CPU/GPU or process resource usage, identify a specific FFmpeg/library cause, evaluate media rights, or make a release approval decision. The report appears only after a local retry with a preserved prior attempt; PostgreSQL integration still requires local `ASSETGRAPH_TEST_DATABASE_URL` migrated through `089`. No external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3106`,`CHK-3121`,`CHK-3124`,`CHK-3145`,`CHK-3182`,`CHK-3291` preparation update (items remain open) | coderdailyone | commit `f292403`; the rendered-video compiler now carries each selected source Shot's fixed ScriptBlock codes into its Worker ShotList and subtitle track. ASS generation freezes source Shot, ordered ScriptBlock codes and a monotonic token timing range on every caption event; the subtitle manifest declares `text_weight_estimate_v1` and a token count. TimelineSegment subtitle-artifact evidence persists the same event fields beside the immutable subtitle checksum, enabling an operator to trace caption timing back to ScriptBlocks without relying on display text alone. Regressions verify source-block propagation, token completeness/monotonicity and timing-source labelling; the PostgreSQL journey asserts the seeded Worker input retains ScriptBlock provenance. Complete backend `698 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: timing is deterministic text-weight estimation bounded by the caption event interval, not forced alignment to synthesized audio, phoneme/word timestamps, karaoke rendering or human correction. It has no timestamp confidence, multilingual segmentation model, per-word visual safe-area test, imported subtitle format, or direct ScriptBlock-offset mapping. The PostgreSQL assertion remains skipped until local `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `089`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3125`,`CHK-3142`,`CHK-3162`,`CHK-3184`,`CHK-3292` preparation update (items remain open) | coderdailyone | commit `9428a2f`; successful local rendering now emits a downloadable `delivery_metadata` artifact at `render/delivery-metadata.xmp`. The deterministic UTF-8 XMP sidecar carries IPTC DigitalSourceType for trained algorithmic media, the rendered-video checksum, a stable document identifier, source asset codes, creator tool and an explicit rights-pending statement. Its own checksum and candidate-pending state are frozen into RenderManifest before that manifest is written, so the video, sidecar and manifest can be handed to an operator together. Migration `090` extends the database artifact contract and the API allowlist; XML escaping and no-local-path behavior are covered by regression. Complete backend `699 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: this is a local XMP/IPTC sidecar, not embedded media metadata and not a C2PA Content Credential. It intentionally has no ReleaseManifest identifier because it is generated before a candidate is created; it declares candidate-pending and rights-pending rather than an approval, license or channel delivery. No signature/ingredient chain, platform-specific metadata writer, delivery readback or rights validator is present. PostgreSQL integration requires local `ASSETGRAPH_TEST_DATABASE_URL` migrated through `090`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3125`,`CHK-3143`,`CHK-3146`,`CHK-3183`,`CHK-3184`,`CHK-3292`,`CHK-3293` preparation update (items remain open) | coderdailyone | commit `26dc05d`; the local FFmpeg quality gate now reads stream-level ffprobe metadata rather than relying only on container duration. It blocks a rendered video unless video frame rate is 30.00 fps within 0.01 fps and independently reported video/audio stream durations differ by at most 100 ms. The QC report stores both durations, their delta and frame rate; the Video workspace renders FPS and the A/V delta beside codec and sample-rate evidence. Media regression covers a 30 fps / 75 ms-sync result, and the Console regression asserts the operator-facing values. Complete backend `700 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript, all three Vite builds and `git diff --check` passed. | N/A: this validates the single locally rendered H.264/AAC profile only. It does not handle variable frame-rate policy, decoded per-frame PTS, lip-sync perception, channel layout, frame pacing/jitter, source-to-output TimeMapping, adaptive tolerances or a human QC waiver. If stream-level duration is missing, the gate blocks rather than assuming container duration proves synchronization. PostgreSQL integration remains unavailable without local `ASSETGRAPH_TEST_DATABASE_URL`; no external API, credential or sample is needed. No strict checkbox is checked. | 2026-07-25 |

| `CHK-3180`,`CHK-3181`,`CHK-3182`,`CHK-3184` preparation update (items remain open) | coderdailyone | commit `ffdfbae`; `/production/videos` now links the rendered-video player to the fixed ProductionTimeline without becoming a general NLE. The preview exposes a bounded time slider and one explicit jump command per frozen Shot; selecting a command seeks only the local player to that clip's timeline start. Poster and contact-sheet preview remain alongside it, and no interaction mutates the timeline, asset selection, render result or release. Console regression verifies the player, bounded slider and both Shot commands; complete frontend `107 passed`, TypeScript and all three Vite builds passed. | N/A: this is local playback navigation, not frame-exact editing. It has no decoded PTS seek confirmation, waveform, source-media preview, clip trim/drag, keyboard transport, thumbnail scrub, audio track solo/mute, frame stepping, playback review record or rendered-vs-source comparison. No backend or external dependency is introduced; strict Phase 3 checkboxes remain open. | 2026-07-25 |

| `CHK-3124`,`CHK-3145`,`CHK-3146`,`CHK-3184`,`CHK-3293` preparation update (items remain open) | coderdailyone | commit `ff6982d`; `subtitle-quality-v1` now validates the frozen ASS manifest against the fixed ShotList at the actual QC stage. It blocks the report on incomplete/truncated text, invalid 1080x1920 safe margins, manifest count mismatch, out-of-Shot or invalid event timing, oversized lines, caption display below a deterministic reading lower bound, and required ScriptBlock codes absent from caption evidence. The rendered-video UI exposes the compact check count, safe margins, source-block coverage and issue codes alongside existing QC diagnostics. Complete backend `703 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript and all three Vite builds passed. | N/A: this is deterministic manifest validation only. It does not inspect rendered subtitle pixels with OCR, calculate visual contrast/occlusion, force-align captions to phonemes/audio, validate all languages, or provide an operator waiver. Those limits remain explicit; strict Phase 3 checkboxes stay open until their full definitions and exit evidence are met. | 2026-07-25 |

| `CHK-0111`,`CHK-0112`,`CHK-A007` maintenance update (items remain open) | coderdailyone | `docs/operations/external-platform-register.md` now contains a stable `EXT-*` non-blocking input queue. It centralizes the minimum evidence needed for fact sources, release signing/readback, recording samples, media rendition/audio metadata, video delivery, C2PA, metric/observation data, knowledge sources and platform/provider use. The queue states exactly which future production action is blocked and which local implementation may continue; credentials and personal data are expressly excluded. Local PostgreSQL/model/media-tool conditions are listed separately and are not misclassified as external APIs. | N/A: no external account, credential, recording, metric batch, signing key or source material was requested or supplied. This is an implementation-continuation control, not an external-dependency approval; all related strict checklist items remain open. | 2026-07-25 |

| `CHK-A007` maintenance update (item remains open) | coderdailyone | The non-blocking input queue now records `EXT-SCM-PRIESTESS-BOT`: a read-only check of the requested `https://github.com/priestess-bot/AssetGraph.git` remote returned repository-not-found / invalid credential, while the configured `origin` remains readable. The remote was not rewritten, no credential was stored in the repository and local implementation continues. | N/A: only the account-migration/push action is blocked. A reachable private repository URL and usable write credential are required later; no product implementation is blocked. | 2026-07-25 |

| `CHK-2202`,`CHK-2205`,`CHK-5120`,`CHK-5121`,`CHK-5122` preparation update (items remain open) | coderdailyone | commit `af84e94`; migration `091` links each newly created or branched rendered-video plan to one immutable `rendered_video_material_selection` DecisionLog. The record freezes the selected material snapshot, group/material-pack expansion, per-asset checksum, constraint snapshot, source ContentProject/ProductionVariant revisions, plan/timeline revision and branching source where applicable. The API and Video workspace expose the decision code beside the frozen materials. The decision is created in the same local plan-write transaction, while pre-existing plans remain explicitly null rather than receiving invented history. Complete backend `703 passed, 129 skipped`, complete frontend `107 passed`, Ruff, TypeScript and all three Vite builds passed. | N/A: this records an explicit local selection, not a ranked recommendation or proof of actual release/exposure. It does not capture an unselected candidate universe, semantic/effect score, propensity, rights grant, platform delivery or causal outcome. PostgreSQL end-to-end assertions are present but skipped until a local `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `091`; no external API, credential or source sample is required. No strict checkbox is checked. | 2026-07-25 |

| `CHK-7101`,`CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | FactClaim now has `GET /api/functional-knowledge/fact-claims/{claim_code}/lineage` and a Knowledge workspace `查看使用链` drill-down. The read model starts only from the immutable `fact_claim_refs`/stored claim code in a ContentProject revision, then follows direct source-revision relationships through StoryBrief, Script, Program, ShotList and ProductionVariant. Where local rows exist, it also projects rendered-video/live-room plans, their Release, a source-backed operational exposure session and an EffectEstimate whose frozen subject snapshot contains the same claim. It returns current claim/fact/source state beside each dated relation and never changes stored lineage. Route/API/UI regressions pass (`5` backend route tests; `13` focused frontend tests), Ruff and TypeScript pass. A PostgreSQL journey verifies the project-to-ShotList portion but is skipped here because `ASSETGRAPH_TEST_DATABASE_URL` is not configured. | N/A: this is a local relational, demand-time traversal rather than a GraphProjection, outbox/watermark, rebuild/reconciliation mechanism or hybrid lexical/vector/graph retrieval. It does not include every possible compliance/terminology object, release delivery attempt, metric bucket or effect whose subject was not frozen with this claim. A search result, a current selection or an inferred relationship is not emitted as historical use and is never authorization; callers must still apply approval, validity, rights and scope checks before new content use. Strict Phase 7 items remain open. | 2026-07-25 |

| `CHK-7102`,`CHK-7104` preparation update (items remain open) | coderdailyone | migration `092` adds a separate local `ContentRule` resource for `content_guidance`, `compliance_rule`, `term` and `expression_ban`; it is not stored as a ProductFactCard or FactClaim. Rules have an explicit `guidance`/`must_include`/`must_avoid` directive, UTC validity window, platform scope, canonical fingerprint and draft/approved/rejected/revoked review lifecycle. Compliance, term and expression-ban rules require approved SourceEvidence; the expression-ban directive is constrained to `must_avoid`. The Knowledge workspace can create and review rules, while the ContentProject creation workspace offers approved rules. Selection freezes rule kind/text/directive/scope/source checksum/fingerprint into the immutable project revision; confirmation and generation re-resolve approval, source, scope and validity. Mandatory include/avoid directives are carried into DesignBrief, StoryBrief and every generated Shot. Focused route/unit/migration checks (`41 passed, 15 PostgreSQL skips`), complete backend `707 passed, 131 skipped`, complete frontend `111 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: rules are compact local entries, not a legal/compliance ontology, source connector, extraction/offset model, policy-as-code engine, approval role/ACL system, contradiction resolver, automated text scanner, human exception/waiver workflow or external regulatory update feed. `guidance` is frozen into generation context for a future compiler but is not interpreted by the deterministic text skeleton; only `must_include`/`must_avoid` currently change generated constraint fields. PostgreSQL persistence journeys require `ASSETGRAPH_TEST_DATABASE_URL` migrated through `092`. No strict checkbox is checked. | 2026-07-25 |

| `CHK-7103`,`CHK-7104` preparation update (items remain open) | coderdailyone | migration `093` adds nullable `citation_start_offset`/`citation_end_offset` to FactClaim. New claim creation accepts a complete explicit half-open character interval or deterministically finds the first exact citation excerpt in the approved locally captured evidence text; it rejects a partial, out-of-range or text-mismatched interval. The selected interval participates in the claim fingerprint, returns through the API/UI, and is copied into the immutable ContentProject claim reference. Historical claims remain intentionally null rather than receiving reconstructed positions. Focused route/migration/API checks pass; complete backend `709 passed, 131 skipped`, complete frontend `111 passed`, Ruff, TypeScript and all Vite builds passed. | N/A: offsets are Unicode character indexes in the locally stored SourceEvidence excerpt, not byte offsets, PDF/page coordinates, immutable captured-document artifacts, OCR/ASR spans, web-DOM selectors or an external source-system locator. Auto-selection is deterministic first-match behavior; an operator must provide an explicit interval for a repeated quote. The PostgreSQL proof remains skipped until `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `093`. No strict checkbox is checked. | 2026-07-25 |

| `CHK-7102`,`CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | migration `094` gives `content_script_blocks` a `content_rule_refs` projection. The deterministic content compiler inserts one 15-second `content_rule` block for each distinct pinned literal `must_include` text, freezes the matching rule code/kind/text/fingerprint on that ScriptBlock, and exposes it in the Content Project script view. Before either deterministic generation or human script revision, the service re-resolves the pinned claim/rule set and applies an exact case-folded text check: a required literal absent from the assembled script yields `CONTENT_RULE_REQUIRED_TEXT_MISSING`; a forbidden literal yields `CONTENT_RULE_BANNED_TEXT`. Human revisions receive rule references from the server after validation rather than trusting a client payload. Focused backend unit/migration checks and Content API checks pass. | N/A: this is an exact-string compiler check only. It does not detect paraphrases, homophones, images/audio, semantic policy violations, legal applicability, conflicting rules, waivers or human approval. `guidance` remains non-blocking context. The added PostgreSQL journey is skipped here until a disposable `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `094`; it verifies generation-to-readback persistence. No strict checkbox is checked. | 2026-07-25 |

| `CHK-7102`,`CHK-7104` preparation update (items remain open) | coderdailyone | The Content Project "事实与规则版本" revision panel now reads the same approved, source-valid content-rule set as project creation and can add/remove rule codes alongside fact-card and FactClaim pins. Saving creates the existing immutable project revision and lets the server re-pin the selected rules; it does not accept client-supplied frozen rule references. Historical selected rules that are no longer eligible are shown separately rather than silently preserved as selectable. The focused Content Project interaction test verifies the exact PATCH body for a rule-only change. | N/A: the selection view has no legal approval role, waiver, policy conflict solver, bulk migration or semantic preview. Current approved/source-valid status remains a precondition for a new revision, and generation still performs its own validity/scope re-resolution. No strict checkbox is checked. | 2026-07-25 |

| `CHK-7103`,`CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | migration `095` adds a local immutable `SourceExtractionRun` behind every newly registered SourceEvidence. Source registration atomically freezes the extractor strategy, source-input fingerprint, excerpt-output checksum, metadata, operator and creation time; SourceEvidence reads return newest-first runs, and `GET /api/functional-knowledge/source-evidences/{evidence_code}/extraction-runs` exposes the run history. The Knowledge `来源证据` workspace sends the strategy at registration and displays strategy, run count and newest output checksum. Focused contract/migration tests passed (`42 passed, 6 skipped`); focused Knowledge API/UI tests passed (`15 passed`), with Ruff and TypeScript passing. | N/A: this supports only manually entered or locally controlled excerpts. It neither acquires a remote document nor stores an immutable original file, page/DOM locator, OCR/ASR output, connector request/response evidence, retention policy, ACL decision or source-system credential. `EXT-P7-KNOWLEDGE-SOURCES` remains the non-blocking record for approved source systems and controlled documents; work on other local checklist items continues without waiting for it. PostgreSQL persistence remains skipped until a disposable `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `095`. No strict checkbox is checked. | 2026-07-25 |

| `CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | `GET /api/functional-knowledge/search` now provides one bounded local search across SourceEvidence, FactClaim and ContentRule. It evaluates every hit at a supplied or current UTC instant: lifecycle approval, source approval, validity window and declared platform scope return independently with stable blocking codes. Search results expose `content_eligible` separately from `authorization_eligible`; the latter is deliberately false because source rights grants are not modeled here. The Knowledge `知识检索` workspace accepts query/platform context and renders those checks rather than treating text match as permission. Route/API/UI coverage includes timezone rejection, scope mismatch, expired claims and the no-authorization projection; complete backend passed (`715 passed, 134 skipped`), complete frontend passed (`114 passed`), Ruff, TypeScript and all Vite builds passed. | N/A: this is bounded relational ILIKE retrieval, not a vector/graph ranker, ACL engine, RightsGrant resolver, field-conflict resolver, projection watermark or complete evidence authorization service. It does not query ProductFactCard revisions, retain a historical source snapshot beyond the local evidence model, or alter existing production selection logic. `rights=not_modeled` remains an explicit unresolved condition; `EXT-P7-KNOWLEDGE-SOURCES` records source-owner inputs while local work continues. PostgreSQL behavior is covered but skipped without a disposable `ASSETGRAPH_TEST_DATABASE_URL`; no strict checkbox is checked. | 2026-07-25 |

| `CHK-7101`,`CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | The local knowledge search now also joins each ProductFactCard only to its `current_approved_version`, rather than creating a parallel fact copy or returning a mutable latest draft. It searches fixed version content, returns the exact revision number and evaluates active/approved lifecycle, JSON validity window and `applicable_platforms` through the same response model as FactClaim and ContentRule. ProductFactCard search coverage includes an in-process fixed-version scope check and a PostgreSQL journey; complete backend passed (`716 passed, 135 skipped`), complete frontend passed (`114 passed`), Ruff, TypeScript and all Vite builds passed. | N/A: legacy ProductFactCard `source_references` remain declared content, not managed SourceEvidence links or rights grants. The search result is therefore still `authorization_eligible=false`, does not resolve field conflicts or historical superseded versions, and does not change ContentProject's established server-side revalidation. PostgreSQL execution remains skipped without `ASSETGRAPH_TEST_DATABASE_URL`; no strict checkbox is checked. | 2026-07-25 |

| `CHK-7104`,`CHK-7105` preparation update (items remain open) | coderdailyone | The Knowledge `知识检索` workspace now exposes the API's point-in-time revalidation context through an optional local-time `核验时间` field. On submission it converts the browser value to an offset-bearing UTC `as_of` parameter, preserving the endpoint's rejection of timezone-free timestamps; the query cache key includes the normalized instant so results do not mix different effective times. Focused Knowledge API/UI tests passed (`17 passed`) with TypeScript and `git diff --check`. | N/A: this is a read-only inspection context, not a persisted decision, timezone policy/locale configuration, historical snapshot restore, rights authorization or effective-time conflict resolver. No strict checkbox is checked. | 2026-07-25 |

| `CHK-1202`-`CHK-1226` preparation update (items remain open) | coderdailyone | migration `096` adds explicit local plan projections for `maitu_running`, `maitu_reconcile_required` and `maitu_failed`. A confirmed Functional Live Room now freezes only a secret-free Worker handoff: persisted BuildPlan code, target room, checkpoint contract, source-plan fingerprint and operation summary. The Worker continues to use the existing fenced script-layout protocol; a new plan-local readback sync reads its durable execution result and only projects `maitu_complete` after a matching finalized `script_layout_draft` execution, frozen source fingerprint, operation count and `ready_for_go_live=false`. The Live Room workspace presents the projected status and an explicit sync action. | N/A: no actual platform mutation or readback has been performed in this repository. `FT-2103` and all strict Phase 1 checklist items remain open until `EXT-P2-MAITU-DRAFT-WORKER` supplies a target-scoped Worker credential, disposable empty non-live room, current platform contract and authoritative final readback evidence. The local unit/contract path continues without waiting for those inputs. | 2026-07-25 |

| `CHK-2210` preparation update (item remains open) | coderdailyone | Asset Library now reads an asset's versioned effect-estimate revisions through `GET /api/assets/{asset_code}/effects` and displays the metric, revision, evidence level and selected-session count beside its group, material-pack and gap relations. The API computes recommendation eligibility explicitly: only an approved `associational` effect with at least three selected sessions is eligible; every other projection returns stable blocking codes. The UI defaults to disabled automatic recommendation when there is no sample, a small sample, descriptive evidence, an unapproved revision, or an unavailable read. Focused route/unit/UI coverage verifies the 2-session, descriptive and eligible boundaries. | N/A: this is an evidence read model, not a selection/ranking mutation or a causal-effect claim. Existing effect creation still produces descriptive candidate evidence, no score is added to the resolver, and the three-session floor is a conservative local UI policy rather than a validated production threshold. Rights, branch context, confounder controls, metric quality and approval roles remain outside this slice; the strict checklist item remains open. | 2026-07-25 |

| `CHK-2204`,`CHK-2205` preparation update (items remain open) | coderdailyone | Both the read-only Material Library candidate preview and Functional Live Room compiler now use the same bounded qualified-effect contribution: an approved `associational` asset effect with at least three selected sessions adds five explainable points; no qualifying evidence remains neutral at zero. Candidate and selected-decision projections contain the effect code, revision, metric and session count, and every new live-room material snapshot freezes those refs before BuildPlan generation. The Material Library shows the code/revision in its candidate explanation. Unit/UI coverage verifies positive evidence wins an otherwise tied candidate and that missing evidence is not penalized. | N/A: the score is deliberately not an effect magnitude, quality/semantic/aspect score, causal conclusion, or automatic-selection authorization. Existing local effect creation yields descriptive candidates, so operators need an independently qualified association before the five-point evidence can appear. There is still no RightsGrant merge, geometry solver, repetition policy calibration, branch-context eligibility, score-weight calibration, or production experiment proof; strict checklist items remain open. | 2026-07-25 |

| `CHK-2203`,`CHK-2205`-`CHK-2206`,`CHK-2209`,`CHK-2306` preparation update (items remain open) | coderdailyone | commit `1a0d4ff`; `POST /api/functional-live-room-plans/material-gap-preview` now inspects the generated ContentProject's fixed Shot-list revision against the current loose assets, expanded groups and published material-pack resolution. It uses the same role, `maitu_bound` capability and classification-pack `replace` filtering as the BuildPlan compiler, then returns grouped missing Shot codes, a deterministic diagnostic key, project/Shot revision provenance, pack-resolution fingerprint and bounded library candidates. The Live Room workspace can run this read-only diagnosis and register one result as a normal open `AssetGap`; its source context is retained and the new gap is selected for the current plan input. No diagnosis silently substitutes an asset, creates a plan or changes a gap state. Backend route/rule, frontend API/UI tests and complete suites passed: backend `724 passed, 135 skipped`; frontend `117 passed`; Ruff, TypeScript, `git diff --check` and all Vite builds passed. | N/A: this detects only absent executable role coverage in the current input, not semantic suitability, media-format/duration, geometry, overlap, RightsGrant, source rights, candidate quality, occurrence maximums, solver feasibility or cross-operator duplicate registration. A `replace` diagnostic can name a library candidate but does not add it to a required classification pack. Dynamic scene-code-only scope rules cannot be proven before a new variant exists; stable Shot-code and scene-type scopes are evaluated. Strict checklist items remain open. | 2026-07-25 |

| `CHK-2200`,`CHK-2203`,`CHK-2209` preparation update (items remain open) | coderdailyone | commit `52bee93`; non-`replace` live-room material-gap candidates can now be selected directly into the current plan's loose-material input from the diagnosis result. The control is idempotent in the browser and makes the selected state explicit; the operator then reruns the read-only diagnostic before creating a plan. A `replace` result remains intentionally non-actionable at this point and explains that the candidate must be added through its classification MaterialPack, preserving the compiler's hard replacement-domain rule. Focused Live Room interaction regression (`4 passed`), TypeScript and `git diff --check` passed. | N/A: this is a convenience selection, not an automatic resolution, ranking authorization, pack mutation, rights decision, constraint preflight or solver proof. Selecting a candidate neither removes the stored AssetGap nor bypasses any BuildPlan blocker; creation remains the explicit operator action. Strict checklist items remain open. | 2026-07-25 |

| `CHK-7105`,`CHK-7122`-`CHK-7125`,`CHK-7128` preparation update (items remain open) | coderdailyone | commit `970eb92`; migration `097` adds a local, disposable `GraphProjectionVersion` snapshot with typed nodes and edges. A manual rebuild reads only existing SourceEvidence, FactClaim, ContentRule, immutable ContentProject revisions, ProductionVariant revisions and EffectEstimate rows, freezes counts/watermark/fingerprint, supersedes the prior completed snapshot and exposes current/explicit projection APIs. The graph stores `SUPPORTS`, `CITES`, `DERIVED_FROM` and `ESTIMATED_EFFECT_ON` edges with source table/code/revision evidence, validity where available, assertion kind and confidence. Descriptive effects are explicitly labelled `descriptive_association`, never a recorded fact. The Knowledge Console workspace can show an absent/current/stale snapshot, its nodes and edges, and trigger an explicit local rebuild. Full backend `729 passed, 135 skipped`; full frontend `119 passed`; Ruff, TypeScript, all Vite builds and `git diff --check` passed. | N/A: this is a PostgreSQL full-snapshot projection, not Neo4j/Milvus, embedding/vector retrieval, lexical-plus-graph reranking, outbox incremental consumption, projection deletion propagation, three-query capacity benchmark or complete ontology. It currently omits assets/rights, templates, plans/releases/exposures, metrics, strategies and experiments when an authoritative fixed relation is not available; it never invents those edges. PostgreSQL persistence integration remains skipped until a disposable `ASSETGRAPH_TEST_DATABASE_URL` is migrated through `097`; no external API, credential or source sample is required for this increment. No strict checkbox is checked. | 2026-07-25 |

## 附录 C. 权威设计覆盖索引

| 权威设计章节 | 主要 checklist 覆盖 | 最终核验 |
| --- | --- | --- |
| 第 1-4 节：目标、决策、范围、产品信息架构 | 使用规则、Phase 0.9、Phase 7.5、Phase 7.8 | `CHK-8101`、`CHK-8291` |
| 第 5-7 节：素材、约束、分组、素材包、缺口 | Phase 2.1-2.6 | `CHK-2390` 至 `CHK-2397` |
| 第 8 节：外部录屏与内容策略模板 | Phase 2.7-2.9 | `CHK-2395`、`CHK-2396` |
| 第 9-12 节：内容聚合、生成、优先级和数据对象 | Phase 0.2、Phase 1、Phase 2.9 | `CHK-1290` 至 `CHK-1295` |
| 第 13-14 节：API 与状态机 | `DOD-001` 至 `DOD-004`、各 Phase API/状态任务 | `CHK-8121`、`CHK-8126`、`CHK-8127` |
| 第 15 节：安全、审计、版本、证据和可复现性 | Phase 0.3-0.5、Phase 1.6、Phase 8.6-8.7 | `CHK-8200` 至 `CHK-8227` |
| 第 16-17 节：现状差距与迁移 | Phase 0.1、Phase 0.9、Phase 8.3 | `CHK-0294`、`CHK-8140` 至 `CHK-8145` |
| 第 18-19 节：实施顺序与验收场景 | Phase 0-7 退出门禁、Phase 8.1、Phase 8.8 | `CHK-8102`、`CHK-8240` 至 `CHK-8247` |
| 第 20 节：成片、剪辑与时间轴 | Phase 3 | `CHK-3290` 至 `CHK-3294` |
| 第 21 节：运营、指标、归因和实验 | Phase 4、Phase 5.1、Phase 6.2 | `CHK-4290` 至 `CHK-4294`、`CHK-6290` 至 `CHK-6292` |
| 第 22-23 节：知识图谱、效果学习与再生产 | Phase 5.2-5.5、Phase 7.1-7.2 | `CHK-5291` 至 `CHK-5295`、`CHK-7290`、`CHK-7291` |
| 第 24 节：Shot、排播和角色策略 | Phase 0.2、Phase 1.3/1.5、Phase 6、Phase 7.3-7.4 | `CHK-1291`、`CHK-6290`、`CHK-7292`、`CHK-7293` |
| 第 25 节：运行、发布与执行授权 | Phase 0.3-0.6、Phase 1.6-1.7 | `CHK-0291` 至 `CHK-0293`、`CHK-1293` |
| 第 26 节：隐私、权利、保留和删除 | Phase 0.8、Phase 2.2、Phase 7.6、Phase 8.6 | `CHK-7294`、`CHK-8203` 至 `CHK-8207` |
| 第 27-28 节：容量、SLO、成本、灾备、标准和采用门槛 | Phase 0.1、Phase 3.4、Phase 7.7、Phase 8.7/8.9 | `CHK-7295`、`CHK-8220` 至 `CHK-8227`、`CHK-8269` |
| 第 29-31 节：最终对齐、完成定义和成熟实践 | Phase 8.9-8.10 | `CHK-8290` 至 `CHK-8297` |
